# 服务器部署说明 — 广告方向决策子智能体（ad-direction-agent）

> 面向 Linux 生产服务器（建议 8 核 16G）。本文覆盖：依赖组件、`.env` 需改哪些、如何启动多 worker、验证与排查。
> 目标运行形态：**uvicorn 6 worker + MySQL 状态后端 + Redis 缓存**，对接外部 Doris 数仓与 MCP 网关。

---

## 1. 架构与依赖组件

```
                    ┌─────────────────────────────────────┐
   运营浏览器 ─────▶│  ad-direction-agent (FastAPI/uvicorn) │
                    │  6 worker 进程，端口 8000             │
                    └───────┬───────────┬───────────┬──────┘
                            │           │           │
              ┌─────────────▼──┐  ┌─────▼─────┐  ┌──▼──────────────┐
              │ MySQL          │  │ Redis     │  │ DeepSeek API     │
              │ ad_agent_state │  │ 缓存+锁   │  │ (flash, 50 key)  │
              │ (长期状态)     │  │           │  └──────────────────┘
              └────────────────┘  └───────────┘
                            │
              ┌─────────────▼──────────────┐   ┌──────────────────┐
              │ Doris/StarRocks 数仓(只读)  │   │ MCP 网关(数据拉取)│
              └─────────────────────────────┘   └──────────────────┘
```

| 组件 | 角色 | 部署方 | 多 worker 影响 |
|------|------|--------|---------------|
| **MySQL** `ad_agent_state` | 长期状态（战略/策略/运营输入/P3/Campaign 结果） | 自建（本机或同内网） | **必须**，JSON 后端多 worker 会损坏文件 |
| **Redis** | 短期 ASIN 数据缓存 + 跨 worker 幂等锁 | 自建（本机或同内网） | 多 worker 幂等锁的强依赖 |
| **Doris/StarRocks** | 业务数仓（只读拉数） | 外部已有 | 无（只读） |
| **MCP 网关** | 亚马逊广告数据拉取 | 外部已有 | 无 |
| **DeepSeek API** | LLM 推理 | 外部 SaaS | 并发上限按 worker 切分（见 §5、§6） |

---

## 2. 前置条件

- **Python ≥ 3.11**（`pyproject.toml` 要求）
- Linux（Ubuntu/CentOS 均可）；不要用 `ad-direction-agent/start_server.py`，那是本地开发机辅助脚本（含 Windows netstat 等）
- 可访问：Doris（如 `36.133.119.99:9030`）、MCP 网关、DeepSeek API（公网）
- 本机或内网可用的 MySQL 8.0 与 Redis 7

---

## 3. 基础设施准备

### 3.1 MySQL（状态库）

只需建库 + 授权用户，**表结构由服务启动时自动创建**（`MySQLStateManager.ensure_schema()` 读 `app/persistence/schema.sql`，并跑 `migrate_json_to_mysql`）。

```sql
CREATE DATABASE IF NOT EXISTS ad_agent_state CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'ad_agent'@'%' IDENTIFIED BY '强密码改这里';
GRANT ALL PRIVILEGES ON ad_agent_state.* TO 'ad_agent'@'%';
FLUSH PRIVILEGES;
```

> 若用仓库根目录的 `docker-compose.yml` 起 MySQL/Redis：它把 MySQL 映射到宿主机 **3307**（容器内 3306）。自建本机 MySQL 通常是 **3306**——`.env` 的 `STATE_DB_PORT` 要对应实际端口。

### 3.2 Redis

默认无密码、`db=0` 即可：

```bash
# 自建或 docker：
docker run -d --name redis-cache -p 6379:6379 --restart unless-stopped redis:7-alpine
redis-cli ping   # 期望 PONG
```

### 3.3（可选）用 docker-compose 一键起基础设施

仓库根 `docker-compose.yml` 提供 `mysql-state`(3307) + `redis-cache`(6379)。**只用它起 MySQL/Redis 即可**，应用本身用 §6 的 uvicorn 直起（compose 里的 app 容器默认 `--workers 1`，不适合本场景）：

```bash
docker compose up -d mysql-state redis-cache
```

---

## 4. 代码与依赖安装

```bash
# 假设代码在 /opt/ad_agent（含 ad-direction-agent/ 与 ad-purpose-agent/ 两个子项目）
cd /opt/ad_agent/ad-direction-agent
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

> **重要**：`ad-direction-agent` 运行时依赖同级的 `ad-purpose-agent`（策略层 `agent_router`）。启动时必须把 `ad-purpose-agent` 加入 `PYTHONPATH`（见 §6）。

---

## 5. `.env` 配置（相对现有 `.env` 的增改）

`.env` 路径固定为 **`ad-direction-agent/.env`**（pydantic-settings 从这里加载，已存在、含真实密钥，**勿提交仓库**）。
下面以**当前 `.env` 实际内容**为基准，只说要**新增/改动**的项；其余（Doris/MCP/Redis/超时等）已配好，保持不变。

### 5.1 ⚠️ 必须新增的变量（当前 `.env` 没有）

```ini
# ── 多 worker 并发治理（新增，缺失会导致并发不切分）──
NUM_WORKERS=6              # 必须 = 启动命令 --workers，否则每 worker 槽数算错
LLM_GLOBAL_CONCURRENCY=240 # 全服务 LLM 总并发（代码默认已 240，建议显式写明）
```

> 不加 `NUM_WORKERS`：默认 1 → 信号量不切分 → 6 个 worker 各开 240 = 总 1440，**会打爆 DeepSeek 账号**。

### 5.2 必须改动的变量

| 变量 | 当前 `.env` 值 | 服务器改为 | 原因 |
|------|---------------|-----------|------|
| `DEEPSEEK_MODEL` | `deepseek-chat` | **`deepseek-v4-flash`** | 本期定的模型 |
| `DEEPSEEK_API_KEYS` | 10 个 key | **50 个 key（逗号分隔）** | 扩容到 50 key/多账号 |
| `STATE_DB_PORT` | `3307` | 视部署：docker-compose 仍 **3307**；自建本机 MySQL 改 **3306** | 端口对齐实际监听 |
| `STATE_DB_PASS` | `ad_agent_pass` | **强密码** | 生产不要用默认弱密码 |
| `DB_PASS` / `MCP_GATEWAY_TOKEN` | 现值 | 确认生产可用凭据 | 若生产用不同账号 |

### 5.3 可清理（可选）

| 变量 | 当前值 | 说明 |
|------|--------|------|
| `CAMPAIGN_GLOBAL_LLM_CONCURRENCY` | `30` | 已 **DEPRECATED**（全局闸收口到 client 层），现在是**无作用**的遗留项，可删可留 |
| `LLM_TIMEOUT` | `90` | 现作 httpx **read** 超时；保持 90 即可（connect 超时已硬编码 8s） |

### 5.4 不用动的（已配好）

`DATA_SOURCE=mcp`、`DB_*`(Doris)、`MCP_*`(网关/超时)、`REDIS_*`、`STATE_BACKEND=mysql`、各 `DB_*_TIMEOUT` —— 当前 `.env` 均已正确，保持。

> 唯一要再确认的部署项：`STATE_BACKEND` 当前已是 `mysql` ✅（多 worker 必须，别改回 json）；`REDIS_ENABLED=true` ✅（幂等锁依赖）。

---

## 6. 启动（6 worker）

**不要用 start_server.py。** 直接 uvicorn，并把 `ad-purpose-agent` 加入 `PYTHONPATH`。

### 6.1 命令行手动起（验证用）

```bash
cd /opt/ad_agent/ad-direction-agent
source .venv/bin/activate
PYTHONPATH=/opt/ad_agent/ad-purpose-agent \
NUM_WORKERS=6 \
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 6 --no-reload
```

启动后每个 worker 应打印：
```
LLM 并发闸初始化: 总上限=240 / worker数=6 → 本 worker 槽=40
```
据此核对 `NUM_WORKERS` 与 `--workers` 是否一致（都应是 6，每 worker 40 槽）。

### 6.2 systemd（生产推荐）

`/etc/systemd/system/ad-direction-agent.service`：

```ini
[Unit]
Description=Ad Direction Agent (FastAPI)
After=network.target mysql.service redis.service

[Service]
Type=exec
User=adagent
WorkingDirectory=/opt/ad_agent/ad-direction-agent
Environment=PYTHONPATH=/opt/ad_agent/ad-purpose-agent
Environment=NUM_WORKERS=6
ExecStart=/opt/ad_agent/ad-direction-agent/.venv/bin/uvicorn app.main:app \
    --host 0.0.0.0 --port 8000 --workers 6 --no-reload --log-level info
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now ad-direction-agent
sudo systemctl status ad-direction-agent
journalctl -u ad-direction-agent -f      # 跟踪日志
```

> 应用日志同时写 `ad-direction-agent/logs/server.log`（启动时自动建目录）。

---

## 7. 验证

```bash
# 1. 进程数（应 6 worker + 1 master）
ps -ef | grep uvicorn | grep -v grep

# 2. 端口监听
ss -ltnp | grep :8000

# 3. 健康/前端
curl -s http://127.0.0.1:8000/demo/ad-asisitant-agent.html -o /dev/null -w "%{http_code}\n"

# 4. MySQL 表已自动建（启动日志应有 "MySQL state schema ensured"）
mysql -u ad_agent -p ad_agent_state -e "SHOW TABLES;"

# 5. Redis 连通（首次 LLM/缓存调用后日志应有 "Shared Redis 已连接"）
redis-cli ping
```

端到端：跑一次真实 ASIN 的 `POST /campaign/analyze`，观察 `logs/server.log` 无连接池耗尽/泄漏告警、无 429 雪崩。

---

## 8. 多 worker 注意事项（务必确认）

| 项 | 要求 | 现状 |
|----|------|------|
| `NUM_WORKERS` == `--workers` | 必须相等（都 6） | 由你启动命令保证；启动日志可核对 |
| `STATE_BACKEND=mysql` | 必须 mysql（json 会多进程写坏文件） | `.env` 设定 |
| Redis 可用 | 幂等锁强依赖；Redis 挂则锁退化为进程内（多 worker 失效） | 建议监控/高可用 |
| LLM 总并发 | 240 静态切给各 worker（6→每 worker 40） | 代码已实现 `240 // NUM_WORKERS` |
| **DeepSeek 账号级限流** | 240 并发摊到账号上需账号扛得住 | ⚠️ **上线前确认**：50 key 散在几个账号？flash 单账号 RPM/并发上限多少？账号数偏少会成瓶颈 |

> 暂未做（不阻塞上线）：Key 冷却跨进程共享（多 worker 下各自感知 429，会快速自愈）；如后续要根治，改为冷却时间戳存 Redis。

---

## 9. 升级 / 重启

```bash
cd /opt/ad_agent && git pull
cd ad-direction-agent && source .venv/bin/activate && pip install -r requirements.txt
sudo systemctl restart ad-direction-agent
```

> 改 `.env`、改 `NUM_WORKERS`/`--workers`、或升级代码后**必须完整重启**（uvicorn worker 数与事件循环策略不能热加载）。

---

## 10. 常见排查

| 现象 | 排查 |
|------|------|
| 启动即 `ModuleNotFoundError`（purpose-agent） | `PYTHONPATH` 未含 `ad-purpose-agent` |
| 启动报 MySQL 连接失败 | `STATE_DB_*` 地址/端口/密码；`3306` vs `3307` |
| 大量 429 / LLM 失败 | DeepSeek 账号级限流；调低 `LLM_GLOBAL_CONCURRENCY` 或增加账号分散 key |
| 每 worker 槽数不对（日志显示非 40） | `NUM_WORKERS` 与 `--workers` 不一致 |
| 同 ASIN 被重复分析 | Redis 不可用导致锁退进程内；查 `REDIS_URL`/`REDIS_ENABLED` |
| 状态丢失/串数据 | `STATE_BACKEND` 仍是 json；改 mysql 并重启 |
| `workflow_state.json` 出现写冲突 | 同上，多 worker 必须 mysql |

---

## 附：相关文档
- LLM 客户端收口与并发治理：`交接文档.md` §7.5
- Campaign 引擎与 Timeout 全景：`docs/Campaign分析引擎交接文档.md` §3.2
