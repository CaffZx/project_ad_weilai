# 广告方向决策子智能体 — 交接文档

> **最后更新**: 2026-06-01
> **版本**: v3.0 / 文档 v4.2

---

## 1. 项目概述

**项目名称**：广告方向决策子智能体（Ad Direction Agent）

**项目定位**：面向亚马逊广告运营的 AI 辅助决策系统，基于标签驱动架构，通过四层递进工作流（战略→策略→诊断→执行），为运营人员提供数据驱动的广告方向推荐。

**数据源**：Doris 数仓（36.133.119.99:9030，MySQL 协议）+ MCP StarRocks 网关（`DATA_SOURCE=mcp` 优先，失败降级 Doris），222 张表中核心使用 9 张表。

**技术栈**：Python 3.13 / FastAPI / PyMySQL / DeepSeek API / LangGraph（可选，`USE_LANGGRAPH=false` 默认） / Pydantic

---

## 2. 项目结构

> 只列架构关键文件，`__init__.py`/次要适配器/备用文件从略。★=近期新增或关键。

```
AD_assistant_agent-v2.5/                  # 仓库根
├── 交接文档.md                            # 本文档（主服务交接）
├── 服务器部署说明.md                      # ★Linux 部署（env/启动/多worker/验证）
├── README.md
├── ERP数据库接入文档1.md / 2_campaign.md   # ERP 表结构与字段映射
├── docker-compose.yml / Dockerfile        # MySQL+Redis 基础设施 + 镜像
├── .env.example                           # 环境变量模板（无密钥）
├── docs/
│   ├── knowledge_base/                    # ★KB 唯一源：14 篇 + 执行规则/ + ontology/（YAML 本体）
│   └── Campaign分析引擎交接文档.md         # Campaign 模块交接
├── scripts/                               # erp_db 批量/审计脚本、KB 实验脚本
├── ad-purpose-agent/                      # ★广告目的 AI（4 目的权威源；PYTHONPATH 引用，勿移目录）
└── ad-direction-agent/                    # 方向决策主服务

ad-direction-agent/app/
├── main.py                                # FastAPI 入口（路由+CORS+静态+shutdown 关 client）
├── config/settings.py                     # 全局配置（DB/LLM/ERP/并发/Redis）+ tags/thresholds/layer_options.toml
├── api/                                   # HTTP 路由（前缀 /api/v1/agent/ad-direction/）
│   ├── strategy / tactics / diagnosis / execution.py   # 四层工作流端点
│   ├── campaign.py                        # ★Campaign 分析 + ERP 自动写入（_maybe_push_erp）
│   └── wizard / long_term_config / feedback / chat / llm / deps.py
├── workflow/
│   ├── context.py / data_summary.py / data_contract.py / data_gate.py   # 上下文 + 数据完整性闸门
│   └── steps/
│       ├── strategy / tactics / diagnosis / execution / p3 / validation_report / wizard.py
│       ├── campaign.py                    # ★Campaign 分析引擎（分流+R1R2投票+sanity+Redis 幂等锁）
│       ├── campaign_portfolio.py          # ★组合分类（精准主力/精准测试/自动广泛/低价捡漏，$5 分界）
│       └── campaign_budget_summary.py     # ★预算约束汇总
├── core/
│   ├── workflow_orchestrator.py           # 薄编排器（数据缓存 + 委托 steps）
│   ├── data_aggregator / scenario_analyzer / recommender / validation_engine / decision_package.py
│   └── metrics_ops_language / validation_ops.py        # 运营文案归一化
├── data/
│   ├── db_adapter.py                      # Doris 数仓适配器（核心，含 campaign + trend GROUP BY 查询）
│   ├── mcp_adapter / mcp_client / mcp_fetch_run / mcp_mapping / mcp_normalizers.py   # MCP 管道（并发 sem）
│   ├── mcp_db_context.py                  # ASIN→SKU/店铺解析（+ known_listings 回落）
│   ├── doris_fallback / fallback_merge.py # MCP 失败/空表 → Doris 回落
│   ├── campaign_fetcher / campaign_prefilter.py        # ★Campaign 取数 + 硬过滤
│   └── known_listings.py                  # ASIN→SKU/店铺 兜底表
├── models/
│   └── asin_data / layers / campaign / decision / validation / feedback.py
├── rules/                                 # 18 条规则：push_natural / expand_keywords / optimize_acos / balance_maintain / cross_tag
├── llm/
│   ├── client.py                          # ★统一 LLM 网关（共享连接池 + 服务级并发闸 + 重试退避）
│   ├── key_pool.py                        # ★多 Key 轮询池（429 冷却 + 落盘）
│   ├── reasoner.py                        # Prompt 组装 + 响应解析（默认复用共享 client）
│   ├── kb_loader.py                       # KB preset 切片注入（ENUM_MAP 英→中）
│   └── purpose_adapter.py                 # purpose-agent 跨项目桥接
├── persistence/
│   ├── state_manager / mysql_state_manager / state_factory.py   # 状态后端（STATE_BACKEND=json|mysql）
│   ├── redis_client.py                    # ★共享 Redis 单例 + 分布式锁（acquire_lock/release_lock）
│   ├── redis_cache.py                     # ASIN 数据缓存（Redis + 进程内 LRU 兜底）
│   └── erp_writer/                        # ★ERP 双写（Campaign/方向 → ERP 测试库）
│       ├── auto_push.py                   # write_full 入口 + payload 组装
│       ├── repository / mappers / models.py
│       ├── text_utils.py                  # 枚举/字段映射（含组合码、wizard content_json）
│       └── listing_context.py
└── skills/                                # 运行时 Skill（playbook 驱动 MCP 取数）：loader/registry/validate + executors/mcp_query.py

ad-direction-agent/                        # （app/ 之外）
├── demo/ad-asisitant-agent.html           # 主看板前端（四层工作流）
├── demo/campaign_test.html                # ★Campaign 分析前端
├── config/{ASIN}/*.json                   # JSON 后端时逐 ASIN 持久化（long_term/workflow/p3/override/adjustment_history/feedback）；MySQL 后端则入库
├── logs/server.log                        # 运行日志（含 Campaign/Stream timing、MCP elapsed、并发闸初始化）
├── tests/                                 # pytest（含 erp_writer / mcp 测试）
├── .env / .env.example                    # 环境变量（.env 含密钥，不入库）
├── pyproject.toml / requirements.txt
└── start_server.py                        # 本地开发机启动脚本（服务器用 uvicorn/systemd，不用它）
```

---

## 3. 四层工作流

### 3.1 流程概览

```
ASIN → 战略层(人工) → 策略层(AI推荐+人工) → 诊断层(只读) → 执行层(AI推荐+人工) → 校验+报告
```

### 3.2 各层详解

| 层级 | 角色 | 输入 | 输出 | API |
|------|------|------|------|-----|
| **1.1 战略层** | 人工选择 | ASIN | product_level / product_stage / season_stage | `POST /strategy/options` → `POST /strategy/confirm` |
| **1.2 策略层** | AI推荐+人工 | 战略层选择 + 诊断数据 | ad_purpose / keyword_type | `POST /tactics/options` → `POST /tactics/confirm` |
| **1.3 诊断层** | 只读（不可编辑） | 战略+策略上下文 | 北极星指标看板 + 告警信号 + 关键词监控表 | `POST /diagnosis`（刷新时缓存快照检测场景 → 一次按需查询，不再双重查库） |
| **1.4 执行层** | AI推荐+人工 | 全上下文 + P3 LLM 统一推荐 | 4方向评分 + LLM推荐方向 + 目标ACOS/预算Bid（手动覆盖 > LLM > 算法降级） | `POST /execution/options` → `POST /execution/confirm` |
| **1.5 校验+报告** | 自动 | 执行选择 | 规则校验 + LLM分析报告 | `POST /validate_and_report` |

### 3.3 关键代码入口

- **编排器**：[app/core/workflow_orchestrator.py](app/core/workflow_orchestrator.py) — `WorkflowOrchestrator` 类
- **API 路由**：[app/api/](app/api/) 下按层分文件
- **前端**：[demo/ad-asisitant-agent.html](demo/ad-asisitant-agent.html) — 左右分栏看板式，4 药丸标签切换

---

## 4. Doris 数仓对接

### 4.1 核心表

| 表名 | 数据域 | 行数 | 最新数据 |
|------|--------|------|----------|
| `dwd_whp_amazon_listing_general` | ASIN 主数据 | 186K | 实时 |
| `dwd_whp_az_extend` | ASIN 扩展属性 | 198K | 实时 |
| `dwd_az_asin_gross_profit` | **利润表（毛利率/库存/销售额）** | ? | 每日 |
| `dwd_amazon_ad_product_report_update` | 广告商品级报告 | 20M | ~当前 |
| `dwd_amazon_ad_placement_report_update` | 广告位报告 | 46M | 滞后~50天 |
| `dwd_amazon_ad_keyword_report` | 关键词级广告报告 | 36M | ~当前 |
| `dwd_amazon_listing_flow_keyword_us/uk/de` | 流量关键词 | US: 2.5M | 实时 |
| `dwd_amazon_asin_keyword_library` | ASIN 关键词排名 | 7.5M | ~7天内 |
| `dwd_amazon_precise_keyword_library` | 关键词搜索量库 | 1M | 实时 |
| `dwd_az_cw_asin_prod_info` | 竞品 ASIN 信息 | 14M | 实时 |

### 4.2 DbAdapter 核心方法

**[app/data/db_adapter.py](app/data/db_adapter.py)** — `DbAdapter` 类（~1050行）：

| 方法 | 查询内容 | 对应元脚本 |
|------|----------|-----------|
| `_fetch_listing()` | ASIN 主数据 — 子ASIN聚合（LEFT JOIN, SUM/加权平均） | 固定执行（非元脚本） |
| `_fetch_ad_summary()` | 广告汇总指标（ACOS/CPC/CVR/花费，参数化7/14/30天） | `META_AD_PRODUCT` |
| `_fetch_placement_summary()` | 精准(TOS) vs 非精准(ROS) 广告位拆分（参数化窗口） | `META_AD_PLACEMENT` |
| `_fetch_ad_keywords()` | 关键词级广告表现（前200条+rank_change） | `META_KW_AD` |
| `_fetch_natural_rankings()` | 关键词自然排名（每个keyword取最佳子ASIN排名，7天窗口硬编码） | `META_KW_COMPETITOR_RANK` + `META_KW_SUB_ASIN_RANK` |
| `_fetch_competitors()` | 竞品列表（价格/评分/BSR） | `META_COMPETITOR` |
| `_fetch_flow_keywords()` | 流量关键词（可用于拓词） | `META_FLOW_KEYWORD` |
| `_fetch_gross_profit()` | 销售额/广告/毛利/可售库存（TACOS+毛利率+can_sale_num） | 固定执行 |
| `_fetch_trend_data()` | 按日聚合趋势数据（跟随主 days 参数） | `META_TREND` |
| **★`_fetch_campaign_context()`** | **Campaign 上下文发现（仅维度：campaign_name/child_asin/keyword_text/match_type/keyword_bid）** | **Campaign 独立调用** |
| **★`_fetch_campaign_perf_from_db()`** | **单活动指标回落（按 campaign_name 聚合 7d）** | **MCP 回落** |
| **★`_fetch_campaign_placement_from_db()`** | **单活动 placement 回落（按 campaign_id 查询，四桶聚合）** | **MCP 回落** |

### 4.3 重要数据源迁移记录（最新：2026-05-11）

| 指标 | 原来（错误） | 现在（正确） | 数据源 |
|------|------------|------------|--------|
| **毛利率** | `gross_profit_amount / order_sale_amount`（自创公式，产出 115% 等异常值） | `gross_profit_amount_proportion` 按 `order_sale_amount` 加权平均（Doris 表预计算字段） | `dwd_az_asin_gross_profit` |
| **库存量** | `fba_stock` SUM（日级快照重复累加）→ MAX（取峰值，不准确） | `can_sale_num` 最新日期所有子 ASIN SUM（父 ASIN 口径可售库存） | `dwd_az_asin_gross_profit` |
| **日均销量** | `SUM(month_sale_num)`（各子ASIN的日均值叠加，值放大~15倍） | `seven_sale_num / 7`（7天总销量/7，各子ASIN加总后做除法） | `listing_general`（`_fetch_listing` 聚合计算） |
| **自然排名去重** | `ORDER BY craw_time DESC`（取最新爬虫，不保证最优排名） | `ORDER BY keyword ASC, craw_nature_rank ASC`（每个 keyword 取最优子 ASIN） | `dwd_amazon_asin_keyword_library` |
| **排名显示** | 只展示有排名词（`craw_nature_rank IS NOT NULL`），无排名词被过滤 | 展示 10 有排名 + 3 高花费无排名词，区分"已掉榜"/"未入榜" | `dwd_amazon_asin_keyword_library` |
| **ACOS 告急阈值** | 30% | 40% | `scenario_analyzer.py` |

**关键结论**：
- `dwd_whp_az_extend.single_gross_profit` 全为 NULL，该字段已废弃
- `listing_general.in_stock_num` 全为 0，真实库存数据在 `dwd_az_asin_gross_profit`
- 毛利率使用 Doris 表预计算的 `gross_profit_amount_proportion`，按销售额加权平均
- 库存使用最新日期的所有子 ASIN `can_sale_num`（可售库存）求和
- 自然排名取每个 keyword 最优子 ASIN 的 `craw_nature_rank`，配合 `near_craw_nature_rank` 判断"已掉榜"

### 4.4 DbAdapter 连接与查询

```python
# 连接（自动重连）
conn = await asyncio.to_thread(pymysql.connect, host=..., cursorclass=DictCursor)
# 使用 DictCursor，返回字段名→值的字典

# 核心查询——一次拉全量（不增量加载）
data = await adapter.fetch_asin_data(asin)
```

### 4.5 查询参数化

所有广告查询方法（`_fetch_ad_summary` / `_fetch_placement_summary` / `_fetch_ad_keywords` / `_fetch_gross_profit`）均支持 `days` 参数（7/14/30，默认 7 天）。**90 天降级机制已于 Phase 1 移除**。placement 表更新滞后 30-50 天，7 天窗口可能无数据（前端需处理精准/非精准 ACOS 为空的情况）。

趋势数据跟随主 `days` 参数，自然排名查询硬编码 `INTERVAL 7 DAY`（不参与参数化）。

### 4.6 查询路由

**[app/core/query_router.py](app/core/query_router.py)** — `QueryRouter` 实现了场景驱动的选择性查询：

- 基础必查：`META_KW_AD` + `META_COMPETITOR` + `META_AD_PRODUCT`
- 场景扩展（如 `acos_crisis` 追加 `META_AD_PLACEMENT` + `META_AD_SEARCH_TERM`）
- 执行方向扩展（如 `expand_keywords` 追加 `META_FLOW_KEYWORD`）
- **[计划扩展]** `META_TREND` 加入基础必查（趋势数据）

### 4.8 MCP 数据管道

**当前默认**：`.env` 配置 `DATA_SOURCE=mcp`，MCP 优先，失败降级 Doris。`MCP_FALLBACK_TO_DB=true` 启用回落。

**MCP 工具映射**（[mcp_mapping.py](app/data/mcp_mapping.py)）：
- 9 个 META 维度 → 12 个 MCP 工具（ASIN 级）
- 4 个 CAMPAIGN_TOOLS（campaign 级）：`ad_campaign_basic_info` / `ad_campaign_product_report` / `ad_campaign_placement_report` / `ad_campaign_search_term_report`

**MCP 中英文字段兼容**（v2.6）：`mcp_normalizers.py` 新增 `_pick(row, *aliases)` 和 `_row_key_index(row)`，支持 MCP 返回中文列名（`花费`/`点击量`/`搜索词`/`曝光量` 等），同时兼容英文 key。

**数据完整性闸门**（v2.6）：[data_contract.py](app/workflow/data_contract.py) 定义四模块契约（required/important/optional），[data_gate.py](app/workflow/data_gate.py) 在 LLM 调用前评估数据完整性 → blocked（拦截）/ degraded（降级含 missing_notice）/ ok。

### 4.9 特殊注意事项

- **Doris 大小写**：列名大小写不敏感，但 SQL 关键字 `INTERVAL 3 DAY` 不能写成 `NOW()-3`
- **连接保持**：DbAdapter 缓存单个连接（`self._conn`），闲置超时后自动重连一次
- **`_fetch_listing` 聚合逻辑**：
  - 数值字段（库存/销量/花费/评价数）：所有子 ASIN **SUM**
  - 退款率：**月销量加权平均**
  - 评分：**7天销量加权平均**
  - 价格：优先取 `buy_box_price`
  - 上架日期：取最早（`min()`）
- **执行顺序**（`_fetch_all`）：listing → 并行(ad / placement / keywords / rankings / competitors / flow / profit) → 组装 → 返回

---

## 5. 场景分析系统

### 5.1 8 种场景（严格优先级）

优先级从高到低，ACOS > 40% 覆盖除清货期外的所有场景：

| 优先级 | 场景 ID | 触发条件 |
|--------|---------|----------|
| 1 | `acos_crisis` | ACOS > 40% 且产品阶段 ≠ 清货期 |
| 2 | `clearance` | 产品阶段=清货期 或 库存=0 |
| 3 | `cold_start` | 测试期 + 引流型/排名型 |
| 4 | `rapid_growth` | 推进期 + 引流型 |
| 5 | `push_ranking` | 推进期 + 排名型 |
| 6 | `profit_harvest` | 收割利润期/维持期 + 盈利型 |
| 7 | `stable_conversion` | 收割利润期/维持期 + 转化型 |
| 8 | `default` | 以上均不匹配 |

### 5.2 北极星指标看板

各场景的北极星指标定义在 **[app/core/scenario_analyzer.py](app/core/scenario_analyzer.py)** 的 `METRIC_DEFS` 字典中。

每个指标有阈值（`warning` / `danger`），分别映射 UI 状态颜色：
- **good**（绿色）：指标在安全范围内
- **warning**（黄色）：指标接近警戒线
- **danger**（红色）：指标严重超标
- **neutral**（灰色）：无数据

**重要**：各场景的指标定义是产品需求严格规定的，修改需确认 8 场景对应的字段和阈值是合理且经过业务方确认的。

### 5.3 数据字段取值

**[app/core/scenario_analyzer.py](app/core/scenario_analyzer.py)** `FIELD_GETTERS` 定义各字段从 ASINData 的提取逻辑。关键映射：

| 看板字段 | 数据来源字段 | 转换 |
|---------|------------|------|
| `acos` | `ad_data.acos` | round(值, 1) |
| `tacos` | `ad_data.tacos` | round(值, 1) |
| `cvr` | `ad_data.cvr` | round(值, 1) |
| `cpc` | `ad_data.cpc` | round(值, 2) |
| `precision_acos` | `ad_data.precision_acos` | round(值, 1) |
| `broad_acos` | `ad_data.broad_acos` | round(值, 1) |
| `margin` | `data.margin` | **data.margin × 100**（小数→百分数） |
| `avg_daily_sales_30d` | `data.avg_daily_sales_30d` | round(值, 1) |
| `natural_order_ratio` | `data.natural_order_ratio` | round(值, 1) |
| `inventory_qty` | `data.signals.inventory_qty` | 直接取 |

**注意**：AdData 字段已在 Phase 1 批量去窗口后缀：`acos_14d` → `acos`，`cvr_14d` → `cvr`，`average_cpc` → `cpc`，`spend_14d` → `spend`，`orders_14d` → `orders` 等。新增 `sales` 字段（广告销售额）。

---

## 6. 规则引擎

### 6.1 注册模式

采用 **装饰器模式** 注册规则，详见 [app/rules/__init__.py](app/rules/__init__.py)：

```python
@validation_rule(rule_id="PN-1", direction="push_natural", priority=1)
async def check_something(data, thresholds) -> ValidationItem | None:
    ...
```

### 6.2 规则清单（18 条）

| 方向 | 规则 ID | 优先级 | 简述 |
|------|---------|--------|------|
| push_natural | PN-1 | 1 | 上升/优质词检测（≥2 个良好） |
| push_natural | PN-2 | 1 | 预算倾斜比例检查（>50% 警告） |
| push_natural | PN-3 | 1 | 广告依赖词检测 |
| push_natural | PN-4 | 2 | 上升词 ACOS 检查（>35% 需先优化） |
| push_natural | PN-5 | 2 | 核心排名位置判断 |
| expand_keywords | KE-1 | 1 | 可用新词数量检测 |
| expand_keywords | KE-2 | 1 | 当前覆盖率检查 |
| expand_keywords | KE-3 | 2 | 现有词健康度评估 |
| expand_keywords | KE-4 | 2 | 新增数量合理性判断 |
| optimize_acos | OA-1 | 0 | ACOS 超标（≥50% force_correct） |
| optimize_acos | OA-2 | 0 | 高花费零转化词 |
| optimize_acos | OA-3 | 1 | Bid 下调空间 |
| optimize_acos | OA-4 | 1 | 否定词机会 |
| balance_maintain | BM-1 | 1 | ACOS/销量波动检测 |
| balance_maintain | BM-2 | 1 | 异常信号检查 |
| balance_maintain | BM-3 | 2 | 参数合理性 |
| balance_maintain | BM-4 | 2 | 排名稳定性 |
| balance_maintain | BM-5 | 2 | 子选项阈值 |
| cross_tag | (3条) | 0 | 标签联动约束 |

---

## 7. LLM 集成

### 7.1 模型配置

- **供应商**：DeepSeek API
- **Base URL**：`https://api.deepseek.com`
- **模型**：通过 `.env` 文件配置（`DEEPSEEK_MODEL`）
- **API Key**：通过 `.env` 文件配置（`DEEPSEEK_API_KEY`），不硬编码
- **ad-direction-agent**：[app/config/settings.py](app/config/settings.py) — pydantic-settings 加载 `.env`
- **ad-purpose-agent**：[config.py](ad-purpose-agent/config.py) — 手动解析 `.env`，共享同一份配置

`.env` 模板见 `ad-direction-agent/.env.example`。`.env` 已在 `.gitignore` 中，不提交到仓库。

### 7.2 LLM Prompt 体系

| Prompt | 文件 | 用途 | Temperature |
|--------|------|------|------------|
| Purpose 知识库（必选） | `ad-purpose-agent/knowledge_base/prompt_kb.md` | 策略层推荐：广告目的 + 关键词分类 + 评分 | 0.1 |
| Purpose 关键词 KB（可选） | `ad-purpose-agent/knowledge_base/prompt_keyword_kb.md` | 关键词类型判定（Broad/Long-tail/Competitor/Brand/Custom） | — |
| `EXECUTION_SYSTEM_PROMPT` | `app/llm/reasoner.py` | 执行层方向推荐 | 0.3 |
| `P3_RECOMMEND_SYSTEM_PROMPT` | `app/llm/reasoner.py` | P3 统一推荐（目标 ACOS + 预算/Bid） | 0.3 |
| `SYSTEM_PROMPT` | `app/llm/reasoner.py` | 综合分析报告（含18条规则说明） | 0.3 |

> **注意**：`TACTICS_SYSTEM_PROMPT`（direction 自己的策略层 prompt）已废弃，策略层 LLM 调用统一走 purpose-agent 的 `agent_router`（OpenAI SDK）。

### 7.3 策略层缓存与降级

- **缓存**：`keyword_analysis` 写入 `workflow_state.json` 后，回访跳过 LLM（`is not None` 判断），仅从 DB 刷新排名字段
- **降级**：purpose-agent 失败 → DB 关键词填充（无 AI 分类）。不再调用 direction 的 `reasoner.recommend_tactics` 作为二次降级
- **重新推荐**：`/tactics/recommend`（前端「AI 重新推荐」按钮）重定向到 purpose-agent
- 诊断层：`keyword_analysis` 缺失时防御性调用 purpose-agent（仅首次）
- 执行层推荐失败 → 按评分排序取 Top 2
- P3 统一推荐失败 → 算法降级（confidence=low，标注"算法降级"）
- 综合分析失败 → 返回降级报告

### 7.4 LLM Prompt 接入知识库（v3.1.0）

**2026-05-28：所有 LLM 业务规则统一迁移至 `docs/knowledge_base/`**

此前业务规则分散在三处：
- `reasoner.py` 4 个 SYSTEM_PROMPT 中内嵌的方向定义、评分公式、阈值
- `ad-purpose-agent/knowledge_base/prompt_kb.md` + `prompt_keyword_kb.md`

现在统一由 `docs/knowledge_base/`（19 个 md，v3.1.0）承载，通过 `kb_loader.py` 按需切片注入。

**新增文件**：[app/llm/kb_loader.py](app/llm/kb_loader.py)

- 启动时一次性读盘 → 编译正则 → ENUM_MAP 英→中翻译 → 写入内存缓存
- 5 个 PRESET 对应 5 个 LLM 调用点，每个加载 2-8 个 KB 切片
- 英文 enum 整词替换为 `layer_options.toml` 中的中文 term

| Preset | 调用点 | 加载 KB 文件 | 字符数 |
|--------|-------|-------------|--------|
| `purpose_tactics` | 策略层 (purpose-agent) | 01 02 03 04 06 09 10 14 | 18.7K |
| `execution_direction` | 执行层 (recommend_execution) | 01 02 03 04 05 09 14 22 | 24.0K |
| `p3_recommend` | P3 (recommend_p3) | 01 03 05 09 10 11 14 19 | 18.7K |
| `analyze_report` | 综合分析 (analyze) | 02 05 09 12 14 | 11.6K |
| `chat` | AI 聊天 (chat_stream) | 02 14 | 5.2K |
| **★`campaign_adjustment`** | **Campaign 活动调整 (新增)** | **18 19 21 22** | **~26K** |

**改造的 LLM 调用点**（共 5 处，全部接入 KB）：

| 文件:方法 | 改造内容 |
|----------|---------|
| `reasoner.py:analyze` | `SYSTEM_PROMPT` → `_ANALYZE_TASK_PROMPT` + `_build_analyze_system_prompt()`，移除方向定义表、评分公式、阶段策略 |
| `reasoner.py:recommend_execution` | `EXECUTION_SYSTEM_PROMPT` → `_EXECUTION_TASK_PROMPT` + `_build_execution_system_prompt()` |
| `reasoner.py:recommend_p3` | `P3_RECOMMEND_SYSTEM_PROMPT` → `_P3_TASK_PROMPT` + `_build_p3_system_prompt()`，保留 ACOS ±40% 硬约束 + 字段语义说明 |
| `agent_router.py:determine_ad_targets_from_metrics` | 旧 `prompt_kb.md` + `prompt_keyword_kb.md` → `kb.build("purpose_tactics")` + `_PURPOSE_OUTPUT_RULES` |
| `chat.py:chat_stream` | `CHAT_SYSTEM_PROMPT` 注入 `kb.build("chat")` |

**删除的废弃代码**：
- `reasoner.py`: `TACTICS_SYSTEM_PROMPT` / `recommend_tactics()` / `_fallback_tactics()`（策略层已统一走 purpose-agent）
- `ad-purpose-agent/knowledge_base/`: `prompt_kb.md` + `prompt_keyword_kb.md` → `_archive/`

**保留不从 KB 迁移的三类内容**：
- (a) 输出 JSON schema + 字段语义注释
- (b) KB 未覆盖的输出硬约束（如 ACOS 目标 ±40%，含 TODO 标记）
- (c) 文风/禁用词/叙事结构规则（`OPS_WRITING_RULES`、`REASONING_DECISION_RULES`）

**核心原则**：
1. KB 是业务规则唯一源，阈值/红线/关键词策略以 `docs/knowledge_base/` 为准
2. `layer_options.toml` 是中文 term 唯一源，ENUM_MAP 单向翻译
3. KB 文件只读，开发人员不修改
4. 每轮 LLM 调用按 preset 最小切片加载，不打包全量

### 7.5 LLM 客户端收口与并发治理（v3.3，2026-06-04）

**面向高并发（2000+ 请求 / 峰值 100+）的基础设施收口。** 此前 LLM 传输层是碎片化的：

**改前的 3 个问题**

1. **3 个独立 httpx 连接池**（应为 1 个）。根因：`LLMReasoner.__init__` 写 `client or DeepSeekClient()`（reasoner.py:462），三个模块单例各自无参构造各开各的池——
   - 池1 = `reasoner` 单例（reasoner.py:1758）→ api/campaign、api/deps、api/llm
   - 池2 = `deepseek_client` 单例（client.py:220）→ api/chat 流式
   - 池3 = `workflow_orchestrator.reasoner`（无参构造）→ 所有 workflow 步骤（p3/execution/validation_report 等）
2. **无服务级并发上限**：`_GLOBAL_LLM_SEM` 只管 campaign 批次，其余 reasoner 方法（均走 `self.client.chat()`）裸奔直冲 DeepSeek。
3. **shutdown 连接泄漏**：main.py 只 close 了池2，池1/池3 永不关闭。

**改后（全服务唯一 client + 集中策略）**

| 改动 | 文件 | 内容 |
|------|------|------|
| 单例收口 | `reasoner.py:462` | 默认 client → 共享单例 `deepseek_client`。池1/池3 坍缩为 1，shutdown 泄漏随之消除（main.py 关的就是唯一 client） |
| 连接池 + 超时分离 | `client.py:_ensure_client` | `max_connections=600, max_keepalive=50`；`Timeout(connect=8, read=llm_timeout, write=10, pool=5)` |
| 服务级并发闸 | `client.py:_global_llm_sem` | 模块级 `Semaphore(llm_global_concurrency)`，在 `chat()` 内包裹整个重试循环（一个逻辑请求占一个槽）。**不**加在 `chat_stream()` → AI 聊天不受影响 |
| 重试退避 | `client.py:chat` | `MAX_RETRIES: 2→3`（首次+2 重试）；重试前指数退避 0.5/1.0s + 抖动 50–200ms，打散高并发雷群 |
| key_pool 热路径硬化 | `key_pool.py` | `mark_success` 不再写盘（每次成功同步文件 I/O 会阻塞事件循环）；`mark_failed` debounce（冷却变化 >1s 才写） |
| 共享 Redis / 分布式锁 | `app/persistence/redis_client.py`（新） | `get_redis()`（懒加载单例+熔断）+ `acquire_lock/release_lock`（SET NX EX + compare-and-delete Lua）。campaign 幂等锁从 campaign.py 迁出复用此处 |

**配置**（settings.py）：`llm_global_concurrency = 420`（服务级总并发；46 key 场景,6 worker 时每 worker = 420/6 = 70；按 Key 数 × 单 Key RPM 调）；新增 `num_workers`（读 `NUM_WORKERS`，把全局闸静态切给各 worker）；`campaign_global_llm_concurrency` 标记 DEPRECATED。连接池 `max_connections=600`。

**边界**：本期只做进程内信号量（多 worker 的跨进程分布式信号量、key 冷却 → Redis 待后续）；换供应商仍走 `.env`（`DEEPSEEK_BASE_URL`/`DEEPSEEK_MODEL`），不引入新抽象层。

**分层**：`LLMReasoner`(prompt) → `DeepSeekClient`(传输+策略，本次强化对象) → `ApiKeyPool`(只管 Key)。

---

### 7.6 组合枚举改名 + ERP 自动写入合并 + KB Clearance 修复（v3.4，2026-06-05）

合并 v3.0.2 分支（ERP 接入）+ 修两处枚举/KB。Campaign 侧细节见 `docs/Campaign分析引擎交接文档.md` §3.4。

- **广告组合枚举改名**：`主推/广泛自动/测试新增/淘汰` → `精准主力组/精准测试组/自动广泛组/低价捡漏组`；ERP 英文码 `core/auto_broad/test/eliminate` → `exact_core_group/exact_testing_group/auto_broad_group/low_bid_retention_group`。中文真源 = `campaign_portfolio.py` 常量；ERP 码映射 = `erp_writer/text_utils._CAMPAIGN_GROUP_TYPE_MAP`（含旧值兼容别名，不破历史数据）。**分类逻辑保留我方 $5 预算分界（KB23 §3.1），未采用 v3.0.2 的 14天+$20 口径。**
- **ERP 自动写入**：并入 v3.0.2 `auto_push`——`POST /campaign/analyze` 成功后可选 write_full 到 ERP 测试库（`api/campaign.py:_maybe_push_erp`；默认关，`ERP_AUTO_WRITE=true` 或请求传 `write_erp:true` 开）。新增 settings `erp_auto_write/erp_host/erp_port/erp_user/erp_password/erp_database/erp_write_timeout`；`erp_writer/__init__` 导出 auto_push；`mcp_db_context` 加 known_listings 回落 + read_timeout 提到 ≥90s；`text_utils` 加 `build_wizard_direction_content_json`。
- **KB Clearance 移除**：`docs/knowledge_base/12-输出规范.md` 核心策略标签枚举删除 `Clearance`（它是场景 scenario / 产品阶段 liquidating，非广告目的；广告目的由 ad-purpose-agent 权威产出，仅 Traffic/Conversion/Ranking/Profit 4 个）。`kb_loader.py` ENUM_MAP 注释同步。**Maintain 保留**（核心策略标签合法值，非广告目的）。
- **ad_keyword_report 遗留注释**：`mcp_mapping.py` 加注——MCP 服务端已拆分该工具，直调返 `Unknown tool`，按 META_KW_AD 稳定回落 Doris，**非 bug**，暂不动。

---

## 8. 反馈收集系统 (v3.4)

### 8.1 设计目的

通过收集运营对 AI 推荐结果的真实评价，积累纠错数据以迭代 prompt 和规则引擎。

### 8.2 前端交互

- **入口**：顶部栏右侧「📝 反馈」按钮，括号内数字表示已自动填充的模块数
- **弹窗表格**：6 列 — 模块 / AI判断 / 人的判断 / 采纳情况* / 纠错理由 / 备注建议
- **自动预填**：各层确认/保存操作触发数据捕获，AI判断和人的判断自动填入
  - 策略层确认 → 广告目的推荐 + 目标关键词推荐
  - P3 ACOS/预算保存 → 目标ACOS推荐 + 每日预算推荐
  - 执行方向确认 → 广告方向推荐
- **校验**：5 个模块的"采纳情况"必须全部选择（防误触），纠错理由始终可选
- **提交后**：显示"感谢反馈，持续优化中，祝工作顺利！"

### 8.3 数据模型

```python
class FeedbackModule(BaseModel):
    module: str                              # "广告目的推荐" | "目标关键词推荐" | ...
    ai_judgment: str | list[str] | None      # AI 推荐值
    human_judgment: str | list[str] | None   # 人工选择值（未手动设定="未手动设定"）
    adoption: str                            # "采纳" | "可以优化" | "完全不采纳"
    correction_reason: str = ""              # 纠错理由
    notes: str = ""                          # 备注

class FeedbackSubmission(BaseModel):
    id: str                                  # {timestamp}_{asin}
    parent_asin: str
    session_start: str                       # 前端首次加载 ISO 时间戳
    submitted_at: str                        # 提交 ISO 时间戳
    has_error: bool = False
    error_info: str | None = None
    modules: list[FeedbackModule]
```

### 8.4 存储格式

```
config/{ASIN}/feedback/{timestamp}_{ASIN}.json
```

- 每个反馈一个独立 JSON 文件，不追加（避免并发写覆盖）
- `timestamp` 使用 `-` 替换 `:` 以兼容 Windows 文件系统
- 完整示例：`feedback/2026-05-14T13-06-59Z_B0B7S3PWWB.json`

### 8.5 导出方式

| 方式 | 命令 | 产物 |
|------|------|------|
| API | `GET /feedback/export?format=json` | 合并 JSON |
| API 筛选 | `GET /feedback/export?asin=B0B7S3PWWB&format=json` | 单 ASIN JSON |
| 一键脚本 | `bash scripts/pull_feedback.sh -o report.md` | Markdown（AI可读） |
| 转换脚本 | `python scripts/feedback_to_md.py export.json -o report.md` | 同上 |

导出不对运营开放，仅 AI 工程团队使用。

### 8.6 关键文件

| 文件 | 说明 |
|------|------|
| [app/api/feedback.py](ad-direction-agent/app/api/feedback.py) | 3 个端点：提交 / 列表 / 导出 |
| [app/models/feedback.py](ad-direction-agent/app/models/feedback.py) | Pydantic 模型 |
| [app/persistence/state_manager.py](ad-direction-agent/app/persistence/state_manager.py) | `save_feedback()` / `list_feedback()` / `export_feedback()` |
| [scripts/feedback_to_md.py](ad-direction-agent/scripts/feedback_to_md.py) | JSON → Markdown 转换 |
| [scripts/pull_feedback.sh](scripts/pull_feedback.sh) | 一键拉取脚本 |

---

## 9. API 端点

所有端点前缀：`/api/v1/agent/ad-direction`

### 反馈端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/feedback` | 提交反馈日志 |
| GET | `/feedback/list?asin=xxx` | 列出某 ASIN 的所有反馈 |
| GET | `/feedback/export?asin=xxx&format=json` | 导出合并 JSON |

### 工作流端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/strategy/options` | 获取战略层选项（含 ASIN 存在性校验） |
| POST | `/strategy/confirm` | 确认战略层选择并推进流程 |
| POST | `/tactics/options` | 获取策略层选项 + LLM 推荐 |
| POST | `/tactics/confirm` | 确认策略层选择并推进流程 |
| POST | `/diagnosis` | 获取诊断数据（只读北极星看板） |
| POST | `/execution/options` | 获取执行方向 + 评分 + LLM 推荐 |
| POST | `/execution/confirm` | 确认执行方向 |
| POST | `/validate_and_report` | 校验 + 决策 + LLM 报告（全自动） |
| POST | `/wizard/state` | 获取向导当前进度状态 |
| POST | `/long-term-config` | 读写长期配置 |

### 辅助端点

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/chat` | Demo AI 对话（流式） |
| POST | `/llm/report` | 独立 LLM 报告入口 |

### [计划] v2 API 端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/api/v2/agent/invoke` | LangGraph 全流程启动 |
| GET | `/api/v2/agent/state/{thread_id}` | 获取当前中断状态 |
| POST | `/api/v2/agent/resume/{thread_id}` | 从中断点恢复执行 |

---

## 10. 持久化

**[app/persistence/state_manager.py](app/persistence/state_manager.py)** — 基于 JSON 文件：

```
config/{ASIN}/
├── long_term_config.json    # 战略+策略选择（跨会话保持）
└── workflow_state.json      # 当前进度 + 执行层选择
```

- `product_level` / `product_stage` / `season_stage` → `long_term_config.json`
- `ad_purposes` / `keyword_types` → `long_term_config.json`
- 工作流进度（当前在第几层、已完成哪些层）→ `workflow_state.json`
- 执行层方向选择 → `workflow_state.json.execution`

**[架构计划]** LangGraph 的 SqliteSaver 将替换 workflow_state.json 的运行时状态管理，
但 `long_term_config.json` 保留（长期配置需要跨会话持久化）。

---

## 11. 已知问题 / 待办

> **已完成里程碑(2026-05~06，历史细节已精简)**：Phase 1-2 数据层重构 + 字段重命名 + days 参数化；Phase 3 P3 统一推荐 + 产品阶段 5 值枚举 + Purpose-Agent 合并；Phase 4 ACOS 规则重构 + 诊断缓存加固；Phase 5 并发安全(PooledDB+文件锁+缓存锁)；Phase 6 反馈系统；Phase 8 部署；Phase 9 v2.6(MCP 字段修复 + 数据闸门)；Phase 10 Campaign 数据管道。v3.3 LLM 网关收口(§7.5)；v3.4 ERP 合并 + 枚举改名(§7.6)。

### P1 — 待优化

1. **`_fetch_search_terms()` 未实现** — `META_AD_SEARCH_TERM` 在 `META_TO_METHOD` 映射中没有对应方法。

2. **`inventory_days` 为 None** — 未用 `can_sale_num` / `avg_daily_sales` 计算库存天数。

3. **`competitor_price_p50` 未填充** — 模型定义了但 `db_adapter` 从未赋值，P1 中用到时走的 `avg_price * 0.95` 兜底。

4. ~~**QueryRouter 选择性过滤**~~ → 已修复。`get_diagnosis` 先缓存/全查 → 场景检测 → `meta_ids` → `_ensure_data(meta_filter=meta_ids)` → 按需重查。非必要场景跳过 placement/flow_keywords/search_terms 查询。

### P2 — 规则引擎调优

- 各规则阈值硬编码，后续可抽取到 `thresholds.toml`
- `balance_maintain` 基准分 50 偏高

### 链路审查 (2026-05-12) — 未修复（涉及 db_adapter/SQL，仍开放）

| 问题 | 说明 |
|------|------|
| `META_AD_SEARCH_TERM` 静默无数据 | SQL 模板已注册，`META_TO_METHOD` 映射缺失，`_fetch_search_terms()` 未实现。`acos_crisis` / `optimize_acos` 场景下搜索词数据永远为空 |
| `competitor_price_p50` 永不赋值 | 模型字段存在，`purpose_adapter` 读取，但 `db_adapter` 从不计算 |
| `inventory_days` 永不计算 | `SpecialSignals` 字段存在，告警 `sig.inventory_days < 14` 永不触发 |
| `META_TREND` 无条件查询 | `_fetch_all()` 中 trend 查询未加 `_should_fetch` 守卫，选择性查询时仍执行 |

### days 时间窗口参数化（2026-05-13，部分未修复）

DB 适配器已支持 `days=7/14/30`，但**主服务链路(前端→API→orchestrator)仍硬编码 7 天**(campaign 链路已传 days)。遗漏点:API `req` 不解构 days、orchestrator 方法签名无 days、缓存键未含 days(不同 days 互相覆盖)。注意 `_fetch_listing` 基础字段来自 Doris 7 天硬编码列,不跟随 days。**仍开放,待排期。**

> **历史 changelog 已精简**：P3 统一推荐、Purpose-Agent 合并、v3.1/v3.2(ACOS 规则重构 + Tab3 独立 + 诊断缓存 4h)、并发安全(PooledDB+锁) 等 5 月明细从略。其中 P2 仍开放：**限流(slowapi)、熔断(LLM 连续失败降级算法)** 待做。部署细节以根目录 `服务器部署说明.md` 为准。

> LangGraph 主图 + SqliteSaver（原 Phase 7）长期**暂缓**。

---

## 12. 启动与测试

### 12.1 启动服务

```bash
cd ad-direction-agent

# 默认数据源=db（连接 Doris 数仓）
DATA_SOURCE=db uvicorn app.main:app --reload --port 8008

# 使用 Mock 数据（不需数据库连接）
DATA_SOURCE=mock uvicorn app.main:app --reload --port 8009
```

**注意**：Windows 端口残留问题 — 如果端口被占用，换一个未使用端口。

### 12.2 访问前端

启动后用浏览器打开：
```
http://localhost:8010/demo/ad-asisitant-agent.html    # 主看板
http://localhost:8010/demo/index.html                 # 首页
```

或 `python start_server.py`（默认端口 8010），终端打印页面地址。

**必须通过 `http://localhost` 访问**，不能直接双击 HTML 文件（`file://` 协议下浏览器会因 CORS 策略拦截 `fetch()` 请求）。

### 12.3 测试 ASIN

已验证可用的测试 ASIN：

| ASIN | 产品类型 | 特点 |
|------|---------|------|
| B0B7S3PWWB | Fishnet Stockings | 数据最全，关键词排名好（34有排名/200广告词），ACOS ~32% |
| B0DNZNWGX5 | Crochet Cover Up | 亏损ASIN，毛利率约-5%，排名有下滑 |
| B0DTPBX66G | Crochet Cover Up (变体) | 排名最差（3/146 有排名），大量高花费无排名词 |
| B0F28PLT4M | Lace Top | 排名最好（61/200 有排名），毛利率 20.8% |
| B0GCZHVBWN | Bikini Set | 中小体量，高排名率（22/28），竞品数据缺失 |

### 12.4 验证数据

```python
python -c "
from app.data.db_adapter import DbAdapter
import asyncio
d = asyncio.run(DbAdapter().fetch_asin_data('B0B7S3PWWB'))
print(f'ASIN: {d.asin}, Price: {d.price}, Keywords: {len(d.keywords)}, Trend: {len(d.trend)}pts')
if d.ad_data:
    print(f'ACOS: {d.ad_data.acos}, CPC: {d.ad_data.cpc}, CVR: {d.ad_data.cvr}')
    print(f'精准ACOS: {d.ad_data.precision_acos}, 非精准ACOS: {d.ad_data.broad_acos}')
print(f'毛利率: {d.margin}, 可售库存: {d.signals.inventory_qty}')
"
```
---

## 13. 关键文件速查

| 文件 | 行数 | 说明 |
|------|------|------|
| [app/data/db_adapter.py](app/data/db_adapter.py) | ~1050 | 数仓查询核心，11 个查询方法（含 3 个 campaign）+ 数据组装 |
| [app/data/campaign_fetcher.py](app/data/campaign_fetcher.py) | ~507 | ★Campaign 编排器（Doris → 预筛选 → MCP → 回落 → 懒加载） |
| [app/core/scenario_analyzer.py](app/core/scenario_analyzer.py) | ~243 | 8 场景检测 + 北极星指标看板 |
| [app/core/workflow_orchestrator.py](app/core/workflow_orchestrator.py) | ~1130 | 四层工作流编排 + P3 统一推荐 + Purpose 集成 |
| [app/core/recommender.py](app/core/recommender.py) | ~670 | 4 方向评分 + TargetAcosRecommender(7步) + BudgetBidRecommender(6步) |
| [app/llm/reasoner.py](app/llm/reasoner.py) | ~1160 | LLM Prompt 构造 + missing_notice 注入 + 4 套 KB builder 函数 |
| [app/llm/kb_loader.py](app/llm/kb_loader.py) | ~155 | KB 加载器：6 个 PRESET（含 campaign_adjustment） |
| [app/models/asin_data.py](app/models/asin_data.py) | ~145 | 数据模型定义（ASINData + 子模型） |
| [app/models/campaign.py](app/models/campaign.py) | ~51 | ★CampaignPerf / CampaignUnit / CampaignData |
| [app/models/layers.py](app/models/layers.py) | ~376 | 四层工作流 Request/Response（含 llm_status/data_completeness） |
| [app/workflow/data_contract.py](app/workflow/data_contract.py) | ~244 | ★数据完整性契约 + evaluate_completeness |
| [app/workflow/data_gate.py](app/workflow/data_gate.py) | ~61 | ★LLM 数据闸门（blocked/degraded/ok） |
| [demo/ad-asisitant-agent.html](demo/ad-asisitant-agent.html) | ~1400 | 主看板前端 — 左右分栏 + 4 标签 + 数据未抓全提醒 |

---

## 14. Doris 数仓字段映射速查

### 核心字段映射（最新，2026-05-11）

| ASINData 字段 | 数仓表 | 数仓列 | 计算/取值方式 |
|--------------|--------|--------|-------------|
| `price` | `listing_general` | `buy_box_price` → `product_price` | 优先 buy_box_price |
| `margin` | `dwd_az_asin_gross_profit` | `gross_profit_amount_proportion` | 按 `order_sale_amount` 加权平均 |
| `rating` | `whp_az_extend` | `star_level` | 7天销量加权平均 |
| `avg_daily_sales_30d` | `listing_general` | `seven_sale_num` | SUM(seven_sale_num) / 7 |
| `days_since_launch` | `listing_general` | `product_site_launch_date` | NOW - date |
| `product_stage` | `whp_az_extend` | `progress_preparation` | 取第一个有值的子 ASIN |
| `season_stage` | `whp_az_extend` | `seasonality` | 取第一个有值的子 ASIN |
| `product_level` | `whp_az_extend` | `product_grade` | 取第一个有值的子 ASIN |
| `signals.inventory_qty` | `dwd_az_asin_gross_profit` | `can_sale_num` | 最新日期所有子 ASIN SUM |
| `ad_data.acos` | `ad_product_report` | `cost / sale` | spend / sales × 100 |
| `ad_data.cpc` | `ad_product_report` | `cost / clicks` | spend / clicks |
| `ad_data.cvr` | `ad_product_report` | `units_order / clicks` | orders / clicks × 100 |
| `ad_data.ctr` | `ad_product_report` | `clicks / impressions` | clicks / impressions × 100 |
| `ad_data.sales` | `ad_product_report` | `sale` | SUM(sale) |
| `ad_data.tacos` | `dwd_az_asin_gross_profit` | `ad_cost_amount / order_sale_amount` | 广告花费/总销售额 |
| `ad_data.precision_acos` | `ad_placement_report` | placement=`TOS` 的 cost/sale | |
| `ad_data.broad_acos` | `ad_placement_report` | placement=`ROS` 的 cost/sale | |
| `keywords[].natural_rank` | `asin_keyword_library` | `craw_nature_rank` | 7天内每个 keyword 取最优子 ASIN |
| `keywords[].near_natural_rank` | `asin_keyword_library` | `near_craw_nature_rank` | 上次爬虫排名（判断"已掉榜"） |
| `keywords[].rank_change_14d` | 计算 | `near - cur` | 正数=排名上升 |
| `keywords[].sp_rank` | `asin_keyword_library` | `craw_sp_rank` | |
| `competitors[].price` | `az_cw_asin_prod_info` | `price` | |

### 已废弃字段

| 废弃字段 | 原表 | 原因 |
|---------|------|------|
| `single_gross_profit` | `dwd_whp_az_extend` | 全库 NULL，67,243 行全部为空 |
| `sale_gross_profit` | `dwd_whp_az_extend` | 同上，全部为空 |
| `in_stock_num` | `listing_general` | 子 ASIN 级全为 0，真实库存不在该表 |
| `month_sale_num` → `avg_daily_sales_30d` | `listing_general` | 该字段是日度均值，SUM叠加后无物理意义 |

---

---

## 15. Campaign 广告活动分析（新增，v3.0）

### 15.1 设计原则

**Doris 轻量发现 campaign 上下文 → MCP 主力拉效果数据 → MCP 失败时降级 Doris。**

- Doris 只做"通讯录"：查 campaign_name / child_asin / keyword_text / match_type / keyword_bid，不取代 MCP 的指标查询
- MCP 是数据主力：basic_info 必拉（~110 次）+ product_report 必拉（~110 次），placement / search_term 按需懒加载
- 代码层硬过滤（非 LLM 淘汰判断）：排除非活跃 / 无数据 / 多关键词活动

### 15.2 数据粒度

```
campaign_key = child_asin + match_type + keyword_text
每行 = 一个广告活动的完整画像（CampaignUnit）
```

### 15.3 核心文件

| 文件 | 用途 |
|------|------|
| [models/campaign.py](app/models/campaign.py) | CampaignPerf / CampaignUnit / CampaignData（独立于 ASINData） |
| [data/campaign_fetcher.py](app/data/campaign_fetcher.py) | 编排器：Doris上下文 → 预筛选 → MCP必拉 → 组装 → 懒加载接口 |
| [data/campaign_prefilter.py](app/data/campaign_prefilter.py) | 硬过滤纯函数（inactive/no_data/multi_keyword） |
| [data/db_adapter.py](app/data/db_adapter.py) | 3 个 campaign SQL 方法（上下文/指标/placement 回落） |
| [data/mcp_mapping.py](app/data/mcp_mapping.py) | CAMPAIGN_TOOLS + build_campaign_tool_args |
| [data/mcp_adapter.py](app/data/mcp_adapter.py) | campaign_call_tool 便捷方法 |

### 15.4 数据流

```
parent_asin
  ├─ Doris 上下文查询 → campaign_name / child_asin / keyword_text / match_type / keyword_bid
  ├─ 代码硬过滤 → 排除 inactive / no_data / multi_keyword
  ├─ MCP basic_info 并行 (~110次, semaphore=8) → days_online / budget / status
  ├─ MCP product_report 并行 (~110次, semaphore=8, 7d) → CTR/CPC/CVR/ACOS/指标
  ├─ MCP 失败 → Doris 回落
  ├─ 组装 CampaignUnit[] → LLM 分析
  └─ LLM 标记 → 懒加载 placement/search_term
```

### 15.5 已验证数据

| 项目 | 值 |
|------|-----|
| 测试 ASIN | B0B7S3PWWB |
| Doris 上下文查询 | 491 条 → 预筛选后 127 条 CampaignUnit |
| MCP basic_info | 实测返回预算/状态/上线天数（中文列名） |
| MCP product_report | 实测返回 CTR/CPC/CVR/ACOS/花费/销售额/订单/曝光 |
| MCP placement_report | 实测返回 4 广告位拆分（TOS/DetailPage/Other/OffAmazon） |
| MCP search_term_report | 实测返回逐搜索词明细 |
| placement Doris 回落 | 已验证：dwd_amazon_ad_placement_report_update 可按 campaign_id 查询 |
| KB preset | campaign_adjustment: ["18","19","21","22"]（~26K字符） |

### 15.6 CampaignData vs ASINData

```
ASINData（已有）                      CampaignData（新增）
─────────────────────────           ─────────────────────────
粒度: 1行 = 1个父ASIN               粒度: N行 = N个campaign
视角: 宏观——"这个品整体怎么样？"      视角: 微观——"每个活动单独怎么样？"
用途: strategy/tactics/diagnosis    用途: campaign adjustment
两者独立共存，非继承或嵌套
```

---

## 16. Campaign LLM 分析引擎（v3.2，2026-06-01）

### 16.1 设计原则

基于方案 [docs/广告活动调整方案.md](docs/广告活动调整方案.md) 实现。规则和阈值全部在 KB（18/19/21/22 号文件）中，代码不做 if-else 判断。

**架构**：精准/广泛分流 → 各自分批 → R1+R2 并行投票 → sanity_check（仅低置信）→ AI 汇总合成。

### 16.2 核心文件

| 文件 | 用途 |
|------|------|
| [models/campaign.py](ad-direction-agent/app/models/campaign.py) | 全部 Campaign 模型 + `CampaignConfirmRequest` |
| [llm/reasoner.py](ad-direction-agent/app/llm/reasoner.py) | `recommend_campaign_batch()` + `recommend_campaign_synthesis()` + `_campaign_to_prompt_dict()` |
| [workflow/steps/campaign.py](ad-direction-agent/app/workflow/steps/campaign.py) | 编排引擎 ~1000 行 |
| [api/campaign.py](ad-direction-agent/app/api/campaign.py) | `POST /campaign/analyze` + `POST /campaign/confirm`（stub） |
| [demo/campaign_test.html](ad-direction-agent/demo/campaign_test.html) | 调试前端：左侧状态读取 + 右侧 AI 汇总/筛选/批量审核 |
| [scripts/campaign_kb_experiment.py](ad-direction-agent/scripts/campaign_kb_experiment.py) | N×temp 实验运行器 + JSONL 落盘 |

### 16.3 数据流

```
parent_asin
  ├─ Doris 上下文查询 → 仅维度字段
  │   (campaign_budget/campaign_status 不参与 GROUP BY)
  ├─ 代码硬过滤 → 排除 non-ENABLED / 无数据 / 多关键词活动
  ├─ MCP basic_info + product_report 并行 → 回落 Doris
  ├─ 组装 CampaignUnit[] (campaign_key = "活动名 × 子ASIN")
  ├─ LLM 预过滤 → 排除疑似已淘汰 (budget≈$1, bid≈$0.2)
  ├─ 分流: EXACT → 精准流 / BROAD+PHRASE+AUTO → 广泛流
  │   ├─ 精准流: 预取 placement → _EXACT_PROMPT → 分批投票
  │   └─ 广泛流: 预取 search_term → _BROAD_PROMPT → 分批投票
  ├─ 合并两流结果
  ├─ Budget 冲突裁决 (淘汰值强约束)
  ├─ Sanity check (仅低置信项，≤10/批，并行)
  └─ AI 汇总合成 (分组叙事 + 特殊调整清单)
```

### 16.4 置信度划分

| 等级 | 条件 |
|------|------|
| **high** | R1+R2 的 action + direction 完全一致，取保守幅度 |
| **medium** | 小流退化为单轮不投票；或 R3 tiebreaker 打破僵局 |
| **low** | 只有一轮有结果（另一轮 missing）；或两轮分歧且 R3 未解决 |

### 16.5 修复的问题

| 问题 | 根因 | 修复 |
|------|------|------|
| Doris 上下文重复行 | `campaign_budget` 参与 GROUP BY | 移除，常量 'ENABLED' 替代 |
| MCP campaign 工具 budget=0 | 双层 content→text→JSON 嵌套 | `unwrap_tool_payload` 递归拆解 |
| campaign_key 不唯一 | `child_asin\|match_type\|keyword` | 改为 `活动名 × 子ASIN` |
| 投票误判 | 只比 action | `_same_direction()` 比较 direction dict |
| Sanity check 截断 | max_tokens=2048 不够 | →4096, timeout→60s, ≤10条+≤80字 |
| _prefetch 预取崩溃 | `result` 未初始化，超时 UnboundLocalError | `result: dict = {}` 显式初始化 |
| API 错误返回缺字段 | 手工拼 dict | → `CampaignAnalysisResult().model_dump()` |
| 丢失率偏高 | 每批 10 个 LLM 覆盖不全 | 改回每批 6 个 |

### 16.6 待完成

| 任务 | 内容 |
|------|------|
| 前端 Tab 5 | `ad-asisitant-agent.html` 增设 Campaign 分析标签 |
| `POST /campaign/confirm` 落地 | MySQL 表 + ERP 推送 |
| 跨活动后处理 | 淘汰联动 + 总预算冲突 |
| is_core 真实数据源 | 替换硬编码 False |
| KB 遵循度评分器 | 消费实验 JSONL |
| R3 tiebreaker 端到端验证 | 当前从未触发（数据特性），需构造分歧用例 |

### 16.7 验证状态

| 测试 | 结果 |
|------|------|
| B0B7S3PWWB, 30 活动 | 103s, 精准/广泛分流正常, sanity PASS |
| 全链路 | 102 活动 campaign_key 唯一, budget 恢复, synthesis 产出分组叙事 |
| 预过滤 | 126→102 (24 已淘汰跳过) |
| 前端 | 汇总叙事卡 + 三维筛选 + 批量审核 + localStorage 隔离 |

---

## 17. 关键词类型命名规范统一（v4.1，2026-05-30）

### 17.1 三个概念定义

| 概念 | 字段名（英文） | 中文 | 枚举值 | 粒度 |
|------|--------------|------|--------|------|
| 1. 关键词策略 | `target_keyword_strategy: list[str]` | 已选关键词类别 | generic / long_tail / competitor / brand / custom | ASIN 级 |
| 2. 匹配类型 | `match_type: str` | 匹配类型 | EXACT / BROAD / PHRASE | 活动级 |
| 3. 关键词类别 | `keyword_class: str` | 识别的关键词类别 | 同概念 1 | 逐词 |

### 17.2 全链路改名映射

| 概念 | 旧名（分散在多处） | 新名（统一） |
|------|-------------------|-------------|
| 1 | `keyword_types` (long_term), `keyword_type` (ASINData), `selected_keyword_types` (campaign), `KeywordType` (枚举) | `target_keyword_strategy` / `TargetKeywordStrategy` |
| 3 | `strategy_type` (purpose-agent), `kw_class` (campaign), `keyword_type` (KB ENUM_MAP) | `keyword_class` |
| 2 | `match_type` | 不变 |

### 17.3 改动范围

| 层 | 改动 |
|----|------|
| Python 模型 | `layers.py` `KeywordType`→`TargetKeywordStrategy`, `asin_data.py` `keyword_type`→`target_keyword_strategy`, `campaign.py` `selected_keyword_types`/`kw_class`→`target_keyword_strategy`/`keyword_class` |
| 后端步骤 | `tactics.py`/`execution.py`/`diagnosis.py`/`p3.py`/`validation_report.py`/`wizard.py` 全部 dict key 统一 |
| LLM prompt | `reasoner.py`/`agent_router.py` prompt 模板中的字段名统一 |
| 知识库加载 | `kb_loader.py` ENUM_MAP 注释更新 |
| 数据库 | `tactics_config.keyword_types` → `target_keyword_strategy`（已执行 ALTER TABLE） |
| TOML 配置 | `layer_options.toml` `[tactics.keyword_types]` → `[tactics.target_keyword_strategy]` |
| 前端 | `ad-asisitant-agent.html` / `campaign_test.html` 全部 JS 变量和 API 字段名统一 |
| 测试 | `test_cross_tag.py` 字段名更新 |
| 旧缓存 | `config/_migrated/` 全部删除 |

### 17.4 兼容措施

- `purpose_adapter.py` `keyword_class` 读取（新 LLM 输出）——旧缓存已全部清除
- `campaign.py` 读 `keyword_class`（新 key）——无旧缓存需回退
- 反馈模块 `_feedbackModules.keyword_type` 保持不变（独立概念，非关键词类型）

---

*最后更新：2026-06-01*
*更新内容：v4.2 — Campaign 分析引擎完善（精准/广泛分流 + synthesis 汇总 + sanity 分批校验 + 调试前端增强 + 关键词命名全链路统一 + DB 迁移）*
*更新内容：v4.1 — 关键词类型三概念命名统一（target_keyword_strategy / match_type / keyword_class）+ 全链路重构（17文件3前端DB迁移TOML）*
