# 广告方向决策子智能体 — 交接文档

> **最后更新**: 2026-06-18
> **版本**: v3.0 / 文档 v5.0
>
> **2026-06-18 上线（已部署 prod chenv3.1 + 本地同步）**：① **目标 ACOS 推荐下限 5→25**（`TargetAcosRecommender` Step7 Hard cap）——真源 `thresholds.toml:125 [target_acos] min_acos`，`recommender.py:753 self.cfg.get("min_acos", 25)` 默认同步（toml 是生效值，代码默认仅兜底）。治"数据差产品被钳到 5% 过激目标 ACOS"。**改后需重启**（`systemctl restart ad-direction-agent`，toml 启动时加载）。② **前端防呆（`demo/ad-asisitant-agent.html`，静态文件免重启）**：策略层 4 保存项 / 5 标签统一**常驻三态**——黄「● 未保存」/ 绿「✓ 已保存」/ 红错误；脏检测（广告目的·关键词·方向 onchange、ACOS·预算 oninput，值变即翻黄，保存转绿，回访已存配置显绿不误报）；ACOS·预算输入框**默认空 + 灰显前次配置值**（placeholder，空显/输入隐/删恢复，无前次则空；取消覆盖回 AI 亦清空，不再预填 AI 值）；**消费前拦截**——`运行执行层分析`（`_mountCampaignRealtime`）前若有未保存字段则 `alert` 列出并拦下。核心 JS：`setSaveTag`/`_saved`/`_dirty`/`_prevCfg`/`recompute{Tactics,Directions,Num}Dirty`/`guardUnsavedBeforeRun`/`resetSaveTags`。**运营须知**：广告方向加载后默认黄「未保存」，运行执行层分析前须先点一次「广告方向保存」（不点则方向未持久化，且被拦截——有意的强制确认）。服务由 **systemd `ad-direction-agent.service`** 管理（6 worker，:8010）；服务器文件为 **CRLF**。
>
> **2026-06-18 更新（文档与代码对账，无代码改动）**：逐节对照真实代码修订过时/矛盾/冗余描述 —— ① **§2 结构树**：repo 根名 v2.5→v3.2、删除不存在的 `steps/load_data.py`、补全 api（campaign/decision/campaign_viewmodel/announcements）·core（validation_ops/metrics_ops_language）·llm（kb_loader/key_pool/purpose_adapter）·persistence（mysql_state_manager/redis/state_factory/erp_writer）·campaign steps；② **§13 关键文件行数**全部按 `wc -l` 实测刷新（`workflow_orchestrator.py` 已重构为薄编排器 ~1130→**283**，文档内所有 `workflow_orchestrator.py:大行号` 引用失效；reasoner 1160→2031、db_adapter 1050→1292、ad-asisitant-agent.html 1400→3306）；③ **§7.4 KB preset** 6→**7 个**（补 `new_campaign`，`campaign_adjustment` 18/19/21/22→**18/17/15/19/22/21**）；④ **§9 端点**补全 Campaign/决策批次/`execution/recommend`/override 族；⑤ **§15/§16 Campaign 段去冗余**——原两段是 2026-06-01 旧快照（把 `/campaign/confirm` 标 stub、campaign.py 标 ~1000 行、Tab5 列待办，均与现状矛盾），收敛为指向 [docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md) 的指针；⑥ 修正末尾"最后更新 2026-06-01"与头部矛盾的旧 footer。专属文档同步修订（§3.1 行数/§3.3 端点 6 个且 confirm 非 stub/§3.2 配置值 `campaign_llm_concurrency` 8→50·`deepseek_model` pro→flash·`mcp_max_concurrency` 115）。
>
> **2026-06-18 更新**（待办逐条对当前服务器代码核实，状态校准，无代码改动；详见 [docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md) §17）：**3 条状态已变** —— ⑦ **库存口径已统一**成 `can_sale_num`（`db_adapter.py:409`，Doris/MCP 两路径不再口径不一致，原「Doris 回落用 in_stock_num」待办已闭环）；④ **P4 定时调度换方式做了**（无 APScheduler，但 cron 已上线：`crontab_schedule.sh` 每天 0 点 → `batch_via_api.py`，定时目标达成）；③ **KB23 父目标闸控已接线**（`campaign_budget_reallocation.py:108` 读 `parent_allowed_net_increase`），但值仍 `=0.0` 占位无源（settings.py:160）、GROUP-009 超目标压缩护栏仍缺（半成品）。**4 条确认仍未实现**：① `is_core` 硬编码 `False`（campaign.py:1106，行号自 956 漂移）；② Tab4 完整方向对象从未落 state（富卡读回不亮）；⑤ 新增活动预算未接入组合回算；⑥ 新增活动 5 类触发场景仅展示标签非门禁（campaign_new.py:6）。⑧ 核心关键词 nature_rank 拉取代码在（db_adapter:194/:314），缺的是数据源（真库 99% 空，属 ETL 侧）。
>
> **2026-06-17 更新**（自服务器 chenv3.1 实测验证版**单向合并**到 v3.0，14 文件覆盖；保留 v3.0 独有的 `mcp_normalizers` 中文 key 兼容 + `workflow/steps/campaign.py` 广告方向英→中翻译两项，未回退）：① **Part 6 续 — 组合(portfolio)预算调整真实执行**（新增 `app/workflow/steps/portfolio_execution.py` + 端点 `POST /campaign/execute-portfolio-budget`；执行时实时调 `agent_query_portfolio_list` 取 portfolioId → `async_batch_update`(portfolioId+portfolioBudget) → 写 `t_advert_agent_modify_portfolio_record`；`repository` 加 `get_decision_basic`/`insert_portfolio_records`；前端 `state.execConstraints` 从占位接通真实下发 + 新增「执行已同意」按钮 `execApproved`→`/campaign/execute`；两步安全模型：同意=标记、专用按钮才经 MCP 真执行；dry-run 安全档）；② **4 个落库 bug 修复** —— operator 审计字段透传(`create_by`/`editor_by`/`creator_id`/`editor_id`)、`product_name` 端到端注入(取 Doris `product_cn_name`，截 200)、`mysql_state_manager.get_analysis_session` 时区比较 bug(naive `started_at` 补 `tzinfo=utc`→修「新建分析事件」进不了 C 态)、`auto_push.wizard_payload_from_state` 的 `dict→list` 退化修复(`target_scores`/`keyword_analysis` 过滤非 dict)；③ `advert_mcp_client.query_portfolio_list` 透传 `currentUserId`；④ **前端脏缓存渲染修复**(`refreshP3`/`refreshTacticsRecommendations` 在 await 查数前先清空旧列/隐藏旧警告 banner，消除「刚点重新推荐即显示上一轮『核心数据不足』」)；⑤ UI：组合操作区「回算修改」→「组合预算调整」重命名 + `.pp-act` 紧凑样式(11px/nowrap)。**6 个 advert MCP 工具(`whp-advert-agent`)均已真实联调验证**(create_portfolio_campaign / async_batch_update / batch_update_result / create_negative_keywords / query_keyword_suggest_bid 实测成功；query_portfolio_list 可调用，新建组合存在数仓同步延迟)，工具入参/出参结构存档见 [advert_mcp_tools_reference.md](advert_mcp_tools_reference.md)。
>
> **2026-06-15 更新**：① 前置**双轨读回** Tab3/Tab4 快照富化（前端复用 `renderP3`/`renderDirCard`，从 `ai_suggest`/`direction_recommend_detail` 读回 ACOS·预算推理 + 方向卡评分理由全文）；② **tab5 执行层口径修复**（`_snapshot_action` action 粗类化=纯查 suggest_category、`_normalize_action` placement 判据按 proposed_pct≠current_pct、顶部预算约束 = 各活跃组加总 + 低价捡漏 $1 backend 化）；③ **低价捡漏组判定重做**（阈值 bid≤0.21 / budget≤1.01：预过滤 AND、分类 `_is_in_elimination_pool` OR、LLM prompt 硬规则 + `_resolve_budget_conflicts` 强制淘汰兜底，复用现有 $1/$0.20 修正治 LLM 不听话）；④ **Part 6 — 广告调整 MCP 真实执行接入**（confirm→「执行已确认调整」→调 `whp-advert-agent` 6 工具→落 4 张 `_record` 表 + 回写 pending execute_status→「调整记录」表；新增 `advert_mcp_client`/`advert_exec_mapper`/`advert_execution` + repo 6 方法 + 3 端点；**默认安全** `advert_mcp_enabled=false`+`advert_exec_dry_run=true`；真库 dry-run 全链路通，真实写 Amazon 仅被 IP 白名单/数仓 campaign_id 外部契约拦）。Campaign 详见 [docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md) §15；Part 6 完整记录见 [docs/2026-06-15-交接文档.md](docs/2026-06-15-交接文档.md)。
>
> **2026-06-14 更新**：方向A收敛（实时轨落库→读快照 + 删 to_viewmodel + 移除 sanity 落库门槛）、决策快照富化 P1/P2（Tab1 评分卡 from purpose_score + Tab3 ACOS 推理 from ai_suggest）、前端左栏重构（广告方向下拉操作台 + 基础信息卡 + 分析天数搬战略层 + 策略层合并 + 砍生成评估报告）、current_*/shop_account 两处数据修复、**核心关键词监控源头改造方案（待实现，出口补丁已撤、normalize 中文别名保留）**。当日完整记录见 [docs/2026-06-14-交接文档.md](docs/2026-06-14-交接文档.md)。
>
> **2026-06-13 更新**：执行层 ERP 全链路落库接线（overview/synthesis/新增卡/预过滤卡/card 新列，表结构 0 加列）、`_ensure_data` 按 filter 分 key 缓存（修「同 ASIN 当天重复拉数不命中」）、Campaign pipeline 并行提速、决策批次面板 7 bug + 缺口1、batch_no/confidence_level/synthesis 截断修复。Campaign 模块详见 [docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md) §14；当日完整记录（含待办清单）见 [docs/2026-06-13-交接文档.md](docs/2026-06-13-交接文档.md)。

---

## 1. 项目概述

**项目名称**：广告方向决策子智能体（Ad Direction Agent）

**项目定位**：面向亚马逊广告运营的 AI 辅助决策系统，基于标签驱动架构，通过四层递进工作流（战略→策略→诊断→执行），为运营人员提供数据驱动的广告方向推荐。

**数据源**：Doris 数仓（36.133.119.99:9030，MySQL 协议）+ MCP StarRocks 网关（`DATA_SOURCE=mcp` 优先，失败降级 Doris），222 张表中核心使用 9 张表。

**技术栈**：Python 3.13 / FastAPI / PyMySQL / DeepSeek API / LangGraph（可选，`USE_LANGGRAPH=false` 默认） / Pydantic

---

## 2. 项目结构

```
AD_assistant_agent-v3.2/                  # 仓库根
├── README.md                             # 5 分钟上手（安装、启动、Demo）
├── docs/                                 # 文档索引、协作指南、LangGraph/ERP 方案
├── .env.example                          # 环境变量模板（无密钥）
├── ad-direction-agent/                   # 方向决策服务（主服务）
├── ad-purpose-agent/                     # 广告目的/词类 AI（PYTHONPATH 引用，勿移目录）
└── start_server.py                       # 统一启动入口

ad-direction-agent/
├── app/
│   ├── agents/                           # LangGraph 编排（USE_LANGGRAPH=true 时）
│   │   ├── bridge.py                     # 端点 → 单节点子图
│   │   ├── graph.py                      # 单节点图编译
│   │   ├── state.py                      # AgentState
│   │   └── nodes/                        # 薄包装，仅调 workflow.steps
│   ├── workflow/
│   │   ├── context.py                    # WorkflowContext
│   │   ├── helpers.py / data_summary.py  # 数据摘要 + missing_notice 注入
│   │   ├── data_status.py / meta_filters.py  # 数据状态 + META 过滤
│   │   ├── data_contract.py             # ★数据完整性契约（required/important/optional + evaluate_completeness）
│   │   ├── data_gate.py                  # ★LLM 数据闸门（blocked/degraded/ok）
│   │   └── steps/                        # 步骤实现（均接入 data_gate）
│   │       ├── strategy.py / tactics.py / diagnosis.py
│   │       ├── execution.py / p3.py / validation_report.py / wizard.py
│   │       ├── campaign.py               # ★Campaign 编排引擎（2098 行）
│   │       ├── campaign_portfolio.py / campaign_budget_summary.py / campaign_budget_reallocation.py
│   │       ├── campaign_new.py / campaign_restart.py
│   │       └── advert_execution.py / portfolio_execution.py  # ★Part 6 真实执行（MCP）
│   ├── main.py                           # FastAPI 应用入口，注册路由 + CORS + 静态文件
│   ├── config/
│   │   ├── settings.py                   # 全局配置（数据源、DB、LLM、TOML 加载）
│   │   ├── tags.toml                     # 标签定义（运行时唯一来源）
│   │   ├── thresholds.toml               # 规则阈值（运行时唯一来源）
│   │   └── layer_options.toml            # 四层工作流的各层选项定义
│   ├── api/
│   │   ├── strategy.py                   # Layer 1.1 — 战略层 API
│   │   ├── tactics.py                    # Layer 1.2 — 策略层 API
│   │   ├── diagnosis.py                  # Layer 1.3 — 诊断层 API
│   │   ├── execution.py                  # Layer 1.4 — 执行层 API
│   │   ├── wizard.py                     # 向导状态 API
│   │   ├── long_term_config.py           # 长期配置读写 API
│   │   ├── feedback.py                   # 反馈收集 + 导出 API
│   │   ├── chat.py                       # Demo AI 对话
│   │   ├── llm.py                        # LLM 分析 API
│   │   ├── campaign.py                   # ★Campaign 分析/执行 API（6 端点，556 行）
│   │   ├── campaign_viewmodel.py         # ★Campaign ViewModel 适配层（双轨渲染）
│   │   ├── decision.py                   # ★决策批次 API（context/new-event/cancel-event/preset）
│   │   ├── announcements.py              # 公告 API
│   │   └── deps.py                       # 依赖注入
│   ├── core/
│   │   ├── workflow_orchestrator.py      # 薄编排器：数据缓存 + 委托 workflow.steps（283 行）
│   │   ├── data_aggregator.py            # 数据聚合调度器（选择适配器）
│   │   ├── query_router.py               # 查询路由器（场景→元脚本映射，含META_TREND扩展计划）
│   │   ├── scenario_analyzer.py          # 场景检测器（8种场景+北极星指标）
│   │   ├── recommender.py                # 方向推荐评分算法（4方向×4评分逻辑）
│   │   ├── validation_engine.py          # 规则校验引擎（调度18条规则）
│   │   ├── validation_ops.py / metrics_ops_language.py  # 运营语言/指标话术
│   │   └── decision_package.py           # 决策包生成器
│   ├── data/
│   │   ├── base.py                       # DataSourceAdapter 抽象基类
│   │   ├── db_adapter.py                 # Doris 数仓适配器（核心，1292行，含 campaign 方法）
│   │   ├── advert_mcp_client.py          # ★广告调整 MCP 客户端（whp-advert-agent，Part 6）
│   │   ├── decision_config_reader.py     # ★决策配置读取（定时调度配置表）
│   │   ├── mcp_adapter.py                # MCP 数据适配器（MCP-first + 回落）+ campaign_call_tool
│   │   ├── mcp_mapping.py                # MCP 工具映射（META_* + campaign 级工具）
│   │   ├── mcp_normalizers.py            # MCP 响应归一化（中英文字段别名 _pick/_row_key_index）
│   │   ├── mcp_client.py                 # MCP Streamable HTTP JSON-RPC
│   │   ├── mcp_db_context.py             # MCP 上下文解析（ASIN→SKU/店铺）
│   │   ├── mcp_fetch_run.py              # MCP 工具并行执行器
│   │   ├── mcp_keyword_report.py         # 关键词报告多 match_type 合并
│   │   ├── mcp_empty_reports.py          # MCP 空报告检测→触发回落
│   │   ├── mcp_tool_fallback.py          # MCP 工具→META 反向映射
│   │   ├── doris_fallback.py             # MCP 失败/空表→Doris 回落编排
│   │   ├── fallback_merge.py             # MCP+Doris 数据合并（含 natural_order_ratio）
│   │   ├── phased_fetcher.py             # 分阶段数据拉取入口
│   │   ├── campaign_fetcher.py           # ★Campaign 编排器（Doris上下文→预筛选→MCP→回落）
│   │   ├── campaign_prefilter.py         # ★Campaign 硬过滤（inactive/no_data/multi_keyword）
│   │   ├── db_sql_helpers.py             # SQL 辅助（IN子句、ListingContext）
│   │   ├── mock.py                       # Mock 适配器
│   │   ├── csv_adapter.py                # CSV/XLSX 适配器（备用）
│   │   ├── template_registry.py          # SQL 元脚本注册中心（7个元脚本）
│   │   └── field_mapping.py              # 字段映射表 + 显式转换函数
│   ├── models/
│   │   ├── asin_data.py                  # ASINData / AdData / KeywordData / CompetitorData / SpecialSignals / TrendPoint
│   │   ├── campaign.py                   # ★CampaignPerf / CampaignUnit / CampaignData（独立于 ASINData）
│   │   ├── layers.py                     # 四层工作流的 Request/Response 模型（含 llm_status/data_completeness）
│   │   ├── decision.py                   # 推荐评分相关模型
│   │   ├── validation.py                 # 校验结果模型
│   │   └── feedback.py                   # 反馈收集模型 (FeedbackModule / FeedbackSubmission)
│   ├── rules/
│   │   ├── __init__.py                   # 规则注册表（装饰器模式）
│   │   ├── push_natural.py               # 推进自然位规则（PN-1~PN-5）
│   │   ├── expand_keywords.py            # 新增扩词规则（KE-1~KE-4）
│   │   ├── optimize_acos.py              # 优化 ACOS 规则（OA-1~OA-4）
│   │   ├── balance_maintain.py           # 平衡维持规则（BM-1~BM-5）
│   │   └── cross_tag.py                  # 标签联动约束规则
│   ├── llm/
│   │   ├── client.py                     # DeepSeek API 客户端（单例 + 服务级并发闸）
│   │   ├── reasoner.py                   # LLM 推理器（Prompt 组装 + 结果解析，含全部 campaign prompt）
│   │   ├── kb_loader.py                  # KB 加载器（7 个 PRESET 按需切片）
│   │   ├── key_pool.py                   # API Key 轮询池（RLock）
│   │   └── purpose_adapter.py            # purpose-agent 跨项目桥接
│   └── persistence/
│       ├── state_manager.py              # JSON 文件持久化（长期配置 + 工作流状态）
│       ├── mysql_state_manager.py        # ★MySQL 状态后端（state_factory 按配置切换）
│       ├── state_factory.py              # 状态后端工厂（JSON / MySQL）
│       ├── redis_client.py / redis_cache.py  # Redis 单例 + 分布式锁 + 缓存
│       ├── schema.sql / migrate_json_to_mysql.py  # 建表 + JSON→MySQL 迁移
│       └── erp_writer/                   # ★ERP 落库（auto_push / repository / mappers / advert_exec_mapper 等）
├── demo/
│   ├── ad-asisitant-agent.html           # 主看板前端（左右分栏，5 标签，含 Campaign tab5）
│   ├── campaign_test.html                # Campaign 调试前端
│   ├── campaign-panel/                   # ★Campaign 前端模块（ES module，tab5 lazy-mount）
│   └── index.html                        # 简单首页
├── data/
│   ├── data_lineage/                     # 18 个数据血缘文档（字段级映射）
│   └── mock/                             # Mock 数据样本
├── config/                               # 各 ASIN 的持久化目录
│   ├── {ASIN}/long_term_config.json      # 战略+策略长期配置
│   ├── {ASIN}/workflow_state.json        # 工作流进度状态 + 关键词分析缓存
│   ├── {ASIN}/p3_recommendation.json     # P3 LLM 推荐缓存（次日5:00过期）
│   ├── {ASIN}/target_acos_override.json  # 目标 ACOS 手动覆盖（次日5:00过期）
│   ├── {ASIN}/adjustment_history.json    # 7天调整历史
│   └── {ASIN}/feedback/                  # 反馈日志（每个反馈一个独立 JSON 文件）
│       └── {timestamp}_{ASIN}.json
├── scripts/
│   └── feedback_to_md.py                 # 反馈 JSON → Markdown 转换脚本
├── tests/                                # pytest 测试
├── docs/
│   ├── 架构设计方案.md                   # v2.0 架构设计文档
│   └── 单元测试方案.md                   # 测试方案文档
├── pyproject.toml
├── requirements.txt
└── start_server.py                       # 启动脚本
```

---

## 3. 四层工作流

### 3.1 流程概览

```
ASIN → 战略层(人工) → 策略层(AI推荐+人工) → 诊断层(只读) → 执行层(AI推荐+人工) → 校验+报告
```

### 3.2 各层详解

| 层级 | 角色 | 输入 | 输出 | API |
|------|------|------|------|-----|
| **1.1 战略层** | 人工选择 | ASIN | product_level / product_stage / season_stage | `POST /strategy/options` → `POST /strategy/confirm` |
| **1.2 策略层** | AI推荐+人工 | 战略层选择 + 诊断数据 | ad_purpose / keyword_type | `POST /tactics/options` → `POST /tactics/confirm` |
| **1.3 诊断层** | 只读（不可编辑） | 战略+策略上下文 | 北极星指标看板 + 告警信号 + 关键词监控表 | `POST /diagnosis`（刷新时缓存快照检测场景 → 一次按需查询，不再双重查库） |
| **1.4 执行层** | AI推荐+人工 | 全上下文 + P3 LLM 统一推荐 | 4方向评分 + LLM推荐方向 + 目标ACOS/预算Bid（手动覆盖 > LLM > 算法降级） | `POST /execution/options` → `POST /execution/confirm` |
| **1.5 校验+报告** | 自动 | 执行选择 | 规则校验 + LLM分析报告 | `POST /validate_and_report` |

### 3.3 关键代码入口

- **编排器**：[app/core/workflow_orchestrator.py](app/core/workflow_orchestrator.py) — `WorkflowOrchestrator` 类
- **API 路由**：[app/api/](app/api/) 下按层分文件
- **前端**：[demo/ad-asisitant-agent.html](demo/ad-asisitant-agent.html) — 左右分栏看板式，4 药丸标签切换

---

## 4. Doris 数仓对接

### 4.1 核心表

| 表名 | 数据域 | 行数 | 最新数据 |
|------|--------|------|----------|
| `dwd_whp_amazon_listing_general` | ASIN 主数据 | 186K | 实时 |
| `dwd_whp_az_extend` | ASIN 扩展属性 | 198K | 实时 |
| `dwd_az_asin_gross_profit` | **利润表（毛利率/库存/销售额）** | ? | 每日 |
| `dwd_amazon_ad_product_report_update` | 广告商品级报告 | 20M | ~当前 |
| `dwd_amazon_ad_placement_report_update` | 广告位报告 | 46M | 滞后~50天 |
| `dwd_amazon_ad_keyword_report` | 关键词级广告报告 | 36M | ~当前 |
| `dwd_amazon_listing_flow_keyword_us/uk/de` | 流量关键词 | US: 2.5M | 实时 |
| `dwd_amazon_asin_keyword_library` | ASIN 关键词排名 | 7.5M | ~7天内 |
| `dwd_amazon_precise_keyword_library` | 关键词搜索量库 | 1M | 实时 |
| `dwd_az_cw_asin_prod_info` | 竞品 ASIN 信息 | 14M | 实时 |

### 4.2 DbAdapter 核心方法

**[app/data/db_adapter.py](app/data/db_adapter.py)** — `DbAdapter` 类（1292 行）：

| 方法 | 查询内容 | 对应元脚本 |
|------|----------|-----------|
| `_fetch_listing()` | ASIN 主数据 — 子ASIN聚合（LEFT JOIN, SUM/加权平均） | 固定执行（非元脚本） |
| `_fetch_ad_summary()` | 广告汇总指标（ACOS/CPC/CVR/花费，参数化7/14/30天） | `META_AD_PRODUCT` |
| `_fetch_placement_summary()` | 精准(TOS) vs 非精准(ROS) 广告位拆分（参数化窗口） | `META_AD_PLACEMENT` |
| `_fetch_ad_keywords()` | 关键词级广告表现（前200条+rank_change） | `META_KW_AD` |
| `_fetch_natural_rankings()` | 关键词自然排名（每个keyword取最佳子ASIN排名，7天窗口硬编码） | `META_KW_COMPETITOR_RANK` + `META_KW_SUB_ASIN_RANK` |
| `_fetch_competitors()` | 竞品列表（价格/评分/BSR） | `META_COMPETITOR` |
| `_fetch_flow_keywords()` | 流量关键词（可用于拓词） | `META_FLOW_KEYWORD` |
| `_fetch_gross_profit()` | 销售额/广告/毛利/可售库存（TACOS+毛利率+can_sale_num） | 固定执行 |
| `_fetch_trend_data()` | 按日聚合趋势数据（跟随主 days 参数） | `META_TREND` |
| **★`_fetch_campaign_context()`** | **Campaign 上下文发现（仅维度：campaign_name/child_asin/keyword_text/match_type/keyword_bid）** | **Campaign 独立调用** |
| **★`_fetch_campaign_perf_from_db()`** | **单活动指标回落（按 campaign_name 聚合 7d）** | **MCP 回落** |
| **★`_fetch_campaign_placement_from_db()`** | **单活动 placement 回落（按 campaign_id 查询，四桶聚合）** | **MCP 回落** |

### 4.3 重要数据源迁移记录（最新：2026-05-11）

| 指标 | 原来（错误） | 现在（正确） | 数据源 |
|------|------------|------------|--------|
| **毛利率** | `gross_profit_amount / order_sale_amount`（自创公式，产出 115% 等异常值） | `gross_profit_amount_proportion` 按 `order_sale_amount` 加权平均（Doris 表预计算字段） | `dwd_az_asin_gross_profit` |
| **库存量** | `fba_stock` SUM（日级快照重复累加）→ MAX（取峰值，不准确） | `can_sale_num` 最新日期所有子 ASIN SUM（父 ASIN 口径可售库存） | `dwd_az_asin_gross_profit` |
| **日均销量** | `SUM(month_sale_num)`（各子ASIN的日均值叠加，值放大~15倍） | `seven_sale_num / 7`（7天总销量/7，各子ASIN加总后做除法） | `listing_general`（`_fetch_listing` 聚合计算） |
| **自然排名去重** | `ORDER BY craw_time DESC`（取最新爬虫，不保证最优排名） | `ORDER BY keyword ASC, craw_nature_rank ASC`（每个 keyword 取最优子 ASIN） | `dwd_amazon_asin_keyword_library` |
| **排名显示** | 只展示有排名词（`craw_nature_rank IS NOT NULL`），无排名词被过滤 | 展示 10 有排名 + 3 高花费无排名词，区分"已掉榜"/"未入榜" | `dwd_amazon_asin_keyword_library` |
| **ACOS 告急阈值** | 30% | 40% | `scenario_analyzer.py` |

**关键结论**：
- `dwd_whp_az_extend.single_gross_profit` 全为 NULL，该字段已废弃
- `listing_general.in_stock_num` 全为 0，真实库存数据在 `dwd_az_asin_gross_profit`
- 毛利率使用 Doris 表预计算的 `gross_profit_amount_proportion`，按销售额加权平均
- 库存使用最新日期的所有子 ASIN `can_sale_num`（可售库存）求和
- 自然排名取每个 keyword 最优子 ASIN 的 `craw_nature_rank`，配合 `near_craw_nature_rank` 判断"已掉榜"

### 4.4 DbAdapter 连接与查询

```python
# 连接（自动重连）
conn = await asyncio.to_thread(pymysql.connect, host=..., cursorclass=DictCursor)
# 使用 DictCursor，返回字段名→值的字典

# 核心查询——一次拉全量（不增量加载）
data = await adapter.fetch_asin_data(asin)
```

### 4.5 查询参数化

所有广告查询方法（`_fetch_ad_summary` / `_fetch_placement_summary` / `_fetch_ad_keywords` / `_fetch_gross_profit`）均支持 `days` 参数（7/14/30，默认 7 天）。**90 天降级机制已于 Phase 1 移除**。placement 表更新滞后 30-50 天，7 天窗口可能无数据（前端需处理精准/非精准 ACOS 为空的情况）。

趋势数据跟随主 `days` 参数，自然排名查询硬编码 `INTERVAL 7 DAY`（不参与参数化）。

### 4.6 查询路由

**[app/core/query_router.py](app/core/query_router.py)** — `QueryRouter` 实现了场景驱动的选择性查询：

- 基础必查：`META_KW_AD` + `META_COMPETITOR` + `META_AD_PRODUCT`
- 场景扩展（如 `acos_crisis` 追加 `META_AD_PLACEMENT` + `META_AD_SEARCH_TERM`）
- 执行方向扩展（如 `expand_keywords` 追加 `META_FLOW_KEYWORD`）
- **[计划扩展]** `META_TREND` 加入基础必查（趋势数据）

### 4.8 MCP 数据管道

**当前默认**：`.env` 配置 `DATA_SOURCE=mcp`，MCP 优先，失败降级 Doris。`MCP_FALLBACK_TO_DB=true` 启用回落。

**MCP 工具映射**（[mcp_mapping.py](app/data/mcp_mapping.py)）：
- 9 个 META 维度 → 12 个 MCP 工具（ASIN 级）
- 4 个 CAMPAIGN_TOOLS（campaign 级）：`ad_campaign_basic_info` / `ad_campaign_product_report` / `ad_campaign_placement_report` / `ad_campaign_search_term_report`

**MCP 中英文字段兼容**（v2.6）：`mcp_normalizers.py` 新增 `_pick(row, *aliases)` 和 `_row_key_index(row)`，支持 MCP 返回中文列名（`花费`/`点击量`/`搜索词`/`曝光量` 等），同时兼容英文 key。

**数据完整性闸门**（v2.6）：[data_contract.py](app/workflow/data_contract.py) 定义四模块契约（required/important/optional），[data_gate.py](app/workflow/data_gate.py) 在 LLM 调用前评估数据完整性 → blocked（拦截）/ degraded（降级含 missing_notice）/ ok。

### 4.9 特殊注意事项

- **Doris 大小写**：列名大小写不敏感，但 SQL 关键字 `INTERVAL 3 DAY` 不能写成 `NOW()-3`
- **连接保持**：DbAdapter 缓存单个连接（`self._conn`），闲置超时后自动重连一次
- **`_fetch_listing` 聚合逻辑**：
  - 数值字段（库存/销量/花费/评价数）：所有子 ASIN **SUM**
  - 退款率：**月销量加权平均**
  - 评分：**7天销量加权平均**
  - 价格：优先取 `buy_box_price`
  - 上架日期：取最早（`min()`）
- **执行顺序**（`_fetch_all`）：listing → 并行(ad / placement / keywords / rankings / competitors / flow / profit) → 组装 → 返回

---

## 5. 场景分析系统

### 5.1 8 种场景（严格优先级）

优先级从高到低，ACOS > 40% 覆盖除清货期外的所有场景：

| 优先级 | 场景 ID | 触发条件 |
|--------|---------|----------|
| 1 | `acos_crisis` | ACOS > 40% 且产品阶段 ≠ 清货期 |
| 2 | `clearance` | 产品阶段=清货期 或 库存=0 |
| 3 | `cold_start` | 测试期 + 引流型/排名型 |
| 4 | `rapid_growth` | 推进期 + 引流型 |
| 5 | `push_ranking` | 推进期 + 排名型 |
| 6 | `profit_harvest` | 收割利润期/维持期 + 盈利型 |
| 7 | `stable_conversion` | 收割利润期/维持期 + 转化型 |
| 8 | `default` | 以上均不匹配 |

### 5.2 北极星指标看板

各场景的北极星指标定义在 **[app/core/scenario_analyzer.py](app/core/scenario_analyzer.py)** 的 `METRIC_DEFS` 字典中。

每个指标有阈值（`warning` / `danger`），分别映射 UI 状态颜色：
- **good**（绿色）：指标在安全范围内
- **warning**（黄色）：指标接近警戒线
- **danger**（红色）：指标严重超标
- **neutral**（灰色）：无数据

**重要**：各场景的指标定义是产品需求严格规定的，修改需确认 8 场景对应的字段和阈值是合理且经过业务方确认的。

### 5.3 数据字段取值

**[app/core/scenario_analyzer.py](app/core/scenario_analyzer.py)** `FIELD_GETTERS` 定义各字段从 ASINData 的提取逻辑。关键映射：

| 看板字段 | 数据来源字段 | 转换 |
|---------|------------|------|
| `acos` | `ad_data.acos` | round(值, 1) |
| `tacos` | `ad_data.tacos` | round(值, 1) |
| `cvr` | `ad_data.cvr` | round(值, 1) |
| `cpc` | `ad_data.cpc` | round(值, 2) |
| `precision_acos` | `ad_data.precision_acos` | round(值, 1) |
| `broad_acos` | `ad_data.broad_acos` | round(值, 1) |
| `margin` | `data.margin` | **data.margin × 100**（小数→百分数） |
| `avg_daily_sales_30d` | `data.avg_daily_sales_30d` | round(值, 1) |
| `natural_order_ratio` | `data.natural_order_ratio` | round(值, 1) |
| `inventory_qty` | `data.signals.inventory_qty` | 直接取 |

**注意**：AdData 字段已在 Phase 1 批量去窗口后缀：`acos_14d` → `acos`，`cvr_14d` → `cvr`，`average_cpc` → `cpc`，`spend_14d` → `spend`，`orders_14d` → `orders` 等。新增 `sales` 字段（广告销售额）。

---

## 6. 规则引擎

### 6.1 注册模式

采用 **装饰器模式** 注册规则，详见 [app/rules/__init__.py](app/rules/__init__.py)：

```python
@validation_rule(rule_id="PN-1", direction="push_natural", priority=1)
async def check_something(data, thresholds) -> ValidationItem | None:
    ...
```

### 6.2 规则清单（18 条）

| 方向 | 规则 ID | 优先级 | 简述 |
|------|---------|--------|------|
| push_natural | PN-1 | 1 | 上升/优质词检测（≥2 个良好） |
| push_natural | PN-2 | 1 | 预算倾斜比例检查（>50% 警告） |
| push_natural | PN-3 | 1 | 广告依赖词检测 |
| push_natural | PN-4 | 2 | 上升词 ACOS 检查（>35% 需先优化） |
| push_natural | PN-5 | 2 | 核心排名位置判断 |
| expand_keywords | KE-1 | 1 | 可用新词数量检测 |
| expand_keywords | KE-2 | 1 | 当前覆盖率检查 |
| expand_keywords | KE-3 | 2 | 现有词健康度评估 |
| expand_keywords | KE-4 | 2 | 新增数量合理性判断 |
| optimize_acos | OA-1 | 0 | ACOS 超标（≥50% force_correct） |
| optimize_acos | OA-2 | 0 | 高花费零转化词 |
| optimize_acos | OA-3 | 1 | Bid 下调空间 |
| optimize_acos | OA-4 | 1 | 否定词机会 |
| balance_maintain | BM-1 | 1 | ACOS/销量波动检测 |
| balance_maintain | BM-2 | 1 | 异常信号检查 |
| balance_maintain | BM-3 | 2 | 参数合理性 |
| balance_maintain | BM-4 | 2 | 排名稳定性 |
| balance_maintain | BM-5 | 2 | 子选项阈值 |
| cross_tag | (3条) | 0 | 标签联动约束 |

---

## 7. LLM 集成

### 7.1 模型配置

- **供应商**：DeepSeek API
- **Base URL**：`https://api.deepseek.com`
- **模型**：通过 `.env` 文件配置（`DEEPSEEK_MODEL`）
- **API Key**：通过 `.env` 文件配置（`DEEPSEEK_API_KEY`），不硬编码
- **ad-direction-agent**：[app/config/settings.py](app/config/settings.py) — pydantic-settings 加载 `.env`
- **ad-purpose-agent**：[config.py](ad-purpose-agent/config.py) — 手动解析 `.env`，共享同一份配置

`.env` 模板见 `ad-direction-agent/.env.example`。`.env` 已在 `.gitignore` 中，不提交到仓库。

### 7.2 LLM Prompt 体系

| Prompt | 文件 | 用途 | Temperature |
|--------|------|------|------------|
| Purpose 知识库（必选） | `ad-purpose-agent/knowledge_base/prompt_kb.md` | 策略层推荐：广告目的 + 关键词分类 + 评分 | 0.1 |
| Purpose 关键词 KB（可选） | `ad-purpose-agent/knowledge_base/prompt_keyword_kb.md` | 关键词类型判定（Broad/Long-tail/Competitor/Brand/Custom） | — |
| `EXECUTION_SYSTEM_PROMPT` | `app/llm/reasoner.py` | 执行层方向推荐 | 0.3 |
| `P3_RECOMMEND_SYSTEM_PROMPT` | `app/llm/reasoner.py` | P3 统一推荐（目标 ACOS + 预算/Bid） | 0.3 |
| `SYSTEM_PROMPT` | `app/llm/reasoner.py` | 综合分析报告（含18条规则说明） | 0.3 |

> **注意**：`TACTICS_SYSTEM_PROMPT`（direction 自己的策略层 prompt）已废弃，策略层 LLM 调用统一走 purpose-agent 的 `agent_router`（OpenAI SDK）。

### 7.3 策略层缓存与降级

- **缓存**：`keyword_analysis` 写入 `workflow_state.json` 后，回访跳过 LLM（`is not None` 判断），仅从 DB 刷新排名字段
- **降级**：purpose-agent 失败 → DB 关键词填充（无 AI 分类）。不再调用 direction 的 `reasoner.recommend_tactics` 作为二次降级
- **重新推荐**：`/tactics/recommend`（前端「AI 重新推荐」按钮）重定向到 purpose-agent
- 诊断层：`keyword_analysis` 缺失时防御性调用 purpose-agent（仅首次）
- 执行层推荐失败 → 按评分排序取 Top 2
- P3 统一推荐失败 → 算法降级（confidence=low，标注"算法降级"）
- 综合分析失败 → 返回降级报告

### 7.4 LLM Prompt 接入知识库（v3.1.0）

**2026-05-28：所有 LLM 业务规则统一迁移至 `docs/knowledge_base/`**

此前业务规则分散在三处：
- `reasoner.py` 4 个 SYSTEM_PROMPT 中内嵌的方向定义、评分公式、阈值
- `ad-purpose-agent/knowledge_base/prompt_kb.md` + `prompt_keyword_kb.md`

现在统一由 `docs/knowledge_base/`（19 个 md，v3.1.0）承载，通过 `kb_loader.py` 按需切片注入。

**新增文件**：[app/llm/kb_loader.py](app/llm/kb_loader.py)

- 启动时一次性读盘 → 编译正则 → ENUM_MAP 英→中翻译 → 写入内存缓存
- 5 个 PRESET 对应 5 个 LLM 调用点，每个加载 2-8 个 KB 切片
- 英文 enum 整词替换为 `layer_options.toml` 中的中文 term

| Preset | 调用点 | 加载 KB 文件 | 字符数 |
|--------|-------|-------------|--------|
| `purpose_tactics` | 策略层 (purpose-agent) | 01 02 03 04 06 09 10 14 | 18.7K |
| `execution_direction` | 执行层 (recommend_execution) | 01 02 03 04 05 09 14 22 | 24.0K |
| `p3_recommend` | P3 (recommend_p3) | 01 03 05 09 10 11 14 19 | 18.7K |
| `analyze_report` | 综合分析 (analyze) | 02 05 09 12 14 | 11.6K |
| `chat` | AI 聊天 (chat_stream) | 02 14 | 5.2K |
| **★`campaign_adjustment`** | **Campaign 活动调整** | **18 17 15 19 22 21** | **~26K** |
| **★`new_campaign`** | **新增活动分析线 (KB16)** | **16 06 02** | — |

**改造的 LLM 调用点**（共 5 处，全部接入 KB）：

| 文件:方法 | 改造内容 |
|----------|---------|
| `reasoner.py:analyze` | `SYSTEM_PROMPT` → `_ANALYZE_TASK_PROMPT` + `_build_analyze_system_prompt()`，移除方向定义表、评分公式、阶段策略 |
| `reasoner.py:recommend_execution` | `EXECUTION_SYSTEM_PROMPT` → `_EXECUTION_TASK_PROMPT` + `_build_execution_system_prompt()` |
| `reasoner.py:recommend_p3` | `P3_RECOMMEND_SYSTEM_PROMPT` → `_P3_TASK_PROMPT` + `_build_p3_system_prompt()`，保留 ACOS ±40% 硬约束 + 字段语义说明 |
| `agent_router.py:determine_ad_targets_from_metrics` | 旧 `prompt_kb.md` + `prompt_keyword_kb.md` → `kb.build("purpose_tactics")` + `_PURPOSE_OUTPUT_RULES` |
| `chat.py:chat_stream` | `CHAT_SYSTEM_PROMPT` 注入 `kb.build("chat")` |

**删除的废弃代码**：
- `reasoner.py`: `TACTICS_SYSTEM_PROMPT` / `recommend_tactics()` / `_fallback_tactics()`（策略层已统一走 purpose-agent）
- `ad-purpose-agent/knowledge_base/`: `prompt_kb.md` + `prompt_keyword_kb.md` → `_archive/`

**保留不从 KB 迁移的三类内容**：
- (a) 输出 JSON schema + 字段语义注释
- (b) KB 未覆盖的输出硬约束（如 ACOS 目标 ±40%，含 TODO 标记）
- (c) 文风/禁用词/叙事结构规则（`OPS_WRITING_RULES`、`REASONING_DECISION_RULES`）

**核心原则**：
1. KB 是业务规则唯一源，阈值/红线/关键词策略以 `docs/knowledge_base/` 为准
2. `layer_options.toml` 是中文 term 唯一源，ENUM_MAP 单向翻译
3. KB 文件只读，开发人员不修改
4. 每轮 LLM 调用按 preset 最小切片加载，不打包全量

### 7.5 LLM 客户端收口与并发治理（v3.3，2026-06-04）

**面向高并发（2000+ 请求 / 峰值 100+）的基础设施收口。** 此前 LLM 传输层是碎片化的：

**改前的 3 个问题**

1. **3 个独立 httpx 连接池**（应为 1 个）。根因：`LLMReasoner.__init__` 写 `client or DeepSeekClient()`（reasoner.py:462），三个模块单例各自无参构造各开各的池——
   - 池1 = `reasoner` 单例（reasoner.py:1758）→ api/campaign、api/deps、api/llm
   - 池2 = `deepseek_client` 单例（client.py:220）→ api/chat 流式
   - 池3 = `workflow_orchestrator.reasoner`（无参构造）→ 所有 workflow 步骤（p3/execution/validation_report 等）
2. **无服务级并发上限**：`_GLOBAL_LLM_SEM` 只管 campaign 批次，其余 reasoner 方法（均走 `self.client.chat()`）裸奔直冲 DeepSeek。
3. **shutdown 连接泄漏**：main.py 只 close 了池2，池1/池3 永不关闭。

**改后（全服务唯一 client + 集中策略）**

| 改动 | 文件 | 内容 |
|------|------|------|
| 单例收口 | `reasoner.py:462` | 默认 client → 共享单例 `deepseek_client`。池1/池3 坍缩为 1，shutdown 泄漏随之消除（main.py 关的就是唯一 client） |
| 连接池 + 超时分离 | `client.py:_ensure_client` | `max_connections=200, max_keepalive=50`；`Timeout(connect=8, read=llm_timeout, write=10, pool=5)` |
| 服务级并发闸 | `client.py:_global_llm_sem` | 模块级 `Semaphore(llm_global_concurrency)`，在 `chat()` 内包裹整个重试循环（一个逻辑请求占一个槽）。**不**加在 `chat_stream()` → AI 聊天不受影响 |
| 重试退避 | `client.py:chat` | `MAX_RETRIES: 2→3`（首次+2 重试）；重试前指数退避 0.5/1.0s + 抖动 50–200ms，打散高并发雷群 |
| key_pool 热路径硬化 | `key_pool.py` | `mark_success` 不再写盘（每次成功同步文件 I/O 会阻塞事件循环）；`mark_failed` debounce（冷却变化 >1s 才写） |
| 共享 Redis / 分布式锁 | `app/persistence/redis_client.py`（新） | `get_redis()`（懒加载单例+熔断）+ `acquire_lock/release_lock`（SET NX EX + compare-and-delete Lua）。campaign 幂等锁从 campaign.py 迁出复用此处 |

**配置**（settings.py）：新增 `llm_global_concurrency: int = 40`（服务级总并发，需按 Key 数 × 单 Key RPM 调）；`campaign_global_llm_concurrency` 标记 DEPRECATED（campaign 不再用全局闸，仅保留 per-stream `campaign_llm_concurrency`）。

**边界**：本期只做进程内信号量（多 worker 的跨进程分布式信号量、key 冷却 → Redis 待后续）；换供应商仍走 `.env`（`DEEPSEEK_BASE_URL`/`DEEPSEEK_MODEL`），不引入新抽象层。

**分层**：`LLMReasoner`(prompt) → `DeepSeekClient`(传输+策略，本次强化对象) → `ApiKeyPool`(只管 Key)。

---

## 8. 反馈收集系统 (v3.4)

### 8.1 设计目的

通过收集运营对 AI 推荐结果的真实评价，积累纠错数据以迭代 prompt 和规则引擎。

### 8.2 前端交互

- **入口**：顶部栏右侧「📝 反馈」按钮，括号内数字表示已自动填充的模块数
- **弹窗表格**：6 列 — 模块 / AI判断 / 人的判断 / 采纳情况* / 纠错理由 / 备注建议
- **自动预填**：各层确认/保存操作触发数据捕获，AI判断和人的判断自动填入
  - 策略层确认 → 广告目的推荐 + 目标关键词推荐
  - P3 ACOS/预算保存 → 目标ACOS推荐 + 每日预算推荐
  - 执行方向确认 → 广告方向推荐
- **校验**：5 个模块的"采纳情况"必须全部选择（防误触），纠错理由始终可选
- **提交后**：显示"感谢反馈，持续优化中，祝工作顺利！"

### 8.3 数据模型

```python
class FeedbackModule(BaseModel):
    module: str                              # "广告目的推荐" | "目标关键词推荐" | ...
    ai_judgment: str | list[str] | None      # AI 推荐值
    human_judgment: str | list[str] | None   # 人工选择值（未手动设定="未手动设定"）
    adoption: str                            # "采纳" | "可以优化" | "完全不采纳"
    correction_reason: str = ""              # 纠错理由
    notes: str = ""                          # 备注

class FeedbackSubmission(BaseModel):
    id: str                                  # {timestamp}_{asin}
    parent_asin: str
    session_start: str                       # 前端首次加载 ISO 时间戳
    submitted_at: str                        # 提交 ISO 时间戳
    has_error: bool = False
    error_info: str | None = None
    modules: list[FeedbackModule]
```

### 8.4 存储格式

```
config/{ASIN}/feedback/{timestamp}_{ASIN}.json
```

- 每个反馈一个独立 JSON 文件，不追加（避免并发写覆盖）
- `timestamp` 使用 `-` 替换 `:` 以兼容 Windows 文件系统
- 完整示例：`feedback/2026-05-14T13-06-59Z_B0B7S3PWWB.json`

### 8.5 导出方式

| 方式 | 命令 | 产物 |
|------|------|------|
| API | `GET /feedback/export?format=json` | 合并 JSON |
| API 筛选 | `GET /feedback/export?asin=B0B7S3PWWB&format=json` | 单 ASIN JSON |
| 一键脚本 | `bash scripts/pull_feedback.sh -o report.md` | Markdown（AI可读） |
| 转换脚本 | `python scripts/feedback_to_md.py export.json -o report.md` | 同上 |

导出不对运营开放，仅 AI 工程团队使用。

### 8.6 关键文件

| 文件 | 说明 |
|------|------|
| [app/api/feedback.py](ad-direction-agent/app/api/feedback.py) | 3 个端点：提交 / 列表 / 导出 |
| [app/models/feedback.py](ad-direction-agent/app/models/feedback.py) | Pydantic 模型 |
| [app/persistence/state_manager.py](ad-direction-agent/app/persistence/state_manager.py) | `save_feedback()` / `list_feedback()` / `export_feedback()` |
| [scripts/feedback_to_md.py](ad-direction-agent/scripts/feedback_to_md.py) | JSON → Markdown 转换 |
| [scripts/pull_feedback.sh](scripts/pull_feedback.sh) | 一键拉取脚本 |

---

## 9. API 端点

所有端点前缀：`/api/v1/agent/ad-direction`

### 反馈端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/feedback` | 提交反馈日志 |
| GET | `/feedback/list?asin=xxx` | 列出某 ASIN 的所有反馈 |
| GET | `/feedback/export?asin=xxx&format=json` | 导出合并 JSON |

### 工作流端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/strategy/options` | 获取战略层选项（含 ASIN 存在性校验） |
| POST | `/strategy/confirm` | 确认战略层选择并推进流程 |
| POST | `/tactics/options` | 获取策略层选项 + LLM 推荐 |
| POST | `/tactics/confirm` | 确认策略层选择并推进流程 |
| POST | `/diagnosis` | 获取诊断数据（只读北极星看板） |
| POST | `/execution/options` | 获取执行方向 + 评分 + LLM 推荐 |
| POST | `/execution/confirm` | 确认执行方向 |
| POST | `/execution/recommend` | P3 统一推荐（目标 ACOS + 预算/Bid，缓存次日 5:00） |
| POST/DELETE | `/execution/target-acos/override` | 目标 ACOS 手动覆盖 / 取消 |
| POST | `/validate_and_report` | 校验 + 决策 + LLM 报告（全自动） |
| POST | `/wizard/state` | 获取向导当前进度状态 |
| POST | `/long-term-config` | 读写长期配置 |

### Campaign / 决策批次端点

> 完整说明见 [docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md) §3.3 / §11。

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/campaign/analyze` | 运行 Campaign LLM 分析 |
| POST | `/campaign/viewmodel` | 实时操作台：分析→落库→回读快照 |
| GET | `/campaign/snapshot` | 读批次执行层快照（只读） |
| POST | `/campaign/confirm` | 双路审核（写 card + pending confirm_status） |
| POST | `/campaign/execute` | 执行已确认调整 → 广告调整 MCP（dry-run 默认） |
| POST | `/campaign/execute-portfolio-budget` | 组合预算调整执行 → MCP |
| GET | `/decision/context` | 批次列表 + 页面三态判定 |
| POST | `/decision/new-event` | 新建分析事件（state 标记进行中） |
| POST | `/decision/cancel-event` | 放弃进行中分析事件 |
| GET | `/decision/{id}/preset` | 读批次冻结的前置 1-4 快照 |

### 辅助端点

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/chat` | Demo AI 对话（流式） |
| POST | `/llm/report` | 独立 LLM 报告入口 |

### [计划] v2 API 端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/api/v2/agent/invoke` | LangGraph 全流程启动 |
| GET | `/api/v2/agent/state/{thread_id}` | 获取当前中断状态 |
| POST | `/api/v2/agent/resume/{thread_id}` | 从中断点恢复执行 |

---

## 10. 持久化

**[app/persistence/state_manager.py](app/persistence/state_manager.py)** — 基于 JSON 文件：

```
config/{ASIN}/
├── long_term_config.json    # 战略+策略选择（跨会话保持）
└── workflow_state.json      # 当前进度 + 执行层选择
```

- `product_level` / `product_stage` / `season_stage` → `long_term_config.json`
- `ad_purposes` / `keyword_types` → `long_term_config.json`
- 工作流进度（当前在第几层、已完成哪些层）→ `workflow_state.json`
- 执行层方向选择 → `workflow_state.json.execution`

**[架构计划]** LangGraph 的 SqliteSaver 将替换 workflow_state.json 的运行时状态管理，
但 `long_term_config.json` 保留（长期配置需要跨会话持久化）。

---

## 11. 已知问题 / 待办

### 已完成（Phase 1 + 2 + 3）

**Phase 1+2（已有）**：
- ~~毛利率公式异常（产出 115%）~~ → 改用表预计算字段 `gross_profit_amount_proportion` 加权平均
- ~~库存 SUM 膨胀~~ → 改用最新日期所有子 ASIN `can_sale_num` 求和（可售库存口径）
- ~~自然排名取最新爬虫非最优排名~~ → 改为 `ORDER BY keyword ASC, rank ASC` 取最优子 ASIN
- ~~无排名词完全不可见~~ → 展示 10 有排名 + 3 高花费无排名词，区分"已掉榜"/"未入榜"
- ~~90 天降级机制~~ → 完全移除，改为 days 参数化（7/14/30，默认 7 天）
- ~~AdData 字段名含窗口后缀~~ → 批量重命名去后缀（`acos_14d` → `acos` 等）
- ~~硬编码 DB/LLM 凭据~~ → `.env` 可配置化
- ~~项目未接入统一数据源~~ → P1 接入 DbAdapter，废弃 `db_connector.py`/`db_executor.py`/`skills/`/`sql_registry.py`
- ~~ACOS 告急阈值 30%~~ → 40%

**Phase 3（2026-05-11~12）**：

| 完成项 | 说明 |
|--------|------|
| P3 LLM 统一推荐 | 新建 `POST /execution/recommend`，LLM 同时给出目标 ACOS + 预算/Bid 建议，缓存至次日 5:00 |
| 产品阶段枚举重构 | 7值→5值（测试/推进/收割/维持/清货），20+ 文件全局替换 |
| 场景检测适配 | `detect_scenario()` 适配 5 阶段，全场景追加 ACOS 指标 |
| DB 查询修复 | `_fetch_ad_summary` NOW→CURDATE 完整天窗口；`_fetch_listing` LEFT JOIN 去重；`campaign_budget` 去重聚合替代 `seven_ad_cost` |
| Purpose-Agent 合并 | `agent_router.py` 新增 `determine_ad_targets_from_metrics()`；`purpose_adapter.py` 跨项目桥接；策略层 LLM 替换为 Purpose 知识库；诊断层新增关键词 AI 分类 + 趋势数据 |
| 7天调整历史 | `adjustment_history.json` 按日期分组存储运营手动值，同日覆盖，超 7 天自动清理 |
| 前端增强 | 诊断层新增 ECharts 趋势图 + P1 格式关键词监控表；关键词监控表 `<details open>` 默认展开 |
| 废弃标记 | `ai_skills.py` / `main.py` / `index.html` 标记 DEPRECATED，后续物理删除 |
| P3 模型修复 | `TargetAcosResult` / `BudgetBidResult` 新增 `manual_override` 字段；旧代码手动覆盖路径 None 访问 Bug 修复（仅设置单侧覆盖时另一侧 `None.xxx` 崩溃） |
| LLM 调用精简 | 策略层加缓存判断（`keyword_analysis is not None` → 回访跳过 LLM）；删除 direction 降级 LLM（`reasoner.recommend_tactics`）；`/tactics/recommend` 重定向到 purpose-agent；诊断层刷新去双重查询（缓存快照做场景检测 → 一次按需查询）；P3 统一推荐端点增加手动覆盖检测 |
| 前端修复 | P3 ACOS/预算编辑框父容器 `hidden` 层级修复；诊断刷新 ECharts dispose 修复；P3 手动覆盖标识 + 清除按钮；调整历史写入 |
| 重复方法清理 | `state_manager.py` 中 `get_adjustment_history` 和 `record_adjustment` 同名方法重复定义，后者覆盖前者导致签名不匹配 → 500 |

### v2.6 合并（2026-05-29）— 已完成

| 完成项 | 说明 |
|--------|------|
| MCP 中英文字段映射修复 | `_pick()` / `_row_key_index()` 新增，4 个 normalizer 全部加中文别名；`compute_natural_order_ratio()` 支持 trend 回退 |
| 数据完整性闸门 | `data_contract.py` + `data_gate.py`：四模块契约 → blocked/degraded/ok；4 个 workflow step 接入闸门 |
| 前端黄条提醒 | 数据未抓全时统一显示"点击刷新重新拉取"提示 |
| 版本号 2.6.0 | settings / pyproject / HTML / 公告 |

### Campaign 数据管道（2026-05-29）— 已完成

| 完成项 | 说明 |
|--------|------|
| Campaign 数据模型 | `CampaignPerf` / `CampaignUnit` / `CampaignData`，独立于 ASINData |
| Doris 上下文查询 | `_fetch_campaign_context()`：仅维度字段（campaign_name/child_asin/keyword_text/match_type/keyword_bid） |
| MCP campaign 工具 | `campaign_call_tool` + `CAMPAIGN_TOOLS` + `build_campaign_tool_args` |
| Campaign 编排器 | `CampaignFetcher`：Doris上下文→预筛选→MCP必拉(basic_info+product_report)→组装→懒加载接口(placement/search_term) |
| 预筛选 | 硬过滤：非活跃/无数据/多关键词活动（budget=$1 保留给 LLM 判断） |
| KB preset | `campaign_adjustment: ["18","19","21","22"]` |
| placement 回落 | 已验证 `dwd_amazon_ad_placement_report_update` 可按 campaign_id 查询 |

### P1 — 待优化

1. **`_fetch_search_terms()` 未实现** — `META_AD_SEARCH_TERM` 在 `META_TO_METHOD` 映射中没有对应方法。

2. **`inventory_days` 为 None** — 未用 `can_sale_num` / `avg_daily_sales` 计算库存天数。

3. **`competitor_price_p50` 未填充** — 模型定义了但 `db_adapter` 从未赋值，P1 中用到时走的 `avg_price * 0.95` 兜底。

4. ~~**QueryRouter 选择性过滤**~~ → 已修复。`get_diagnosis` 先缓存/全查 → 场景检测 → `meta_ids` → `_ensure_data(meta_filter=meta_ids)` → 按需重查。非必要场景跳过 placement/flow_keywords/search_terms 查询。

### P2 — 规则引擎调优

- 各规则阈值硬编码，后续可抽取到 `thresholds.toml`
- `balance_maintain` 基准分 50 偏高

**Phase 8（2026-05-28）— LLM Prompt 接入知识库 v3.1.0**：

| 完成项 | 说明 |
|--------|------|
| KB 加载器 | 新增 `kb_loader.py`：5 个 PRESET 按需切片注入，ENUM_MAP 英→中翻译，模块级单例预热 |
| reasoner.py 改造 | 4 个 SYSTEM_PROMPT 重构为 KB 注入模板（~170 行 → ~50 行），删除废弃 TACTICS_SYSTEM_PROMPT/recommend_tactics/_fallback_tactics |
| agent_router.py 改造 | 旧 `prompt_kb.md` + `prompt_keyword_kb.md` → `kb.build("purpose_tactics")` + `_PURPOSE_OUTPUT_RULES`，增加 targets 格式明确 + purpose_adapter 防御 dict 格式 |
| chat.py 改造 | CHAT_SYSTEM_PROMPT 注入 `kb.build("chat")` |
| 旧 KB 归档 | `prompt_kb.md` / `prompt_keyword_kb.md` → `_archive/` |
| P0 修复 | `}}` → `}` 残留（3 处）→ LLM JSON 示例格式修复；`target_scores` 失败可见化（scoring_error 字段从 tactics.py 透传到前端） |
| 一键启动 | `start_server.py` 新增 `--with-infra`（自动拉起 Docker MySQL+Redis）+ `--all`（完整 docker compose up） |
| 前端 | 版本号 v2.4→v2.5；target_scores 空时显示错误提示而非静默空白；新增 v2.5 公告 |
| 知识库目录索引 | `docs/knowledge_base/知识库目录索引.md` — 19 份文档按 8 分类组织，含依赖关系图 |

### 链路审查 (2026-05-12) — 已修复

| 问题 | 涉及文件 | 修复内容 |
|------|---------|---------|
| BudgetBid `last_adjustment` 类型错误 → 500 | [recommender.py](ad-direction-agent/app/core/recommender.py#L507) + [workflow_orchestrator.py](ad-direction-agent/app/core/workflow_orchestrator.py#L693) | `get_adjustment_history()` 返回 `list[dict]`，`BudgetBidRecommender.recommend()` 原对其调用 `.get("records", [])` 导致 `AttributeError`。改为接受 `list[dict]` 参数 |
| 调整历史键名 `"operated_at"` vs `"timestamp"` | [recommender.py](ad-direction-agent/app/core/recommender.py#L578) | `record_adjustment()` 写入 `"operated_at"`，`_hours_since()` 读取 `"timestamp"`，24h 阻尼逻辑永不生效。统一为 `"operated_at"` |
| `acos_7d` 死代码 | [recommender.py](ad-direction-agent/app/core/recommender.py#L172) + [balance_maintain.py](ad-direction-agent/app/rules/balance_maintain.py#L22) | DB 适配器不填充 `acos_7d`，但 `_score_balance_maintain()` 和 BM-1 规则有 7d vs 14d 对比分支（不可达）。删除无效分支 |
| BM-1 `ad.acos_14d` 字段引用错误 | [balance_maintain.py](ad-direction-agent/app/rules/balance_maintain.py#L31) | 原引用 `ad.acos_14d`，Phase 1 已重命名为 `ad.acos`，ACOS>40% 时触发 `AttributeError`。已修正 |
| 前端 `confirmStrategy` 未检查返回值 | 前端代码 | ASIN 不存在时后端返回 `accepted=false`，前端忽略直接显示"已保存 ✓"。已加检查 |
| P3 手动覆盖阻塞 AI 重新推荐 | [workflow_orchestrator.py](ad-direction-agent/app/core/workflow_orchestrator.py#L718) | `get_unified_recommendation()` 手动覆盖检查在 `refresh` 判断之前，`refresh=true` 仍返回手动值。已改为 `not refresh` 前置条件 |
| P3 手动覆盖提示文本 | [workflow_orchestrator.py](ad-direction-agent/app/core/workflow_orchestrator.py#L739) | `overall_reasoning` 原为"运营手动设定值，跳过 AI 分析"，改为"当前使用运营手动设定值，点击「AI 重新推荐」可覆盖" |

### 链路审查 (2026-05-12) — 未修复（涉及 db_adapter/SQL）

| 问题 | 说明 |
|------|------|
| `META_AD_SEARCH_TERM` 静默无数据 | SQL 模板已注册，`META_TO_METHOD` 映射缺失，`_fetch_search_terms()` 未实现。`acos_crisis` / `optimize_acos` 场景下搜索词数据永远为空 |
| `competitor_price_p50` 永不赋值 | 模型字段存在，`purpose_adapter` 读取，但 `db_adapter` 从不计算 |
| `inventory_days` 永不计算 | `SpecialSignals` 字段存在，告警 `sig.inventory_days < 14` 永不触发 |
| `META_TREND` 无条件查询 | `_fetch_all()` 中 trend 查询未加 `_should_fetch` 守卫，选择性查询时仍执行 |

### 前端 UI 重构 (2026-05-13, v2.6)

**新看板** `ad-asisitant-agent.html`（~1315行）替代旧的 `wizard.html` 作为主推前端。

| 维度 | 旧 wizard.html | 新 ad-asisitant-agent.html |
|------|---------------|------------------------|
| 布局 | 单列纵向，5 板块依次排列 | 左右分栏：400px 侧边栏配置 + 4 药丸标签洞察 |
| 风格 | 系统默认灰白 | Inter 字体 + 淡蓝紫渐变底色 + Notion 式卡片 |
| 交互 | 滚轮翻页 | 标签切换（100ms 交叉溶解），一屏内操作 |
| Tab | 无 | Tab1=广告目的与关键词推荐, Tab2=数据监控, Tab3=目标ACOS和预算推荐, Tab4=广告方向推荐 |
| P3 输入 | 右侧面板含"+"编辑按钮 | 输入统一在左侧，Tab3 只做展示 + 取消覆盖，左右双向同步 |
| 解锁 | `.section-disabled` 灰掉 | 灰掉标签不可点击，占位文案引导 |

**Tab 解锁链**（基于数据依赖而非 UI 状态）：
- 战略确认 → Tab1(策略推荐) 解锁（评分卡片+关键词表，数据来自 `/tactics/options`）
- 策略确认 → Tab2(诊断) + Tab3(ACOS预算) 解锁（诊断依赖 ad_purposes 做 8 场景判定）
- 诊断加载完成 → Tab4(广告方向) 解锁
- 执行确认 → Tab4 下半段（校验报告）出现

**P3 交互重构**：
- 左侧 ACOS/预算输入框初始隐藏 → 策略确认后出现 → 自动填入 AI 推荐值（不自动保存）
- 手动修改保存 → Tab3 显示"手动覆盖"标签 + "取消覆盖"按钮
- Tab3 取消覆盖 → DELETE override → 左侧恢复 AI 推荐值
- Tab3 不再有"+"编辑按钮（原 wizard 遗留的编辑行互斥问题一并消除）

### 产品阶段枚举全链路改名 (2026-05-13, v2.6)

| 旧值 | 新值 | 涉及文件 |
|------|------|---------|
| `"测试"` | `"测试期"` | layers.py, layer_options.toml, scenario_analyzer.py, workflow_orchestrator.py, cross_tag.py, mock.py, field_mapping.py, recommender.py, reasoner.py, state_manager.py |
| `"推进"` | `"推进期"` | 同上 |
| `"收割"` | `"收割利润期"` | 同上 |
| `"维持"` | `"维持期"` | 同上 |
| `"清货"` | `"清货期"` | 同上 |

**兼容方案**：`STAGE_OLD_TO_NEW` 映射覆盖 7 值旧系统 + 5 值旧系统 → 当前枚举。`state_manager.get_long_term_config()` 读时自动归一化，存量 config 无需手动迁移即可正常工作。`field_mapping.VALUE_MAP` 增加旧→新映射兼容。

### 广告目的枚举改名 (2026-05-13)

| 旧值 | 新值 | 涉及文件 |
|------|------|---------|
| `"引流"` | `"引流型"` | layers.py, layer_options.toml, thresholds.toml/yaml, tags.toml, purpose_adapter.py, scenario_analyzer.py, recommender.py, cross_tag.py, reasoner.py, 前端, test_cross_tag.py, asin_data_sample.json |
| `"转化"` | `"转化型"` | 同上 |
| `"卡位"` | `"排名型"` | 同上 + 所有 description/suggestion 文本中"卡位"→"排名" |
| `"盈利"` | `"盈利型"` | 同上 |
| `"清货型"` | 不变 | — |

Config 中所有 `id`/`label`/`allowed_purposes`/`disallowed_purposes` 键值和 Prompt 中枚举引用全部同步更新。`ad-purpose-agent/config.py` 不修改（另一套命名体系）。

### 产品定位枚举 3 档→4 档 + (Px) 后缀 (2026-06-08)

**背景**：与知识库对齐——KB（`docs/knowledge_base/02-标签维度定义.md`）早已是 P0-P3 四档，但代码层停在旧 3 档「头部/腰部/长尾」，LLM 拿到"腰部"在 KB 里找不到对应规则。本次把代码层升级为 4 档并与 KB 对齐。

| 旧值(3档) | 新值(4档，带后缀) | ERP code |
|------|------|------|
| `头部` | `战略级产品 (P0)` | `P0_PRODUCT` |
| —（新增） | `重点产品 (P1)` | `P1_PRODUCT` |
| `腰部` → **归并到 P2** | `常规产品 (P2)` | `P2_PRODUCT` |
| `长尾` | `长尾产品 (P3)` | `P3_PRODUCT` |

**关键设计**：
- **枚举值带 ` (Px)` 后缀**：`ProductLevel` 的 value = toml `id` = toml `label` 三者统一（如 `战略级产品 (P0)`），前端下拉提交即合法、零改动（根除 label≠id 的 422 崩溃）。
- **`LEVEL_OLD_TO_NEW`**（layers.py）：旧 3 档 + 中间态裸中文 → 带后缀标准值；**腰部统一归 P2**（与兜底默认一致）。
- **`normalize_product_level()` + `ProductLevel._missing_`**：全角括号/全角空格/多空格/旧值 → 自动归一到合法成员；任何 `ProductLevel(x)`（含 Pydantic 校验）走 `_missing_` 容错，**防 422**。
- **读取归一化**：`state_manager` + `mysql_state_manager` 的 `get_long_term_config` 读时 `normalize + LEVEL_OLD_TO_NEW`，存量数据透明兼容。
- **LLM 双写**：`product_level_with_code()` 把值渲染为 `战略级产品 (P0)` 注入 prompt（reasoner 5 处 + agent_router 1 处）——KB 规则用 P0/P1，自带 `(Px)` 确保 LLM 命中；`campaign_adjustment` preset 不含 02/03 也靠双写桥接。
- **ERP code → P0~P3_PRODUCT**：`erp_writer/text_utils._PRODUCT_POSITION_MAP`（带后缀 + 裸值 + 旧值兼容）+ `scripts/erp_db/audit_latest_writes.py` 的 `VALID_POSITION`。**数仓侧若对 position 字段有 CHECK/枚举约束，需 DBA 同步放行 P0~P3_PRODUCT**。

**涉及文件**：`layers.py`（枚举 + 3 映射 + helper）、`layer_options.toml`（4 option id/label/desc）、`cross_tag.py`（CROSS-8 比较值）、`mock.py`、`field_mapping.py`（VALUE_MAP）、`text_utils.py`（ERP map）、5 个 workflow steps（兜底默认 `常规产品 (P2)`）、`state_manager.py` + `mysql_state_manager.py`（归一化）、`reasoner.py`（5 处双写）、`agent_router.py`（双写）、scripts（apply_strategy_preset / wizard_kb_experiment）、tests（test_erp_enums / test_erp_auto_push / test_recommender_scoring）。

**唯一按值做业务判断的点**：`cross_tag.py` CROSS-8（收割/维持期 + 战略级/重点产品 → 建议盈利型）。其余皆为枚举校验 / ERP 转码 / LLM 输入 / 纯透传。核心算法层（`scenario_analyzer` / `recommender`）**不依赖具体值**，改枚举对它们零影响。

### LLM 模型分级：强档思考 / 弱档非思考 (2026-06-09)

`DeepSeekClient.chat()` 新增 `model` / `thinking` / `reasoning_effort` 参数，支持**按调用点分级**（body 注入 DeepSeek OpenAI 格式 `"thinking":{"type":...}` + `reasoning_effort`）：

| 档位 | 用在哪 | 配置 |
|------|--------|------|
| **强档**（思考模式） | Campaign 总览 overview + 汇总 synthesis（重要判断节点，各每次 1 调用） | `LLM_MODEL_STRONG` + `thinking=enabled` + `reasoning_effort=high` |
| **弱档**（非思考，默认） | 其余全部（逐活动批量 / P3 / 执行 / 策略 / 综合分析 / 聊天） | `DEEPSEEK_MODEL` |

`.env` 配置（settings 默认兜底）：
```
DEEPSEEK_MODEL=deepseek-v4-flash        # 弱档(默认)，非思考
LLM_MODEL_STRONG=deepseek-v4-flash      # 强档(当前同模型，仅开思考)
LLM_STRONG_REASONING_EFFORT=high
```

- **同 client / key 池 / 连接池 / 并发闸**（不破坏 v3.5 单 client 收口）；差异仅在请求 body 的 `model`/`thinking`/`reasoning_effort`；
- 强档调用点（reasoner overview/synthesis）超时拉到 ≥180s（思考模式慢）；
- 运维调档只动 `.env`，不改代码（如把某节点也升强档、关思考省钱、调 high↔max）。

### days 时间窗口参数化 — 链路分析报告 (2026-05-13, 未修复，待排期)

**背景**：DB 适配器已支持 `days=7/14/30` 参数，但前端→API→编排器链路未传递，始终硬编码 7 天。以下为完整追踪结果。

**当前状态**：

| 层级 | 文件 | 问题 |
|------|------|------|
| 前端 | `ad-asisitant-agent.html` | 无下拉框，所有 API 调用不传 `days` |
| API | [strategy.py](ad-direction-agent/app/api/strategy.py#L16), [tactics.py](ad-direction-agent/app/api/tactics.py#L16), [diagnosis.py](ad-direction-agent/app/api/diagnosis.py#L12), [execution.py](ad-direction-agent/app/api/execution.py#L24), [wizard.py](ad-direction-agent/app/api/wizard.py#L37) | `req: dict` 不解构 `days` |
| 编排器 | [workflow_orchestrator.py](ad-direction-agent/app/core/workflow_orchestrator.py) | 所有方法签名无 `days` 参数；`_ensure_data` / `_do_preload` 不转发 `days` |
| 数据层 | [data_aggregator.py](ad-direction-agent/app/core/data_aggregator.py) + [db_adapter.py](ad-direction-agent/app/data/db_adapter.py) | **已支持** `days` 参数且 SQL 参数化已完成 |

**关键遗漏点**（共 6 处）：

| # | 位置 | 说明 |
|---|------|------|
| 1 | [workflow_orchestrator.py:115](ad-direction-agent/app/core/workflow_orchestrator.py#L115) | `_ensure_data` meta_filter 路径不传 `days`，`get_diagnosis` 刷新时始终查 7 天 |
| 2 | [workflow_orchestrator.py:269](ad-direction-agent/app/core/workflow_orchestrator.py#L269) + [line 356](ad-direction-agent/app/core/workflow_orchestrator.py#L356) | 策略层两处调用 `recommend_tactics_from_purpose()` 不传 `days`，AI 策略推荐永远基于 7 天 |
| 3 | [wizard.py:37](ad-direction-agent/app/api/wizard.py#L37) + [orchestrator:876](ad-direction-agent/app/core/workflow_orchestrator.py#L876) | `/wizard/report` 端点未列入 Change 1 的 API 清单 |
| 4 | [orchestrator:419](ad-direction-agent/app/core/workflow_orchestrator.py#L419) | `get_diagnosis` 内部 5 处 `_ensure_data` 调用，全部不传 `days` |
| 5 | [orchestrator:644](ad-direction-agent/app/core/workflow_orchestrator.py#L644) + [line 672](ad-direction-agent/app/core/workflow_orchestrator.py#L672) | `get_target_acos_recommendation` / `get_budget_bid_recommendation` 不传 `days` |
| 6 | — | 缓存键仅 `asin` 字符串，不同 `days` 值会互相覆盖。需改为 `f"{asin}:{days}"` 复合键，`CacheEntry` 加 `days` 字段 |

**设计说明**：`_fetch_listing` 的基础字段（`seven_sale_num` / `buy_box_price` / `star_level`）来自 Doris 7 天硬编码列，不跟随 `days` 参数变化。`days` 只影响广告数据窗口（ad_summary / placement / keywords / trend / gross_profit）。这不是本次引入的问题，但上线后需关注用户困惑。

**修复方案**：详见计划文件 [ad-direaction-ad-direaction-kind-tulip.md](C:\Users\wlzx\.claude\plans\ad-direaction-ad-direaction-kind-tulip.md) Change 1 部分。

### P3 — 已完成（执行层扩展）

| 功能 | 实现方式 | API |
|------|---------|-----|
| **目标 ACOS + 预算/Bid 统一推荐** | LLM 单次调用（`P3_RECOMMEND_SYSTEM_PROMPT`），同时输出两份推荐，缓存至次日 5:00。LLM 失败时降级为算法推荐器（`TargetAcosRecommender` / `BudgetBidRecommender`） | `POST /execution/recommend` |
| **运营手动设定** | 目标 ACOS 覆盖（`target_acos_override.json`，次日 5:00 过期）；日预算覆盖（`long_term_config.daily_budget_override`，持久）。前端通过 `manual_override` 字段判断是否显示"手动覆盖"标签和清除按钮 | `POST/DELETE /execution/target-acos/override` |
| **7天调整历史** | `adjustment_history.json`，按日期分组，同日多次取最新，超 7 天自动清理。LLM prompt 注入历史记录 | StateManager |
| **P3 模型 Bug 修复 (v2.3)** | `TargetAcosResult` 和 `BudgetBidResult` 新增 `manual_override: bool = False`；`UnifiedRecommendResponse` 已有 `from_cache` 和 `cached_until`。修复前 Pydantic 静默丢弃这些多余字段，前端无法检测手动覆盖状态 | [layers.py](app/models/layers.py#L287) |

### Purpose-Agent 合并（v2.2）

| 功能 | P1 原位置 | 合并后位置 |
|------|---------|-----------|
| AI 推荐广告目的 + 关键词类型 | `agent_router.determine_ad_targets()` | `purpose_adapter.recommend_tactics_from_purpose()` → 策略层 `get_tactics_options()` |
| 知识库（广告投手规则 + 关键词类型） | `ad-purpose-agent/knowledge_base/prompt_kb.md` | 由 purpose-agent 加载 |
| 关键数据看板 + 关键词监控 | P1 `index.html` | 诊断层（ECharts 趋势图 + P1 格式关键词表） |
| AI 预警建议（第4层） | `ai_skills.generate_dynamic_warnings()` | **已废弃**，运营确认不需要 |

### v3.1 更新 (2026-05-14)

**P3 值下沉执行层**：

| 改动 | 文件 | 说明 |
|------|------|------|
| `acos_target` 硬编码 25 移除 | [workflow_orchestrator.py:1062](ad-direction-agent/app/core/workflow_orchestrator.py#L1062) | 错误默认值比未知危害更大。仅在运营手动设定后注入 |
| 手动 ACOS 覆盖注入诊断层 | [workflow_orchestrator.py:559-561](ad-direction-agent/app/core/workflow_orchestrator.py#L559-L561) | `get_diagnosis()` data_summary 注入 `manual_acos` |
| 手动 ACOS 覆盖注入执行层 | [workflow_orchestrator.py:605-608](ad-direction-agent/app/core/workflow_orchestrator.py#L605-L608) | `get_execution_options()` → `recommend_execution()` LLM 看到真实目标 |
| 手动 ACOS 覆盖注入报告层 | [workflow_orchestrator.py:956-958](ad-direction-agent/app/core/workflow_orchestrator.py#L956-L958) | `run_validation_and_report()` LLM 分析看到真实目标 |
| P3 当前生效设定传入 LLM | [reasoner.py:634-667](ad-direction-agent/app/llm/reasoner.py#L634-L667) | `recommend_p3()` 新增 `current_acos_target` / `current_daily_budget` 参数；Prompt 新增"当前生效设定"章节；系统 Prompt 新增第 5 条规则（基于当前设定微调） |

**前端优化**：

| 改动 | 说明 |
|------|------|
| 投放建议可展开 | >30 字出现展开/收回按钮，点击切换全文 |
| 旧前端删除 | `unified.html` (289行) 已删除，仅保留 `ad-asisitant-agent.html` 为主看板 |
| `start_server.py` 引用修复 | 移除已不存在的 `wizard.html` URL |

**仓库清理**：

| 操作 | 内容 |
|------|------|
| 删除 start.bat | 与 `start_server.py` 重复，仅包装 `chcp 65001` |
| 删除 data/ (22 files) | 数据血缘文档，本地保留，远程移除 |
| 删除 docs/ | 架构设计方案，本地保留，远程移除 |
| .gitignore 更新 | 追加 `data/` `docs/` 排除 |

### v3.2 更新 (2026-05-14)

**目标 ACOS 推荐规则重构**：

| 改动 | 文件 | 说明 |
|------|------|------|
| 合法值放宽 5%-100% | frontend + thresholds.toml + recommender.py | 原 5%-40%，ACOS 输入框 + 硬边界同步放宽 |
| 删除利润率硬约束 | recommender.py | Step 3（`目标ACOS ≤ 毛利率-5%`）移除，7步→6步（后增1步回7步） |
| 新增相对变化约束 ≤40% | recommender.py + reasoner.py | 规则引擎 Step 6 + LLM prompt 硬约束。清货期/测试期豁免 |
| 阶段范围放宽 | thresholds.toml | 各阶段 max 大幅上调（推进期 25%→50%，测试期 35%→60% 等） |
| LLM 精度提升至 1% | reasoner.py | 推荐值可精确至 23% 而非仅有 5% 增量 |
| LLM reasoning 质量要求 | reasoner.py | 禁止泄露内部约束术语；必须包含状态判断+多维度理由+节奏建议；80-150字 |
| 修正 ACOS vs TACOS 概念 | reasoner.py | 明确定义 TACOS < 毛利率 才代表盈利，ACOS 只看广告部分 |
| 软约束：渐进调整 | reasoner.py | 大幅偏离当前值会造成数据剧烈波动，应遵循渐进原则 |

**前端修复与优化**：

| 改动 | 说明 |
|------|------|
| Tab3 左右独立化 | ACOS/预算各自独立卡片，紫色(AI)↔白色(人工)切换，仅 AI 重新推荐共享 |
| P3 按钮独立 bug 修复 | save/clear 各管各的，不再相互清零；AI 推荐值保留在内存不污染 |
| 取消覆盖清零 bug 修复 | `clearP3Acos`/`clearP3Budget` 不用服务端回包，直接读取内存中保留的原始 AI 值 |
| Tab1↔Tab2 交换 | Tab1=策略推荐(战略确认后解锁)，Tab2=数据监控(策略确认后解锁，依赖 ad_purposes 做场景判定) |
| 方向卡片去对勾 + 去重复评分条 | 方向卡片纯 CSS 选中态，删除 `<input checkbox>`；报告区删除与方向卡片重复的评分条 |
| expand_keywords 标签修正 | "拓关键词" → "新增扩词" |
| 顶部 v2.0 气泡移除 | 标题栏只保留「广告辅助决策Agent」 |
| 关键词表列名修正 | "14d变化" → "14天变化" |
| 文件名重命名 | `adAsistantAgent.html` → `ad-asisitant-agent.html` |

**诊断缓存加固**：

| 改动 | 文件 | 说明 |
|------|------|------|
| TTL 延长至 4 小时 | workflow_orchestrator.py | `CACHE_TTL = 14400`（原 600） |
| `_is_valid_data` 防御 | workflow_orchestrator.py | 6 个关键字段空值 > 3 → 不入缓存 |
| 刷新链路修正 | workflow_orchestrator.py | 刷新时清除旧缓存 → 按场景按需查询（非全量），不查脏缓存 |
| 回访降级保护 | workflow_orchestrator.py | `data_missing` 或非法数据 → 降级全量查询 |

**并发安全 — 已实施**：

| # | 问题 | 优先级 | 方案 | 状态 |
|---|------|:--:|------|:--:|
| 1 | DbAdapter 单连接非线程安全 | P0 | PooledDB 连接池（mincached=8, maxcached=8, maxconnections=10, ping=1）+ `__init__` 预热 8 连接 | ✅ |
| 2 | StateManager JSON 并发写入覆盖 | P0 | `threading.RLock` + master lock 覆盖全部 ASIN JSON 写 | ✅ |
| 3 | Orchestrator `_data_cache` 竞态 | P0 | `asyncio.Lock` — `get_diagnosis`/`_ensure_data`/`_preload_data` 全覆盖 | ✅ |
| 4 | DeepSeekClient 每次新建 httpx | P1 | 实例变量 `_ensure_client()` 复用 + shutdown 事件 `close()` | ✅ |
| 5 | purpose_adapter sys.path 修改 | P1 | 删除 `sys.path.insert` → `start_server.py` PYTHONPATH | ✅ |
| 6 | DB/LLM 无超时兜底 | P1 | DB `read_timeout=30`；LLM `asyncio.wait_for(..., 60)` | ✅ |
| 7 | 无限流 | P2 | `slowapi` | 待做 |
| 8 | 无熔断 | P2 | LLM 连续失败 3 次 → 降级算法 | 待做 |
| 9 | 全局单例跨 worker | P2 | `--workers 1` 约束 | ✅ |

**PooledDB 关键设计**：每次 `_query()` 整段在同一个 `asyncio.to_thread` 内完成「借连接→执行→归还」，PyMySQL 连接不跨线程。`_fetch_all` 的 `asyncio.gather` 在池化后自动并行，单次拉 ASIN 耗时从 SUM(SQL) 降为 MAX(SQL)。`__init__` 预热 8 连接避免首个请求冷启动 ~15s 延迟。`_do_query()` 错误重试路径已修复连接双重归还 Bug（closed 标志）。

**Docker 部署评估**：
- `.env` 在 `ad-direction-agent/` 下即生效，无需修改路径
- 所有路径使用 `Path(__file__).resolve().parent...` 相对定位，Docker 安全
- `config/` 目录需 volume 挂载（逐 ASIN 持久化）
- `ad-purpose-agent/` 跨目录依赖，PYTHONPATH 解决
- `requirements.txt` 新增 `dbutils>=3.0`
- 云服务器需确认 Doris 数仓（36.133.119.99:9030）网络可达
- `--workers 1` 硬约束（多 worker 需换 portalocker/Redis）

### 架构计划（LangGraph 编排 + 部署）

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 数据层扩展 + 参数化 + 字段重命名 | 已完成 |
| Phase 2 | P1 接入统一数据源 + 关键词分析 + 废弃清理 | 已完成 |
| Phase 3 | P3 LLM 推荐 + 产品阶段重构 + Purpose-Agent 合并 | 已完成 |
| Phase 4 | 目标 ACOS 规则重构 + Tab3 独立化 + 诊断缓存加固 | 已完成 |
| Phase 5 | 并发安全修复 (PooledDB + 文件锁 + 缓存锁) | **已完成** |
| Phase 6 | 反馈收集系统 (5模块弹窗 + JSON存储 + Markdown导出) | **已完成** |
| Phase 7 | LangGraph 主图 + SqliteSaver（暂缓） | 待做 |
| Phase 8 | 部署上线 (Docker + 内网数仓) | 已完成 |
| **Phase 9** | **v2.6 合并（MCP中英文字段修复 + 数据完整性闸门 + 前端黄条提醒）** | **已完成** |
| **Phase 10** | **Campaign 广告活动分析数据管道（Doris上下文→MCP→回落 + 硬过滤）** | **已完成** |


---

## 12. 启动与测试

### 12.1 启动服务

```bash
cd ad-direction-agent

# 默认数据源=db（连接 Doris 数仓）
DATA_SOURCE=db uvicorn app.main:app --reload --port 8008

# 使用 Mock 数据（不需数据库连接）
DATA_SOURCE=mock uvicorn app.main:app --reload --port 8009
```

**注意**：Windows 端口残留问题 — 如果端口被占用，换一个未使用端口。

### 12.2 访问前端

启动后用浏览器打开：
```
http://localhost:8010/demo/ad-asisitant-agent.html    # 主看板
http://localhost:8010/demo/index.html                 # 首页
```

或 `python start_server.py`（默认端口 8010），终端打印页面地址。

**必须通过 `http://localhost` 访问**，不能直接双击 HTML 文件（`file://` 协议下浏览器会因 CORS 策略拦截 `fetch()` 请求）。

### 12.3 测试 ASIN

已验证可用的测试 ASIN：

| ASIN | 产品类型 | 特点 |
|------|---------|------|
| B0B7S3PWWB | Fishnet Stockings | 数据最全，关键词排名好（34有排名/200广告词），ACOS ~32% |
| B0DNZNWGX5 | Crochet Cover Up | 亏损ASIN，毛利率约-5%，排名有下滑 |
| B0DTPBX66G | Crochet Cover Up (变体) | 排名最差（3/146 有排名），大量高花费无排名词 |
| B0F28PLT4M | Lace Top | 排名最好（61/200 有排名），毛利率 20.8% |
| B0GCZHVBWN | Bikini Set | 中小体量，高排名率（22/28），竞品数据缺失 |

### 12.4 验证数据

```python
python -c "
from app.data.db_adapter import DbAdapter
import asyncio
d = asyncio.run(DbAdapter().fetch_asin_data('B0B7S3PWWB'))
print(f'ASIN: {d.asin}, Price: {d.price}, Keywords: {len(d.keywords)}, Trend: {len(d.trend)}pts')
if d.ad_data:
    print(f'ACOS: {d.ad_data.acos}, CPC: {d.ad_data.cpc}, CVR: {d.ad_data.cvr}')
    print(f'精准ACOS: {d.ad_data.precision_acos}, 非精准ACOS: {d.ad_data.broad_acos}')
print(f'毛利率: {d.margin}, 可售库存: {d.signals.inventory_qty}')
"
```
---

## 13. 关键文件速查

> 行数为 2026-06-18 实测（`wc -l`）。

| 文件 | 行数 | 说明 |
|------|------|------|
| [app/data/db_adapter.py](app/data/db_adapter.py) | 1292 | 数仓查询核心，11 个查询方法（含 3 个 campaign）+ 数据组装 |
| [app/data/campaign_fetcher.py](app/data/campaign_fetcher.py) | 738 | ★Campaign 编排器（Doris → 预筛选 → MCP → 回落 → 懒加载） |
| [app/core/scenario_analyzer.py](app/core/scenario_analyzer.py) | 244 | 8 场景检测 + 北极星指标看板 |
| [app/core/workflow_orchestrator.py](app/core/workflow_orchestrator.py) | 283 | ★**已重构为薄编排器**（数据缓存 + 委托 workflow/steps）；原 ~1130 行的业务逻辑已下沉到 `workflow/steps/`。**文档中所有 `workflow_orchestrator.py:大行号` 引用均已失效** |
| [app/core/recommender.py](app/core/recommender.py) | 979 | 4 方向评分 + TargetAcosRecommender(7步) + BudgetBidRecommender(6步) |
| [app/llm/reasoner.py](app/llm/reasoner.py) | 2031 | LLM Prompt 构造 + missing_notice 注入 + KB builder 函数 + 全部 campaign prompt |
| [app/llm/kb_loader.py](app/llm/kb_loader.py) | 183 | KB 加载器：**7 个 PRESET**（含 campaign_adjustment + new_campaign） |
| [app/models/asin_data.py](app/models/asin_data.py) | 179 | 数据模型定义（ASINData + 子模型） |
| [app/models/campaign.py](app/models/campaign.py) | 262 | ★CampaignPerf / CampaignUnit / CampaignData + portfolio/budget_summary 模型 |
| [app/models/layers.py](app/models/layers.py) | 448 | 四层工作流 Request/Response（含 llm_status/data_completeness） |
| [app/workflow/data_contract.py](app/workflow/data_contract.py) | 284 | ★数据完整性契约 + evaluate_completeness |
| [app/workflow/data_gate.py](app/workflow/data_gate.py) | 61 | ★LLM 数据闸门（blocked/degraded/ok） |
| [demo/ad-asisitant-agent.html](demo/ad-asisitant-agent.html) | 3341 | 主看板前端 — 左右分栏 + 5 标签（含 Campaign tab5）+ 数据未抓全提醒 + 防呆保存状态(2026-06-18) |

---

## 14. Doris 数仓字段映射速查

### 核心字段映射（最新，2026-05-11）

| ASINData 字段 | 数仓表 | 数仓列 | 计算/取值方式 |
|--------------|--------|--------|-------------|
| `price` | `listing_general` | `buy_box_price` → `product_price` | 优先 buy_box_price |
| `margin` | `dwd_az_asin_gross_profit` | `gross_profit_amount_proportion` | 按 `order_sale_amount` 加权平均 |
| `rating` | `whp_az_extend` | `star_level` | 7天销量加权平均 |
| `avg_daily_sales_30d` | `listing_general` | `seven_sale_num` | SUM(seven_sale_num) / 7 |
| `days_since_launch` | `listing_general` | `product_site_launch_date` | NOW - date |
| `product_stage` | `whp_az_extend` | `progress_preparation` | 取第一个有值的子 ASIN |
| `season_stage` | `whp_az_extend` | `seasonality` | 取第一个有值的子 ASIN |
| `product_level` | `whp_az_extend` | `product_grade` | 取第一个有值的子 ASIN |
| `signals.inventory_qty` | `dwd_az_asin_gross_profit` | `can_sale_num` | 最新日期所有子 ASIN SUM |
| `ad_data.acos` | `ad_product_report` | `cost / sale` | spend / sales × 100 |
| `ad_data.cpc` | `ad_product_report` | `cost / clicks` | spend / clicks |
| `ad_data.cvr` | `ad_product_report` | `units_order / clicks` | orders / clicks × 100 |
| `ad_data.ctr` | `ad_product_report` | `clicks / impressions` | clicks / impressions × 100 |
| `ad_data.sales` | `ad_product_report` | `sale` | SUM(sale) |
| `ad_data.tacos` | `dwd_az_asin_gross_profit` | `ad_cost_amount / order_sale_amount` | 广告花费/总销售额 |
| `ad_data.precision_acos` | `ad_placement_report` | placement=`TOS` 的 cost/sale | |
| `ad_data.broad_acos` | `ad_placement_report` | placement=`ROS` 的 cost/sale | |
| `keywords[].natural_rank` | `asin_keyword_library` | `craw_nature_rank` | 7天内每个 keyword 取最优子 ASIN |
| `keywords[].near_natural_rank` | `asin_keyword_library` | `near_craw_nature_rank` | 上次爬虫排名（判断"已掉榜"） |
| `keywords[].rank_change_14d` | 计算 | `near - cur` | 正数=排名上升 |
| `keywords[].sp_rank` | `asin_keyword_library` | `craw_sp_rank` | |
| `competitors[].price` | `az_cw_asin_prod_info` | `price` | |

### 已废弃字段

| 废弃字段 | 原表 | 原因 |
|---------|------|------|
| `single_gross_profit` | `dwd_whp_az_extend` | 全库 NULL，67,243 行全部为空 |
| `sale_gross_profit` | `dwd_whp_az_extend` | 同上，全部为空 |
| `in_stock_num` | `listing_general` | 子 ASIN 级全为 0，真实库存不在该表 |
| `month_sale_num` → `avg_daily_sales_30d` | `listing_general` | 该字段是日度均值，SUM叠加后无物理意义 |

---

## 15. Campaign 广告活动分析（概念定位）

> 设计原则、核心文件、数据流、验证状态等**详细内容已迁至专属文档** [docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md)（持续维护，唯一权威）。本节仅保留主文档读者所需的概念定位。
>
> ⚠ 原 §15.2 曾写 `campaign_key = child_asin + match_type + keyword_text` —— 该定义**已废弃**，现为 `活动名 × 子ASIN`（专属文档 §2.2）。

### 15.1 设计原则（一句话）

**Doris 轻量发现 campaign 上下文（通讯录）→ MCP 主力拉效果数据 → MCP 失败时降级 Doris。** 代码层硬过滤（非 LLM）排除非活跃/无数据/多关键词活动。

### 15.2 CampaignData vs ASINData

```
ASINData（已有）                      CampaignData（新增）
─────────────────────────           ─────────────────────────
粒度: 1行 = 1个父ASIN               粒度: N行 = N个campaign
视角: 宏观——"这个品整体怎么样？"      视角: 微观——"每个活动单独怎么样？"
用途: strategy/tactics/diagnosis    用途: campaign adjustment
两者独立共存，非继承或嵌套
```

---

## 16. Campaign LLM 分析引擎 → 见专属交接文档

> **本节原为 2026-06-01 的 Campaign LLM 引擎快照，已被专属文档全面取代并删除**（避免双份维护导致内容漂移：此处曾把 `/campaign/confirm` 标 stub、campaign.py 标 ~1000 行、Tab5 列为待办，均与现状矛盾）。
>
> Campaign 引擎的架构、数据流、核心文件、置信度、待办、验证状态以 **[docs/Campaign分析引擎交接文档.md](docs/Campaign分析引擎交接文档.md)** 为唯一权威来源（持续维护至 2026-06-18）。

---

## 17. 关键词类型命名规范统一（v4.1，2026-05-30）

### 17.1 三个概念定义

| 概念 | 字段名（英文） | 中文 | 枚举值 | 粒度 |
|------|--------------|------|--------|------|
| 1. 关键词策略 | `target_keyword_strategy: list[str]` | 已选关键词类别 | generic / long_tail / competitor / brand / custom | ASIN 级 |
| 2. 匹配类型 | `match_type: str` | 匹配类型 | EXACT / BROAD / PHRASE | 活动级 |
| 3. 关键词类别 | `keyword_class: str` | 识别的关键词类别 | 同概念 1 | 逐词 |

### 17.2 全链路改名映射

| 概念 | 旧名（分散在多处） | 新名（统一） |
|------|-------------------|-------------|
| 1 | `keyword_types` (long_term), `keyword_type` (ASINData), `selected_keyword_types` (campaign), `KeywordType` (枚举) | `target_keyword_strategy` / `TargetKeywordStrategy` |
| 3 | `strategy_type` (purpose-agent), `kw_class` (campaign), `keyword_type` (KB ENUM_MAP) | `keyword_class` |
| 2 | `match_type` | 不变 |

### 17.3 改动范围

| 层 | 改动 |
|----|------|
| Python 模型 | `layers.py` `KeywordType`→`TargetKeywordStrategy`, `asin_data.py` `keyword_type`→`target_keyword_strategy`, `campaign.py` `selected_keyword_types`/`kw_class`→`target_keyword_strategy`/`keyword_class` |
| 后端步骤 | `tactics.py`/`execution.py`/`diagnosis.py`/`p3.py`/`validation_report.py`/`wizard.py` 全部 dict key 统一 |
| LLM prompt | `reasoner.py`/`agent_router.py` prompt 模板中的字段名统一 |
| 知识库加载 | `kb_loader.py` ENUM_MAP 注释更新 |
| 数据库 | `tactics_config.keyword_types` → `target_keyword_strategy`（已执行 ALTER TABLE） |
| TOML 配置 | `layer_options.toml` `[tactics.keyword_types]` → `[tactics.target_keyword_strategy]` |
| 前端 | `ad-asisitant-agent.html` / `campaign_test.html` 全部 JS 变量和 API 字段名统一 |
| 测试 | `test_cross_tag.py` 字段名更新 |
| 旧缓存 | `config/_migrated/` 全部删除 |

### 17.4 兼容措施

- `purpose_adapter.py` `keyword_class` 读取（新 LLM 输出）——旧缓存已全部清除
- `campaign.py` 读 `keyword_class`（新 key）——无旧缓存需回退
- 反馈模块 `_feedbackModules.keyword_type` 保持不变（独立概念，非关键词类型）

---

> **变更历史以文档顶部的"最后更新"区块为准**（持续维护至 2026-06-18 / 文档 v4.8）。下方为早期里程碑留存：
>
> *v4.2（2026-06-01）— Campaign 分析引擎完善（精准/广泛分流 + synthesis 汇总 + sanity 分批校验 + 调试前端增强 + 关键词命名全链路统一 + DB 迁移）*
> *v4.1（2026-05-30）— 关键词类型三概念命名统一（target_keyword_strategy / match_type / keyword_class）+ 全链路重构（17文件3前端DB迁移TOML）*
