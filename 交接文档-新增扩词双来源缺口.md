# 新增扩词双来源缺口 — 现状 / 问题 / 知识库权威参考

> 记录时间：2026-08-04。主题：新增扩词应有的"双来源"（流量入口→广泛探词；搜索词报告→精准承接）在代码中的实现缺口，以及"流量入口词被错误建成精准"的问题。
> 适用代码：`AD_assistant_agent-v3.2/ad-direction-agent`（Campaign 新增活动分析线）。
> 知识库权威参考：`docs/knowledge_base/执行规则/16-新增活动规则.md`、`28-新增词来源相关性与场景化配额规则.md`、`docs/knowledge_base/03-阈值参数.md`、`docs/knowledge_base/23-广告组合与预算分配规则.md`（§3.4）。

---

## 1. 结论摘要

新增扩词在业务上应有**两个来源**，知识库对此有完整权威定义；但当前代码只实现了其中一个，且实现方式与知识库冲突：

| 来源 | 业务语义（知识库权威） | 代码现状 |
|---|---|---|
| **流量来源：流量入口 → 新建广泛词** | 流量词库/词池缺口补流量入口，**低成本拿搜索词样本**（16号§4：BROAD = "低成本拿搜索词样本，重点看预算、Bid、搜索词质量和否词"；28号§3.2：新品冷启动"广泛探词1条 + 精准承接"） | ✅ 数据源已实现（`flow_keywords`），**但错误地把流量词建成精准活动**（见问题1） |
| **搜索词来源：搜索词报告 → 承接精准词** | 已有广泛/自动活动打出来的搜索词，经准入（16号§1 `KEYWORD_PROMOTED_FROM_BROAD`，23号§3.4 三通道为唯一权威）后**承接为精准活动**（28号§1 `SRC_CONVERTED` / `SRC_BROAD_DERIVED` 等来源码） | ❌ **扩词流未实现**。数据管道已存在（`fetch_search_terms_for` / `_prefetch_search_terms`）但**只消费于广泛流否词链路**；新增扩词流（`NewKeywordFetcher` / `campaign_new.py`）不消费搜索词数据，无此来源（见问题2） |

一句话：**"探词 → 看搜索词样本 → 好词精准承接"的闭环只有前半段（还建错了匹配类型）；后半段（搜索词承接精准）在扩词流中完全没有，搜索词数据仅被广泛流否词链路消费。**

---

## 2. 现状：代码实现全景

### 2.1 新增活动分析线数据源（`NewKeywordFetcher`，app/data/new_keyword_fetcher.py）

| Step | MCP 工具 | 用途 | 产出的 source |
|---|---|---|---|
| Step 1 | `flow_keywords` | 流量词库（搜索量≥100 硬过滤） | `flow`（默认）/ `ranking_opportunity`（自然位28-48） |
| Step 1 | `own_keyword_flow` | 自身自然位/周排名 | 同上 |
| Step 4 | own-only 补漏 | 自然位 28-48 且 flow 没有的词 | `ranking_opportunity` |
| Step 6 | `erp_listing_asin_keyword_rank_history` | 逐词历史排名 enrichment（可选开关） | — |
| Step 7 | `whp_amazon_advert_keyword_suggest_bid`（在 campaign_new.py 并行调） | 建议竞价 | — |

**没有搜索词报告源接入扩词流。** 但注意：搜索词报告数据管道**在其他链路已实现并消费**（详见 §2.7），并非全仓库未接通——只是**未接到新增扩词流**（`NewKeywordFetcher` / `campaign_new.py` 不消费它）。

### 2.2 新增流 match_type 推导（app/workflow/steps/campaign_new.py）

LLM 只判 `keyword_class`（generic / long_tail / competitor / brand / custom），match_type 由代码硬映射（`_CLASS_TO_MATCH_TYPE`，campaign_new.py:187-193）：

```python
generic → BROAD；long_tail → EXACT；competitor → EXACT；brand → EXACT；custom → EXACT
```

缺失时回退：有自然位 → EXACT，否则 BROAD（campaign_new.py:196-201）。

### 2.3 排序层的精准倾斜（campaign_new.py）

- 候选排序 `_longtail_sort_key`（:74-83）：长尾优先（词数多者优先，cap 5）→ 流量词库里的长尾词天然排前。
- 输出排序（:589）：EXACT 优先 → 高置信优先 → 截断 Top-20。两条排序都让"流量长尾词建成精准"更容易进最终名额。

### 2.4 prompt 层的精准引导（app/llm/reasoner.py `recommend_new_campaigns`）

| 位置 | 内容 | 影响 |
|---|---|---|
| reasoner.py:398 | "**禁止**输出 … match_type … 由代码确定" | match_type 无 LLM 输出权（正确，但映射本身有问题） |
| reasoner.py:425 | "**优先精准长尾词**（多词、含产品具体属性 → 相关性高、竞争低、ACOS 可控）" | 软引导 LLM 把流量长尾词判 long_tail → 落 EXACT |
| reasoner.py:421 | "搜索量低但属性精确吻合… 仍可 R1（低量精准长尾是运营偏好）" | 同上 |
| reasoner.py:438 | few-shot：长尾词 → "建议：新建精准活动承接" | 同上 |

### 2.5 触发场景标注（campaign_new.py `_label_trigger`，:153-171）

- `RANKING_OPPORTUNITY_NO_EXACT`（自然位28-48）、`SEASONAL_ADVANCE_BUILD`、`FILL_AFTER_ELIMINATION`、兜底 `KEYWORD_POOL_EXPANSION`。
- **`KEYWORD_PROMOTED_FROM_BROAD` 未实现**（:171 TODO：需搜索词聚合）；`COMPETITOR_INTERCEPT_WINDOW` 未实现（需 KB08）；`CUSTOM_KEYWORD_POOL` 未实现（需运营词池表）。

### 2.6 相关但未接通的模块

- `rules/expand_keywords.py`（KE-1~4）：方向评分器的"扩词校验规则"，依赖 `available_new_keywords`（该字段全仓库无赋值点，恒 None）→ 永远缺数据。
- `core/recommender.py`：扩词方向评分，消费同一恒 None 字段 → "搜索词报告约有 N 个高转化词尚未收录" 永不成立。
- `scenario_analyzer.py:118`：`available_new_keywords` 作为场景信号，同受恒 None 影响。

### 2.7 搜索词报告数据管道的实际状态（重要澄清）

搜索词报告**不是全仓库未接通**——它在广泛流（BROAD/PHRASE/AUTO）的否词链路已完整实现并消费：

| 环节 | 位置 | 说明 |
|---|---|---|
| 拉取 | `app/data/campaign_fetcher.py:629` `fetch_search_terms_for` | 用 `ad_campaign_search_term_report` MCP，**docstring 明确"仅广泛广告"**；带预过滤（有订单 / 点击≥3 / 曝光≥200且点击≤1 保留；点击≤1且0订单且曝光<200 过滤），花费降序取前10 |
| 预取 | `app/workflow/steps/campaign.py:2192` `_prefetch_search_terms` | **"预取 BROAD/PHRASE/AUTO 活动的 search_term 数据并注入 summaries"**，注入 `_search_term_data` |
| LLM 注入 | `app/llm/reasoner.py:1798-1812` | 注入 prompt"★搜索词报告"，注释指向 **KB 22 §3.3（否词）** |
| 否词消费 | `app/llm/reasoner.py:357` | 否词 prompt"每轮必读搜索词报告" |

**结论**：搜索词报告数据**已接通至广泛流否词链路**（判断否词用），但**未接入新增扩词流**（`NewKeywordFetcher` / `campaign_new.py` 不消费 `_search_term_data`，无"搜索词→承接精准"的候选来源）。搜索词来源缺的不是数据管道本身，而是**扩词流对搜索词数据的消费**。

---

## 3. 问题清单

### 问题1（P0）：流量入口来源的词被错误建成精准活动

流量词库（`flow` 来源）的词**没有任何验证信号**（无点击/无转化/无搜索词样本），按知识库应"广泛探词拿样本"（16号§4 BROAD 语义、28号§3.2 冷启动"广泛探词1条"）；但代码三层叠加把这类词建成精准：

1. `_CLASS_TO_MATCH_TYPE`：LLM 判 long_tail → 硬映射 EXACT（campaign_new.py:187-193）；
2. prompt"优先精准长尾词"软引导 LLM 多判 long_tail（reasoner.py:425）；
3. 排序长尾优先 + EXACT 优先，把流量长尾词顶进最终创建名单（campaign_new.py:74-83, 589）。

后果：未验证的流量词直接建精准，烧预算、ACOS 不可控，且破坏"先探词拿样本 → 后精准承接"的闭环；与 16号§4"BROAD = 低成本拿搜索词样本"直接冲突。

### 问题2（P0）：搜索词来源（搜索词报告→精准承接）在扩词流缺失

- **数据管道本身已存在**：`fetch_search_terms_for`（campaign_fetcher.py:629）+ `_prefetch_search_terms`（campaign.py:2192）已实现，消费于广泛流否词链路（reasoner.py:1798-1812，KB 22 §3.3）。
- **扩词流未消费**：`NewKeywordFetcher`（new_keyword_fetcher.py）候选来源只有 flow/own/history/suggest_bid，**无搜索词源**；`campaign_new.py` 不消费 `_search_term_data`；`KEYWORD_PROMOTED_FROM_BROAD` 触发场景未实现（campaign_new.py:171 TODO）。
- 方向评分层的 `ASINData.available_new_keywords` / `expand_keyword_candidates`（app/models/asin_data.py:148-149）**全仓库无赋值点**（仅测试赋值），恒 None → `rules/expand_keywords.py` KE-1 永远"缺少搜索词报告数据"。
- 后果：从已有广泛/自动活动中打出来的好搜索词（有订单/有 CVR）**没有任何路径被承接为精准活动**——这是 16号§1 第一个触发场景、28号§1 最高可信度来源（SRC_CONVERTED，R1，可 AUTO_APPROVED），业务价值最大的一块在扩词流中完全缺失。

### 问题3（P1）：代码 source 体系与知识库来源码不对应

| 代码 source（new_keyword_fetcher.py:304-314） | 知识库来源码（28号§1） | 差距 |
|---|---|---|
| `flow`（流量词库） | **无对应码** | 流量词库不在 28号§1 来源表内；其业务定位应为"流量入口/词池缺口"（16号§1 LOW_BUDGET_UTILIZATION / SEASONAL_ADVANCE_BUILD），语义上是广泛探词 |
| `ranking_opportunity`（自然位28-48） | 无直接对应（近似 16号§1 RANKING_OPPORTUNITY_NO_EXACT） | 触发场景有码但来源码缺 |
| `competitor`（竞品反查） | `SRC_COMPETITOR_ASIN` | 28号§4.1 要求**强制 HIGH_RISK_REVIEW**、单轮上限、毛利率门槛；代码未执行这些门槛 |
| — | `SRC_CONVERTED` / `SRC_AUTO_CLICKED` / `SRC_BROAD_DERIVED` / `SRC_AUTO_BROAD_TERM` / `SRC_BRAND_CATEGORY` | 全部未接入（依赖搜索词报告） |

### 问题4（P2）：竞品/品牌词风险门槛缺失

代码里 competitor 词与普通词同流程（campaign_new.py:364-384 竞品合并后直接进 LLM 选词），未执行 28号§4.1（强制 HIGH_RISK_REVIEW、单轮上限1、出价倍数0.8、毛利率<20% 禁扩）与 28号§4.2（品牌词强制 HIGH_RISK_REVIEW、预估日耗≤剩余预算30%、类目大词默认 EXACT 起投禁 BROAD 大词）。

### 问题5（P2）：匹配类型推导忽略验证信号

`_derive_match_type` 只看 keyword_class（campaign_new.py:196-201），不看来源/验证信号。知识库语义：匹配类型应由"是否有验证信号"决定——无信号探词→BROAD/PHRASE；有订单/CVR 信号→EXACT 承接（16号§4 + 28号§1）。

---

## 4. 权威业务知识参考（知识库条文摘录）

> 出处均为 `docs/knowledge_base/`，行号以 2026-08-04 为准。

### 4.1 16-新增活动规则 §1 触发场景（双来源的直接权威）

| 场景码 | 触发条件 | 数据来源 |
|---|---|---|
| `KEYWORD_PROMOTED_FROM_BROAD` | **准入以 `23号§3.4` 三通道为唯一权威**：订单通道（7天订单≥3 且 ACOS≤目标）/ 词根通道 / CVR 通道（CVR>品类均值×0.8 且 花费≥`03号§7.3` 无单花费门槛，强制 HIGH_RISK_REVIEW） | **搜索词报告** |
| `RANKING_OPPORTUNITY_NO_EXACT` | 该词自然位 28–48 且连续上升 且 尚无精准活动承接 | 自然排名数据 |
| `LOW_BUDGET_UTILIZATION` | 现有词池无法消耗预算（预算利用率连续3天 < 50%），需新增 Keyword 或 Campaign **补充流量入口** | 广告指标 |
| `SEASONAL_ADVANCE_BUILD` | 进入旺季前期或旺季，提前新增 Generic、Long-tail、Competitor、Custom 词池活动 | 淡旺季标签 |

### 4.2 16-新增活动规则 §4 主投广告位策略（流量入口 vs 精准承接的语义分界）

| 新增活动类型 | 广告类型归类 | 运营备注 |
|---|---|---|
| `match_type=BROAD`（广泛匹配扩词活动） | 广泛广告 | **低成本拿搜索词样本**，重点看预算、Bid、搜索词质量和否词 |
| Long-tail（长尾词）— Exact 投放 | 精准广告 | 长尾词相关性高，先跑 Bid / 预算数据 |
| Long-tail（长尾词）— BROAD/PHRASE 投放 | 广泛广告 | 按广泛广告策略处理，重点看搜索词质量和否词 |
| Exact（精准出单词） | 精准广告 | **词已验证有效**，先稳定 Bid 和预算 |

> 关键：16号§4 对 Long-tail 同时定义了两行（Exact 投放 / BROAD/PHRASE 投放），**没有"长尾必精准"的规定**。是否精准取决于词是否已验证。

### 4.3 28-新增词来源相关性与场景化配额规则 §1 词池来源映射

| 来源码 | 数据源 | 默认可信度 | 默认相关性档位 | 最低 review_level |
|---|---|---|---|---|
| SRC_CONVERTED | 搜索词报告中**有成交**的客户搜索词 | 最高 | R1 | AUTO_APPROVED（满足历史有效词定义时） |
| SRC_AUTO_CLICKED | 自动活动中有点击有曝光、暂无成交的搜索词 | 中 | R2 | MANUAL_REVIEW |
| SRC_BROAD_DERIVED | 广泛/词组活动衍生出的搜索词 | 中 | R2 | MANUAL_REVIEW |
| SRC_AUTO_BROAD_TERM | 自动活动里的宽泛搜索词 | 低 | R3 | MANUAL_REVIEW（命中§4门槛升 HIGH_RISK） |
| SRC_COMPETITOR_ASIN | 竞品 ASIN 反查词 | 最低 | R3 | **HIGH_RISK_REVIEW（强制人工）** |
| SRC_BRAND_CATEGORY | 品牌词 / 类目大词 | 低 | R3 | **HIGH_RISK_REVIEW（强制人工）** |

> **缺 `search_term_report` 时**：`SRC_CONVERTED` / `SRC_AUTO_CLICKED` / `SRC_BROAD_DERIVED` / `SRC_AUTO_BROAD_TERM` 全部不可用（`ONT-018` 词保真校验阻断，25号§4.2），仅竞品/品牌可用但强制人工——**即缺报告时扩词整体降级为「仅人工提案」**。

### 4.4 28号§3.2 场景词类型组合（"广泛探词 + 精准承接"）

- 新品测试期-完全无数据冷启动："广泛探词1条 + 精准承接"（long_tail、generic、competitor 限1个；R1 为主）。
- 新品测试期-现有词表现差："EXACT 测试为主，单词$3验证"。
- 预算缺口-保守填补："EXACT 为主，先不碰竞品/品牌词"（long_tail、generic(EXACT)，R1）。
- 旺季提前布局："多类型组合"（generic、long_tail、custom、competitor，R2）。

### 4.5 23号§3.4（16号§1 引用为唯一权威）

提词准入双通道（订单通道 + CVR 通道；CVR 通道强制人工）——见 00-知识库总纲 C-13 行。词根通道为 16号§1 三通道之一。

### 4.6 28号§4 高风险来源门槛（竞品/品牌，现状代码缺失）

- §4.1 竞品：强制 HIGH_RISK_REVIEW；单轮上限1（测试期）/3（旺季）；出价倍数 0.8 与防守位约束；毛利率<20% 禁扩（PRC-004）。
- §4.2 品牌/类目大词：强制 HIGH_RISK_REVIEW；预估单词日耗 ≤ ASIN 剩余预算 30% 否则 BLOCKED；**类目大词默认 match_type=EXACT 起投，禁止 BROAD 大词**。

### 4.7 每日新词额度（03号§7）

新增扩词每日额度共享上限 **15**（campaign_new 现用 `campaign_new_max_creates=20`，交接文档标注"待 KB/运营定夺"，与此冲突）。

---

## 5. 差距对照表（知识库要求 vs 代码现状）

| 知识库要求 | 代码现状 | 差距等级 |
|---|---|---|
| 搜索词报告来源（SRC_CONVERTED / SRC_BROAD_DERIVED / SRC_AUTO_CLICKED）→ 精准承接（KEYWORD_PROMOTED_FROM_BROAD 三通道准入） | 数据管道已存在（广泛流否词链路消费），但**扩词流未消费**：NewKeywordFetcher 无搜索词源、campaign_new.py 无 KEYWORD_PROMOTED_FROM_BROAD | **P0 缺失** |
| 流量入口来源（LOW_BUDGET_UTILIZATION / 词池缺口）→ 广泛探词拿样本 | flow 来源已实现，但被 long_tail→EXACT 映射 + prompt 引导 + 排序建成了精准 | **P0 错误** |
| 来源码体系（SRC_* 六码）与可信度/相关性/review 等级联动 | 代码 source 三值（flow/ranking_opportunity/competitor），无可信度/无 review 联动 | P1 |
| 竞品词强制 HIGH_RISK_REVIEW + 上限 + 毛利率门槛 | 未执行 | P2 |
| 品牌词强制 HIGH_RISK_REVIEW + 日耗上限 + 禁 BROAD 大词 | 未接入（无品牌来源） | P2 |
| 每日新词额度 ≤15 | `campaign_new_max_creates=20` | P2（交接文档已标"待定夺"） |

---

## 6. 修复建议（方向，未实施）

1. **扩词流接入搜索词来源**：复用既有 `fetch_search_terms_for`（campaign_fetcher.py:629，`ad_campaign_search_term_report`）与 `_prefetch_search_terms`（campaign.py:2192）管道，把 `_search_term_data` 接入新增扩词流候选；给 `ASINData.available_new_keywords` / `expand_keyword_candidates` 赋值（激活 KE-1~4 规则与扩词方向评分）。
2. **新增流补搜索词来源**：候选词发现加"搜索词报告"源，按 16号§1 `KEYWORD_PROMOTED_FROM_BROAD` + 23号§3.4 三通道准入，产出的词建**精准**承接（SRC_CONVERTED → R1 → 可 AUTO_APPROVED）。
3. **流量来源改建广泛**：`_derive_match_type` 改为"按来源+验证信号"推导——流量词库/无验证信号 → BROAD/PHRASE（探词拿样本）；有订单/CVR/词根验证 → EXACT（承接）。去掉 `long_tail→EXACT` 无条件映射。
4. **prompt 修正**：reasoner.py:425"优先精准长尾词"改为来源感知（流量入口词引导判"探词"语义而非"承接"语义）；few-shot 示例同步。
5. **来源码对齐**：代码 source 对齐 28号§1 六码，接入可信度/相关性档位/review_level 联动；竞品/品牌执行 §4 高风险门槛。
6. **额度对齐**：`campaign_new_max_creates` 20 → 15（03号§7），或与运营确认。

---

## 7. 相关文件索引

| 文件 | 说明 |
|---|---|
| `ad-direction-agent/app/data/new_keyword_fetcher.py` | 新增流数据编排（flow/own/history/suggest_bid，无搜索词） |
| `ad-direction-agent/app/workflow/steps/campaign_new.py` | 新增流主流程（match_type 映射 / 排序 / 触发场景 / L171 TODO） |
| `ad-direction-agent/app/llm/reasoner.py` | `recommend_new_campaigns` prompt（L394-448：keyword_class/match_type 禁止输出/精准长尾引导） |
| `ad-direction-agent/app/data/campaign_fetcher.py:629` | `fetch_search_terms_for`：搜索词报告拉取（仅广泛广告，`ad_campaign_search_term_report`）——扩词流可复用的管道 |
| `ad-direction-agent/app/workflow/steps/campaign.py:2192` | `_prefetch_search_terms`：BROAD/PHRASE/AUTO 活动搜索词预取并注入 `_search_term_data` |
| `ad-direction-agent/app/llm/reasoner.py:1798-1812, 357` | 搜索词报告注入 LLM（否词链路消费，KB 22 §3.3） |
| `ad-direction-agent/app/models/asin_data.py:148-149` | `available_new_keywords` / `expand_keyword_candidates`（无赋值点） |
| `ad-direction-agent/app/rules/expand_keywords.py` | KE-1~4 扩词校验规则（依赖恒 None 字段） |
| `ad-direction-agent/app/core/recommender.py` | 扩词方向评分（消费恒 None 字段） |
| `ad-direction-agent/app/core/scenario_analyzer.py:118` | available_new_keywords 场景信号 |
| `docs/knowledge_base/执行规则/16-新增活动规则.md` | 双来源权威（§1 触发场景 / §4 主投广告位语义） |
| `docs/knowledge_base/执行规则/28-新增词来源相关性与场景化配额规则.md` | 来源码 SRC_* / 相关性 / 配额 / 高风险门槛权威 |
| `docs/knowledge_base/执行规则/23-广告组合与预算分配规则.md` | §3.4 提词三通道（KEYWORD_PROMOTED_FROM_BROAD 准入） |
