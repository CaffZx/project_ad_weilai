# 6.12 待办清单

> 最后更新：2026-06-12 18:00

---

## 一、前端合并：campaign 进主看板第 5 tab ✅ 已完成

| 项目 | 状态 |
|---|---|
| campaign-panel/ 6 文件（ES module + 事件委托 + CSS 前缀） | ✅ |
| 主看板 tab5 挂载点 + lazy-mount | ✅ |
| 跳转自动传父 ASIN（选定 ASIN 即解锁 tab5） | ✅ |
| mode 语义修正：执行层双轨均可勾选审核 | ✅ |
| executable 门禁（`state.executable` 替换 `mode` 判断） | ✅ |
| in-panel 模式条移除（改由页面级批次选择器驱动） | ✅ |
| review badge CSS `.camp-root` 作用域 | ✅ |

### 🔴 前端剩余接线

- [ ] 批次栏改 `run_id` 作批次句柄（前端之前用 `decision_id`，DB 已确认 `decision_id` 写库时才生成）
- [ ] `新建分析事件` 文案/流程适配新模型（state session 而非 ERP DRAFT）
- [ ] tab5 挂载传 `run_id` + 快照 `_fetchSnapshot(asin, decision_id)` 接线
- [ ] confirm 按钮调真 `/campaign/confirm` API（后端已通）
- [ ] 前置 1-4 只读快照渲染（`/decision/{id}/preset` 已提供数据）

---

## 二、后端接口

### 实时轨 ✅

- [x] `POST /campaign/analyze` — 保持老格式（campaign_test.html 独立调试）
- [x] `POST /campaign/viewmodel` — 返回统一 ViewModel（tab5 实时轨专用）
- [ ] 纯渲染派生字段从后端移除（低优，不影响功能）

### 快照轨 ✅ P2 核心已完成

- [x] `GET /campaign/snapshot?decision_id=` — 不再 stub，读真 DB（6 表 → ViewModel）
- [x] `from_db_snapshot()` — 21 表反向 mapper（card + 3×pending → items）
- [x] summary 8 列拆字段 → `portfolio_constraints` 重组
- [x] `suggest_category` 枚举 → `action` 推导（含 `CREATE`、group_type 兜底）
- [x] `GET /decision/context` — 批次列表 + A/B/C 判定
- [x] `GET /decision/{id}/preset` — 前置 1-4 配置快照（ERP 码→中文 label）
- [x] `POST /decision/new-event` — 新建分析（写 state session，不碰 ERP）
- [x] `POST /decision/cancel-event` — 取消分析（清 state session）
- [x] `/campaign/confirm` — 不再 stub，真写 card + 3×pending `confirm_status` + 幂等 + 批次可执行校验

### DB 迁移 ✅

- [x] `t_advert_agent_decision` 加 `is_latest` / `analysis_mode`
- [x] `card` 加 `keyword_class` / `review_level` / `is_core` / `perf_json` / `is_prefiltered` / `prefilter_reason`
- [x] `summary` 加 `create_count` / `analysis_overview` / `version` / portfolio 8 列
- [x] `decision_config` 加 `enabled` / `frequency` / `last_run_time` / `last_decision_id`（P4 依赖）
- [x] synthesis 3 表 collation 统一 `utf8mb4_general_ci`
- [x] `is_latest` 回填已执行（每 ASIN 唯一）
- [x] state 库 `analysis_session` 表（进行中标记）

### 硬缺口

- [ ] 新增活动落库（P2 待办，复用 card + pending，`suggest_category=CREATE`）
- [ ] 逐活动 perf 落库（card.perf_json 列已备，写入端未填）
- [ ] 预过滤活动落库（card.is_prefiltered 列已备）

---

## 三、决策批次驱动改造

### P1 批次生命周期 ✅

- [x] DB 迁移（`is_latest` / `analysis_mode`）
- [x] `/decision/*` 三端点
- [x] 前端批次栏 + A/B/C 三态
- [x] executable 门禁 + 模式条移除
- [x] 进行中态从 ERP decision 表迁到 state 库 `analysis_session`

### P2 快照 + confirm ✅

- [x] 快照回读 mapper（`from_db_snapshot`）
- [x] `/campaign/snapshot` 接真库
- [x] `/campaign/confirm` 真写 pending
- [x] `finalize_batch` 闭环（`_maybe_push_erp` 成功后自动翻 `is_latest`）

### P3 前置只读 + 生命周期闭环 🔴

- [ ] 前置 1-4 只读快照渲染（`/decision/{id}/preset` 数据已通，前端未接）
- [ ] tab5 B↔C unmount/remount 生命周期
- [ ] `_pendingBatchSwitch` 死代码接线（切批次 → tab5 重挂）

### P4 定时调度器 🔴

- [ ] APScheduler / cron 扫 `decision_config(enabled=1)` → 逐 ASIN 跑执行层
- [ ] 前端轮询感知新批次（`latest_completed_updated_at` 比对）

---

## 四、Campaign 分析引擎功能

### 新增活动分析 ✅

- [x] 候选词发现 + 硬过滤 + 双轮 LLM 取交集（KB 16+06）
- [x] `_calc_initial_bid`（KB 16 §3）
- [x] `_generate_campaign_name`（运营拍板格式）
- [ ] 建议竞价 MCP `whp_amazon_advert_keyword_suggest_bid` 已上线，**未接入 `analyze_new_campaigns`**

### 广告位加价比例 ✅

- [x] MCP 字段解析（3 路径）+ 注入 `_placement_pcts` → LLM prompt
- [x] `_backfill_placement_pcts` 代码回填 `current_pct` + `proposed_pct`
- [x] prompt schema 改：LLM 只输出 `action` + `evidence`
- [ ] LLM 产出 placement 覆盖率 41/69（28 个活动未产出），prompt 待加强

### 预过滤可见化 ✅

- [x] 淘汰池打 `__prefiltered` + 去重 + 多词活动硬编码
- [x] 前端灰卡区分渲染

### 待办

- [ ] 淘汰重启功能
- [ ] Synthesis JSON 截断偶发修复
- [ ] 建议竞价 MCP 接入 `analyze_new_campaigns`
- [ ] `campaign_test.html` 独立调试页回归验证（被 tab5 拆走后是否仍正常工作）

---

## 五、基础设施

- [x] DEEPSEEK_MODEL=deepseek-v4-flash + LLM 分级
- [x] 代码清理：known_listings / _context_from_known / pyc / _erp_q
- [ ] ERP 凭据硬编码去重（`settings.py` / `_erp_conn.py` 三处）
- [ ] 日志轮转（`server.log` 1.15MB）
- [ ] 建议竞价字段接入（MCP 已上线，代码待接）
