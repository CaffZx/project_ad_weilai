# 测试与验证地图

## 目的

本篇把现有测试文件映射到项目子系统，帮助开发者和开发 Agent 在改动后选择最小但有效的验证范围。

## 当前代码真源

- 测试目录：`ad-direction-agent/tests`
- 项目配置：`ad-direction-agent/pytest.ini` 或同级测试配置文件
- 业务代码：`ad-direction-agent/app`

## 代码锚点地图

| 测试域 | 主要目录/文件 | 覆盖对象 |
| --- | --- | --- |
| MCP client/adapter | `tests/data/test_mcp_client.py`、`tests/data/test_mcp_adapter.py` | JSON-RPC 解包、adapter 错误处理、MCP 参数 |
| MCP 日期/上下文 | `tests/test_mcp_date_window.py`、`tests/test_mcp_resolve_context.py` | 站点时区、parent ASIN 上下文 |
| Campaign discovery | `tests/test_mcp_campaign_discover.py`、`tests/test_campaign_basic_info_batch.py` | campaign list、basic_info_v2、discovery 合并 |
| Campaign 护栏 | `tests/workflow/test_campaign_guardrails.py`、`test_campaign_guardrail_retry.py` | P0-P11、retry instruction、R3/R4 注入 |
| Campaign 预算/复评/新建 | `tests/workflow/test_budget_reallocation.py`、`test_campaign_restart.py`、`test_campaign_new_quota.py` | portfolio budget、新建活动配额、低价池复评 |
| 前置策略 | `tests/workflow/test_api_parity.py`、`test_lightweight_loading.py`、`test_data_contract.py` | API parity、meta_filter、data gate |
| 规则推荐器 | `tests/rules/*.py`、`tests/test_recommender_scoring.py` | 四方向规则和 scoring |
| ERP writer | `tests/persistence/test_erp_*.py`、`test_ai_suggest_numeric_null.py` | card/pending/enums/tab4/auto push |
| Redis/State | `tests/persistence/test_redis_cache.py`、`test_override_persistence.py` | 缓存、override |
| 执行链路 | `tests/test_advert_exec_child_asin.py`、`tests/persistence/test_pool_entry_sync.py` | execution mapper、pool sync |
| 前端迁移 | `tests/test_demo_ux_migration.py` | demo HTML/UX 关键约束 |
| KB 切片 | `tests/test_kb_slicing.py` | preset、章节和 enum |

## 推荐命令

在 `ad-direction-agent` 目录执行：

```powershell
python -m pytest tests -q
```

按子系统执行：

```powershell
python -m pytest tests/data tests/workflow -q
python -m pytest tests/persistence -q
python -m pytest tests/test_kb_slicing.py -q
python -m pytest tests/test_mcp_date_window.py tests/test_mcp_resolve_context.py -q
```

如果当前环境缺少外部服务，应优先运行不依赖真实 MCP/ERP 的单元测试，并在结果中说明跳过原因。

按风险选择验证：

| 改动类型 | 最小建议验证 |
| --- | --- |
| 改 prompt/KB preset | `python -m pytest tests/test_kb_slicing.py tests/test_reasoner_polish.py -q` |
| 改 MCP 工具名/参数 | `python -m pytest tests/data tests/test_mcp_date_window.py tests/test_mcp_resolve_context.py tests/test_mcp_campaign_discover.py -q` |
| 改 Campaign 护栏 | `python -m pytest tests/workflow/test_campaign_guardrails.py tests/workflow/test_campaign_guardrail_retry.py -q` |
| 改 Campaign 主流程 | `python -m pytest tests/workflow tests/test_campaign_basic_info_batch.py -q` |
| 改 ERP 映射 | `python -m pytest tests/persistence -q` |
| 改前端 Campaign panel | `python -m pytest tests/test_demo_ux_migration.py -q` + 浏览器手动检查 |
| 改 Advert 执行 | `python -m pytest tests/test_advert_exec_child_asin.py tests/persistence/test_pool_entry_sync.py -q` |

## MCP 与数据层

相关测试：

- `tests/data/test_data_fetcher.py`
- `tests/data/test_mcp_adapter.py`
- `tests/data/test_mcp_client.py`
- `tests/test_mcp_campaign_discover.py`
- `tests/test_mcp_date_window.py`
- `tests/test_mcp_resolve_context.py`
- `tests/test_prefetch_ctx_regression.py`
- `tests/skills/test_mcp_query_playbook.py`

适用改动：

- `mcp_mapping.py`
- `mcp_adapter.py`
- `mcp_client.py`
- `mcp_db_context.py`
- 日期窗口和站点时区逻辑
- Campaign discovery 参数

这些测试通常应避免真实依赖 MCP 网关；需要 live smoke 时使用脚本或手工工具，并在验证说明中标出“live MCP”。

## Campaign 工作流

相关测试：

- `tests/workflow/test_campaign_guardrails.py`
- `tests/workflow/test_campaign_guardrail_retry.py`
- `tests/workflow/test_campaign_restart.py`
- `tests/workflow/test_campaign_resolve_conflicts.py`
- `tests/workflow/test_campaign_new_quota.py`
- `tests/workflow/test_budget_reallocation.py`
- `tests/test_campaign_basic_info_batch.py`

适用改动：

- Campaign LLM 输出处理
- 护栏
- 新建活动
- 预算重分配
- 复评/低价池
- basic info v2 批处理

Campaign 护栏修改必须至少覆盖：

- 触发规则本身。
- 不触发的边界值。
- 与 P0/P1/P2/P3 优先级相关的冲突场景。
- retry instruction 是否只约束违规点，避免把 LLM 重判诱导成一律 keep。

## 前置策略工作流

相关测试：

- `tests/workflow/test_api_parity.py`
- `tests/workflow/test_lightweight_loading.py`
- `tests/workflow/test_data_contract.py`
- `tests/test_recommender_scoring.py`
- `tests/test_target_acos_band.py`
- `tests/rules/test_balance_maintain.py`
- `tests/rules/test_cross_tag.py`
- `tests/rules/test_optimize_acos.py`
- `tests/rules/test_expand_keywords.py`
- `tests/rules/test_push_natural.py`

适用改动：

- Strategy/Tactics/Diagnosis/Execution
- P3 推荐
- 轻量 meta_filter
- 规则推荐器

前置策略工作流的验证重点不是只看返回 200，还要看：

- data gate 是否正确 blocked/degraded。
- meta_filter 是否没有拉过多工具。
- options 路径是否避免意外重跑 LLM。
- confirm 是否写入长期配置并推进 layer。

## ERP 与持久化

相关测试：

- `tests/persistence/test_redis_cache.py`
- `tests/persistence/test_pool_entry_sync.py`
- `tests/persistence/test_override_persistence.py`
- `tests/persistence/test_erp_warehouse_ids.py`
- `tests/persistence/test_erp_tab4_content.py`
- `tests/persistence/test_erp_gray_cards.py`
- `tests/persistence/test_erp_enums.py`
- `tests/persistence/test_erp_auto_push.py`
- `tests/persistence/test_ai_suggest_numeric_null.py`

适用改动：

- Redis 缓存
- State manager
- ERP writer
- Campaign card/pending 映射
- 池同步
- enum 映射

ERP 测试通常验证 canonical mapper 和 repository 行为，不等同于真实数据库迁移验证。涉及 live schema 或生产库字段时，还需要单独跑数据库审计/写入 smoke。

## 前端与 UX

相关测试：

- `tests/test_demo_ux_migration.py`
- 前端改动还应手动打开 `demo/ad-asisitant-agent.html` 或本地服务页面检查。

适用改动：

- HTML 结构
- Campaign panel render/state/events/viewmodel
- toast 和交互状态

前端手动检查清单：

- realtime 与 snapshot 两种模式都能渲染。
- `vm.is_latest=false` 时不可执行。
- 选择、全选、分组选择、确认弹窗、取消确认都正常。
- 执行失败 toast 可复制、可关闭、长文本不溢出。
- move_errors 显示活动名而不是只有数量。

## 广告执行

相关测试：

- `tests/test_advert_exec_child_asin.py`
- `tests/persistence/test_pool_entry_sync.py`

适用改动：

- `advert_exec_mapper.py`
- `advert_execution.py`
- portfolio 匹配
- pending 到 Advert MCP 参数映射

执行链路验证时默认应保持 dry-run。只有在明确的受控环境中，才把 `advert_mcp_enabled=true` 且 `advert_exec_dry_run=false` 作为真实执行验证。

## 知识库切片

相关测试：

- `tests/test_kb_slicing.py`

适用改动：

- `kb_loader.py`
- `docs/knowledge_base`
- preset 和 enum 映射

## 常见误区

- 只跑全量测试失败后不定位子系统，会浪费大量时间。
- 修改 MCP 参数后只跑业务规则测试不够。
- 修改前端 viewmodel 后只跑后端测试不够，还应手动或浏览器验证 UI。
- 外部 MCP/ERP 不可用时，应明确说明验证覆盖缺口。

## 更新检查清单

- 新增测试文件后，同步加入本篇对应子系统。
- 新增关键链路但没有测试时，在对应专题文档中标出验证缺口。
- 修改测试目录结构后，同步更新推荐命令。
- 修改 CI 命令后，同步更新本篇。
