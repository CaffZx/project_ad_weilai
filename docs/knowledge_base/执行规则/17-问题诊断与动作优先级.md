# 问题诊断与动作优先级（diagnosis_action_priority.yaml）

定义 Agent 如何将完整策略标签栈 + 数据诊断结论 + 记忆上下文，转化为有序的 Campaign 执行动作建议。

> **在决策链中的位置**
> `18-广告执行调整流程.md` Step 7（分层诊断）完成后，**立即进入本文**。
> 本文输出有序动作列表，再交由 `15-策略约束值规则.md` 取具体约束值，最终生成提案。

---

## 0. 完整输入结构

Agent 执行本文逻辑前，必须已读取以下所有输入：

```
主输入（决定做什么）：
  ① 广告方向   push_natural_rank / expand_keywords / optimize_acos / balance_maintain
  ② 广告目的   traffic / conversion / ranking / profit
  ③ 问题类型   见 §1（由本轮数据诊断得出）

策略修正（决定幅度和保护规则）：
  ④ 产品定位   P0 / P1 / P2 / P3
  ⑤ 产品阶段   testing / pushing / harvesting / maintaining / liquidating
  ⑥ 淡旺季     淡季 / 旺季前期 / 旺季 / 旺季末期
  ⑦ 目标ACOS  定义"达标"基准线，所有 ACOS 高低判断均对比此值
  ⑧ 目标预算  定义"花够/花不够"的基准线

记忆修正（决定是否调整方向或强度）：
  ⑨ cooldown_active             该 Campaign 是否在冷却期
  ⑩ recent_effectiveness_status 上次同类动作的复盘结论
  ⑪ reverse_action_risk         本次建议是否与上次方向相反
```

---

## 1. 问题类型定义

### 1.1 判断顺序（命中即止）

```
① 特殊场景已激活（02号§7）→ 直接走特殊场景逻辑，跳出本文
② BLOCKED 类（数据/库存阻断）
③ SAMPLE_INSUFFICIENT（样本不足）
④ CVR_WEAK（页面转化问题）
⑤ ACOS 类问题
⑥ 预算类问题
⑦ 排名类问题
⑧ 词池 / 广告位类问题
⑨ ALL_HEALTHY
```

### 1.2 问题类型表

**注意：所有 ACOS 类问题的判断阈值，必须先用 `15-策略约束值规则.md §4` 计算当前有效容忍上限，不得使用硬编码倍数。**

| 问题类型代码 | 判断条件 | 描述 |
|------------|---------|------|
| `BLOCKED_INVENTORY` | 库存天数 < 7天 | 库存不足，阻断所有增长动作 |
| `BLOCKED_DATA_MISSING` | 必需数据缺失（排名/广告位/库存） | 数据缺失，无法决策 |
| `SAMPLE_INSUFFICIENT` | 活动上线 < 3天 OR 7天花费 < $5 OR 7天点击 < 10次 | 样本不足 |
| `CVR_WEAK` | 3天曝光 > 1500 且 CVR < 品类均值×30% | 曝光够但转化差，Listing 问题 |
| `HIGH_ACOS_NO_ORDER` | 3天ACOS > 当前有效容忍上限 且 7天订单 = 0 | 花钱无产出 |
| `HIGH_ACOS_LOW_ORDER` | 3天ACOS > 当前有效容忍上限 且 7天订单 < 目标80% | 超标且出单不足 |
| `HIGH_ACOS_WITH_ORDER` | 3天ACOS > 目标ACOS 但 ≤ 有效容忍上限，有订单 | ACOS偏高但未超容忍，有出单 |
| `BUDGET_NO_SPEND` | 7天花费 = $0 | 完全无花费 |
| `BUDGET_CANT_SPEND` | 预算利用率 < 50% 且 有花费 | 花不出去 |
| `BUDGET_EXHAUSTED` | 预算利用率 = 100% 且 ACOS ≤ 目标×1.1 | 预算不够用且效果好 |
| `RANK_OPPORTUNITY` | 自然位 ≤ 48 且 7天排名上升 ≥ 2位 且 ACOS ≤ 有效容忍上限 | 排名上升机会 |
| `RANK_DROPPING` | 自然排名连续7天下滑 ≥ 3位 | 排名在跌 |
| `KEYWORD_POOL_DIRTY` | 广泛广告：高点击无单词 > 5个 OR 不相关词 > 30% | 词池质量差（仅广泛广告） |
| `KEYWORD_POOL_EXHAUSTED` | 精准广告有效词 ≤ 3个 且 7天花费 < $10 | 精准词池枯竭 |
| `PLACEMENT_INEFFICIENT` | 精准广告某广告位：花费 > $5 且 点击 > 7次 且 订单 = 0 | 某广告位高消耗无单 |
| `ALL_HEALTHY` | 以上均不命中 | 全部正常 |

---

## 2. 有效容忍上限计算（使用前必算）

在判断任何 ACOS 类问题前，先按以下公式从 `15-策略约束值规则.md §4` 得出当前有效容忍上限：

```
有效容忍上限 = 目标ACOS
             + 阶段容忍加值（见15号§4）
             + 广告目的=ranking 时额外加值
             + 淡旺季=旺季 时额外加值
             + 特殊场景=promotion 时额外加值
```

> 示例：目标ACOS=25%，pushing阶段+10%，ranking目的+10%，旺季+10%
> 有效容忍上限 = 25% + 10% + 10% + 10% = **55%**
> 当前ACOS=38% < 55%，**不触发 ACOS 类问题**，不降Bid。

---

## 3. 核心动作矩阵：广告方向 × 问题类型 → 有序动作

### 3.1 广告方向 = `push_natural_rank`（推自然排名）

| 问题类型 | 有序动作列表 |
|---------|------------|
| `ALL_HEALTHY` / `RANK_OPPORTUNITY` | ① 提主推词Bid ② TOS加价 ③ 确认预算不断供 |
| `HIGH_ACOS_WITH_ORDER` | ① 确认ACOS在容忍上限内（不降主推词）② 降非排名词Bid ③ 维持预算 |
| `HIGH_ACOS_LOW_ORDER` | ① 查15号§4确认容忍上限 → 容忍内：降非排名词Bid，不动主推词；超容忍：进入§4冲突裁决 |
| `HIGH_ACOS_NO_ORDER` | ① 进入§4冲突裁决（Ranking目的 vs 无出单） |
| `RANK_DROPPING` | ① 检查预算是否断供 → 加预算 ② 提主推词Bid ③ TOS加价恢复 |
| `BUDGET_CANT_SPEND` | ① 提主推词Bid（排名词花不出去是严重信号）② 检查词量 ③ 加预算 |
| `BUDGET_NO_SPEND` | ① 大幅提Bid ② 确认广告位加价比例初始值（见19号）③ 检查活动状态 |
| `BUDGET_EXHAUSTED` | ① 加预算（排名任务不能断供）② 提Bid ③ TOS加价 |
| `KEYWORD_POOL_DIRTY` | ① 先否词净化 ② 否词后重新评估是否继续push_natural_rank |
| `CVR_WEAK` | ① 阻断push动作 ② 建议运营诊断Listing ③ 维持现状 |
| `SAMPLE_INSUFFICIENT` | ① 观察，不做Bid/预算大动作 |

---

### 3.2 广告方向 = `expand_keywords`（扩词/引流）

| 问题类型 | 有序动作列表 |
|---------|------------|
| `ALL_HEALTHY` | ① 从搜索词报告提有效词 ② 新增精准承接活动 ③ 新增广泛测词活动 |
| `HIGH_ACOS_WITH_ORDER` | ① 否词净化无效词 ② 保留有效词继续扩展 ③ 降无效词Bid |
| `HIGH_ACOS_LOW_ORDER` | ① 否词优先 ② 净化后再从好词中扩展 ③ 不在高ACOS词池上扩词 |
| `HIGH_ACOS_NO_ORDER` | ① 进入§4冲突裁决（Traffic目的继续扩 vs 完全无单停止扩） |
| `BUDGET_CANT_SPEND` | ① 检查Bid是否过低 → 提Bid ② 检查词量 → 扩词补充 ③ 小幅加预算 |
| `BUDGET_NO_SPEND` | ① 大幅提Bid ② 检查词相关性 ③ 补充词量 |
| `BUDGET_EXHAUSTED` | ① 加预算 ② 扩词承接更多流量 |
| `KEYWORD_POOL_DIRTY` | ① 先否词净化 ② 净化完成后再扩词（不在脏词池上扩） |
| `KEYWORD_POOL_EXHAUSTED` | ① 竞品反查补词 ② 新增广泛活动探词 ③ 历史有效词重新激活 |
| `CVR_WEAK` | ① 阻断扩词动作 ② 建议Listing诊断 |
| `SAMPLE_INSUFFICIENT` | ① 等待数据积累，不新增活动 |

---

### 3.3 广告方向 = `optimize_acos`（控ACOS/提效率）

| 问题类型 | 有序动作列表 |
|---------|------------|
| `HIGH_ACOS_NO_ORDER` | ① 否词（广泛）/ 降Bid（精准）② 大幅降预算 ③ 按21号诊断路径评估淘汰 |
| `HIGH_ACOS_LOW_ORDER` | ① 否词治理 ② 降低效词Bid ③ 精准广告降无效广告位加价 ④ 降预算 |
| `HIGH_ACOS_WITH_ORDER` | ① 否词治理 ② 广告位优化（降无效位置）③ 小幅降Bid ④ 维持预算 |
| `KEYWORD_POOL_DIRTY` | ① 否词 ② 降Bid ③ 按21号路径评估淘汰 |
| `PLACEMENT_INEFFICIENT` | ① 降该广告位加价 ② 不动基础Bid ③ 不直接淘汰活动 |
| `ALL_HEALTHY` | ① balance_maintain_no_action（optimize方向但数据正常，保持） |
| `BUDGET_EXHAUSTED` | ① 先优化ACOS再看是否加预算，不能边ACOS高边加预算 |
| `SAMPLE_INSUFFICIENT` | ① 观察，不否词，不淘汰 |

---

### 3.4 广告方向 = `balance_maintain`（平衡维持）

| 问题类型 | 有序动作列表 |
|---------|------------|
| `ALL_HEALTHY` | ① balance_maintain_no_action ② 检查是否有RANK_OPPORTUNITY可顺势推 |
| `HIGH_ACOS_WITH_ORDER` | ① 小幅否词 ② 不做大动作 ③ 审核等级提升至 MANUAL_REVIEW |
| `HIGH_ACOS_LOW_ORDER` | ① 标记异常 ② 建议运营确认是否切换方向至 optimize_acos |
| `RANK_DROPPING` | ① 标记预警 ② 建议运营确认是否切换方向至 push_natural_rank |
| `BUDGET_EXHAUSTED` | ① 小幅加预算（balance_maintain方向不激进加量）|
| `BUDGET_CANT_SPEND` | ① 维持（balance_maintain方向下花不出去可接受，不强行放量）|

---

## 4. 冲突裁决规则

当 广告方向 指向增长/推排名，但 数据问题 显示效果不佳时，用 **广告目的** 裁决。

| 广告方向 | 问题类型 | 广告目的 | 裁决结果 |
|---------|---------|---------|---------|
| push_natural_rank | HIGH_ACOS_LOW_ORDER | ranking | ACOS在容忍上限内 → 继续推排名；超上限 → 降非排名词，观察7天 |
| push_natural_rank | HIGH_ACOS_LOW_ORDER | conversion / profit | 停止执行 push_natural_rank，切换执行 optimize_acos 逻辑 → MANUAL_REVIEW |
| push_natural_rank | HIGH_ACOS_NO_ORDER | ranking | 保护主推词；降非排名词和预算；7天无改善进入淘汰评估 |
| push_natural_rank | HIGH_ACOS_NO_ORDER | conversion / profit | 停止push；执行 optimize_acos；建议目的切换 → SENIOR_APPROVAL |
| expand_keywords | HIGH_ACOS_NO_ORDER | traffic | 否词净化后继续扩，控制词质量 |
| expand_keywords | HIGH_ACOS_NO_ORDER | conversion / profit | 停止扩词；先治理ACOS；ACOS达标后再扩 |
| push_natural_rank | BLOCKED_INVENTORY | 任何 | BLOCKED，停止所有增长动作 |
| expand_keywords | CVR_WEAK | 任何 | BLOCKED，停止扩词，优先Listing诊断 |
| optimize_acos | RANK_OPPORTUNITY | ranking | 冲突：建议运营确认是否切换方向 → MANUAL_REVIEW |
| balance_maintain | RANK_DROPPING | 任何 | 冲突：建议运营升级为 push_natural_rank → MANUAL_REVIEW |

---

## 5. 策略修正：定位 + 阶段 + 淡旺季

§3和§4得出初步动作列表后，按以下规则修正幅度和保护规则。

### 5.1 产品定位修正

| 定位 | 附加约束 |
|-----|---------|
| P0 | 核心词Bid每次降幅 ≤ 5%；主力活动禁止自动淘汰；淡季保留Conversion承接和balance_maintain执行方向 |
| P1 | 正常执行，无额外约束 |
| P2 | 预算上限收紧至15号§2中P2列；超标持续7天才进淘汰评估 |
| P3 | 禁止追大词排名；TOS加价上限10%；目标ACOS通常≤30% |

### 5.2 产品阶段：动作幅度系数

| 产品阶段 | Bid幅度系数 | 预算幅度系数 | 特殊约束 |
|---------|-----------|-----------|---------|
| testing | ×0.5 | ×0.5 | 禁止淘汰（<14天）；禁止TOS加价；禁止大幅扩词 |
| pushing | ×1.2 | ×1.2 | 允许TOS加价；允许大幅加预算；ACOS容忍度最高 |
| harvesting | ×1.0 | ×1.0 | 标准执行；不新增大预算活动 |
| maintaining | ×0.7 | ×0.7 | 只做微调；不做策略性大动作 |
| liquidating | ×0.8 | 上限$80 | 只允许PP加价；禁止TOS加价；禁止扩词 |

### 5.3 淡旺季：幅度系数叠加

| 淡旺季 | Bid系数 | 预算系数 |
|-------|--------|--------|
| 淡季 | ×0.8 | ×0.7 |
| 旺季前期 | ×1.1 | ×1.2 |
| 旺季 | ×1.2 | ×1.5 |
| 旺季末期 | ×0.9 | ×0.85 |

> **最终幅度 = 19号基础幅度 × 阶段系数 × 淡旺季系数**
> 示例：19号大涨=+10%，pushing×1.2，旺季×1.2 → 实际幅度=+10%×1.2×1.2=**+14.4%，取整+14%**
>
> 注意：本节预算系数用于修正**本次预算调整幅度**，不用于计算预算范围。预算范围仍按 `15-策略约束值规则.md §2.2` 判断。

---

## 6. 记忆修正

§3~§5 得出最终动作列表后，检查记忆信号，按以下规则修正输出。

| 记忆信号 | 修正规则 |
|---------|---------|
| `cooldown_active = true` | 同方向动作强度降50%；禁止反向动作；输出带冷却期说明 |
| `recent_effectiveness_status = not_achieved` | 不重复上次同类动作；尝试备选动作；审核等级升至MANUAL_REVIEW |
| `recent_effectiveness_status = not_achieved` 连续2次 | 触发长期记忆查询；审核等级升至SENIOR_APPROVAL |
| `reverse_action_risk = true` | 强制MANUAL_REVIEW；输出中说明上次方向和本次方向的冲突原因 |

---

## 7. 淘汰前诊断路径（不可跳过）

§3中任何"评估淘汰"动作，必须先按以下路径执行，不得跳过直接淘汰：

```
精准广告：
  广告位花费高无单？→ 先降广告位加价，观察3天
    仍无改善？→ 降基础Bid
      仍无改善？→ 评估淘汰（按21号执行）

广泛/词组/自动广告：
  高点击无单？→ 先执行否词
    否词后仍无改善？→ 降Bid / 降预算
      仍无改善？→ 评估淘汰（按21号执行）
```

> 参见 `21-淘汰广告活动规则.md §3`

---

## 8. Agent 输出格式

```yaml
diagnosis:
  problem_type: "HIGH_ACOS_LOW_ORDER"
  acos_tolerance_calc: "目标25% + pushing+10% + ranking+10% + 旺季+10% = 55%，当前ACOS38%未超容忍上限"
  evidence:
    - "3天ACOS=38%，目标25%，在容忍上限55%内"
    - "7天订单6单，目标8单，达成率75%"

strategy_context:
  ad_direction: "push_natural_rank"
  ad_purpose: "ranking"
  product_position: "P1"
  product_stage: "pushing"
  season: "旺季"
  target_acos: "25%"
  target_budget: "$50/天"

memory_context:
  cooldown_active: false
  recent_effectiveness: "partially_achieved"
  reverse_risk: false

conflict_resolution:
  conflict_detected: false

action_plan:
  - order: 1
    action: "balance_maintain"
    target: "主推词Bid"
    reason: "ACOS在容忍上限内，ranking目的保护主推词不降"
    base_magnitude: "0%"
    stage_modifier: "×1.2（pushing）"
    season_modifier: "×1.2（旺季）"
    final_magnitude: "0%（维持）"
  - order: 2
    action: "bid_down_for_acos"
    target: "非排名词"
    reason: "非排名词ACOS超标，可降；不影响排名任务"
    base_magnitude: "-5%（小降）"
    stage_modifier: "×1.2"
    season_modifier: "×1.2"
    final_magnitude: "-7%"
  - order: 3
    action: "balance_maintain"
    target: "预算"
    reason: "排名任务不能断供，预算维持"

review_level: "AUTO_BATCHABLE"
kb_refs:
  - "17号§3.1 push_natural_rank × HIGH_ACOS_LOW_ORDER"
  - "17号§2 有效容忍上限计算"
  - "15号§4 ACOS容忍度矩阵"
  - "15号§1 Bid幅度公式"
```
