# 本地改动汇总（2026-06-04）

> **仓库**：`D:\cor_wkplace\AD_assistant_agent-v3.0.2`  
> **分支**：`master`（与 `sale_ai/master` 已同步至提交 `c1e69a1`）  
> **本文范围**：仅 **尚未提交 / 未推送** 的改动，以及 **`.gitignore` 内本地运行产物**（`cursor临时文件/`）。  
> **不含**：已提交并推送的 `c1e69a1` 程序变更（见该 commit 或 `git show c1e69a1`）。

---

## 1. Git 状态一览

| 类型 | 数量 | 说明 |
|------|------|------|
| 已修改未暂存 | 2 | 应用代码 |
| 未跟踪（新文件） | 10 | 5 个程序 + 5 个文档 |
| 已暂存 | 0 | — |

```text
modified:   ad-direction-agent/app/data/mcp_db_context.py
modified:   ad-direction-agent/app/persistence/erp_writer/listing_context.py
untracked:  ad-direction-agent/app/data/known_listings.py
untracked:  ad-direction-agent/scripts/apply_strategy_preset.py
untracked:  ad-direction-agent/scripts/e2e_test_b0cgh9.py
untracked:  ad-direction-agent/scripts/smoke_api_test.py
untracked:  ad-direction-agent/scripts/wizard_kb_experiment.py
untracked:  docs/ERP数据库设计问题清单.md
untracked:  docs/ERP测试库现状说明.md
untracked:  docs/WHP-Tab4-对接说明（发给WHP）.md
untracked:  docs/WHP-Tab4-方向推荐字段契约.md
untracked:  docs/WHP-前端对接完整说明.md
```

---

## 2. 应用代码（已修改，未提交）

### 2.1 `ad-direction-agent/app/data/known_listings.py`（新增）

| 项 | 内容 |
|----|------|
| 作用 | Doris 慢查询/超时/不可达时，为 MCP 与 ERP 提供 **静态 listing 上下文** |
| 数据结构 | `KNOWN_LISTINGS: dict[parent_asin → (parent_seller_sku, shop_id, site_code, shop_account)]` |
| 当前条目 | `B0CGH9QRKK`、`B0B7S3PWWB`、`B0DDWX58G3`、`B0GCZHVBWN`、`B0DNZNWGX5`（SKU/店铺来自 Doris 实测或历史写入） |
| 导出函数 | `known_mcp_context(parent_asin)` → 供 `mcp_db_context` 构造 `McpDbContext` 字段 |

**各 ASIN 缓存值（本地维护，非自动同步 Doris）：**

| ASIN | parent_seller_sku | shop_id | shop_account |
|------|-------------------|---------|--------------|
| B0CGH9QRKK | WY03235-80D新款 | 1622 | am_vivibeautyus |
| B0B7S3PWWB | FS02721-3pcs | 1622 | am_vivibeautyus |
| B0DDWX58G3 | 8pcs-fishnet | 1622 | am_vivibeautyus |
| B0GCZHVBWN | FS03929-新宽三角bikini | 72549 | am_yidong_US |
| B0DNZNWGX5 | FS03024-longsleeve-FU | 55454 | am_slow_US |

---

### 2.2 `ad-direction-agent/app/data/mcp_db_context.py`（修改）

| 变更点 | 说明 |
|--------|------|
| 依赖 | `from app.data.known_listings import known_mcp_context` |
| `read_timeout` | 由固定 `20` 改为 `max(90, int(settings.mcp_context_timeout or 30))`，降低 Doris 慢查询误超时 |
| `_context_from_known()` | **新增**：从 `known_listings` 组装 `McpDbContext`，打日志 `MCP 上下文(缓存)` |
| 失败回退 | `OperationalError`、其它异常、或 listing **无记录** 时，不再直接 `return None`，改为 `return _context_from_known(asin)` |

**影响链路**：`resolve_mcp_context_from_db` → `CampaignFetcher.fetch_campaigns` → `DataAggregator` / `mcp-query` skill → 批量 `campaign_kb_experiment` 能否拉到活动数据。

---

### 2.3 `ad-direction-agent/app/persistence/erp_writer/listing_context.py`（修改）

| 变更点 | 说明 |
|--------|------|
| 常量迁移 | 删除本文件内 `_KNOWN_LISTINGS` 二元组定义，改为 `from app.data.known_listings import KNOWN_LISTINGS` |
| 已知 ASIN 回退 | 元组由 `(sku, shop_id, site)` 扩展为 `(sku, shop_id, site, shop_account)` |
| **修复** | 回退分支不再把所有已知 ASIN 的 `shop_account` 写死为 `am_vivibeautyus`；按表内店铺账号写入（如 `am_yidong_US`、`am_slow_US`） |

**未改部分**：`_lookup_from_erp_summary()`（仍连 `192.168.2.51` ERP 库查 `t_advert_agent_modify_suggest_summary`）、`resolve_listing_context_async` 主流程顺序（Doris → ERP 摘要 → known → 抛错）。

---

## 3. 脚本（新增，未提交）

### 3.1 `ad-direction-agent/scripts/wizard_kb_experiment.py`

| 项 | 内容 |
|----|------|
| 用途 | 对指定 ASIN **跑完整 Wizard 管线**并导出 `{asin}_wizard.jsonl` |
| 依赖 | `WorkflowOrchestrator` + `DataAggregator` + `LLMReasoner` + `MySQLStateManager`；`sys.path` 含 `ad-purpose-agent` |
| 策略预设 | 与批量脚本一致：腰部 / 推进期 / 旺季准备 |
| 流程 | `confirm_strategy` → `confirm_tactics` → `get_diagnosis` → `get_unified_recommendation` → `confirm_execution` → `run_validation_and_report` |
| 输出字段 | `directions`、`validations`、`decisions`、`decision_meta`、`advert_direction_types`（经 `map_direction_type`）等 |
| CLI | `--asins`（必填）、`--output-dir`（默认 `../cursor临时文件`）、`--days`（默认 7） |

**与已提交代码关系**：`scripts/erp_db/run_erp_batch_sequential.py` 已提交，其 `_wizard_one()` **子进程调用本脚本**；本文件未入库则他人 clone 后批量 Wizard 步会缺文件。

---

### 3.2 `ad-direction-agent/scripts/apply_strategy_preset.py`

| 项 | 内容 |
|----|------|
| 用途 | 批量 ASIN 写入 MySQL **长期策略层**预设 |
| 预设值 | `product_level=腰部`、`product_stage=推进期`、`season_stage=旺季准备` |
| 副作用 | 默认 `--clear-tactics-cache`：清除 workflow 中 `target_scores`、`keyword_analysis` |
| CLI | `--asins` 逗号分隔 |

**调用方**：`run_erp_batch_sequential.py` 在 Campaign/Wizard 前默认执行（非 `--erp-only` 时）。

---

### 3.3 `ad-direction-agent/scripts/e2e_test_b0cgh9.py`

| 项 | 内容 |
|----|------|
| 用途 | 单 ASIN（硬编码 `B0CGH9QRKK`）**基础设施连通性**检查 |
| 检查项 | MySQL state（`settings` + `get_state_manager`）、Redis ping、ERP 库 `t_advert_agent_modify_suggest_summary` 行数 |
| 输出 | `cursor临时文件/B0CGH9QRKK_e2e_report.json` |
| 退出码 | 三项均 ok 为 0，否则 1 |

---

### 3.4 `ad-direction-agent/scripts/smoke_api_test.py`

| 项 | 内容 |
|----|------|
| 用途 | **无外部 DB** 的 FastAPI 冒烟（`DATA_SOURCE=mock`、`STATE_BACKEND=json`、`REDIS_ENABLED=false`） |
| 用例 | `/health`、`/announcements`、`strategy/options`、`diagnosis`、`wizard/state`、`campaign/analyze`（空 asin） |
| 工具 | `TestClient(app)` |

---

## 4. 文档（新增，未提交）

| 文件 | 版本/受众 | 主要内容 |
|------|-----------|----------|
| `docs/WHP-Tab4-方向推荐字段契约.md` | 2026-06-03，WHP | Tab4 表名、`content_json` 的 `display_*` 字段、列绑定、解析示例、检查清单 |
| `docs/WHP-Tab4-对接说明（发给WHP）.md` | 2026-06-03，WHP | 现象（JSON 正文）、表结构、简短对接步骤；指向契约与枚举文档 |
| `docs/WHP-前端对接完整说明.md` | 2026-06-04，WHP | **合并版**：Tab4 渲染问题 + 枚举码表 + 组合类型；说明「发给前端一份即可」 |
| `docs/ERP数据库设计问题清单.md` | 2026-06-03 | ERP 18 表设计问题：collation、shop_id、旧表粒度、跨表规范等（非写入逻辑） |
| `docs/ERP测试库现状说明.md` | 2026-06-03 | 全表字段实测说明、存储约定、样本 decision_id |

**说明**：已提交仓库的 `docs/WHP-Tab4渲染与组合枚举说明.md` 为精简合并版；上表三份 WHP 文档为本地额外副本/详版，**内容有重叠**，未进 git。

---

## 5. 本地运行产物（`cursor临时文件/`，gitignore）

以下为今日批量/单测生成，**不进入版本库**，但与本地工作直接相关。

### 5.1 批量状态

**文件**：`erp_batch_sequential_status.json`  

| ASIN | 结果 | decision_id | modern_card 行数 |
|------|------|-------------|------------------|
| B0DDWX58G3 | ok | dec56ed774997e96806640f1a4b182c6 | 9 |
| B0CGH9QRKK | ok | dec8fafa8afb8a3b58b67a8d3c4bf3da | 56 |
| B0GCZHVBWN | ok | dec365dc8e8b0c26622304d0ed7e24e0 | 27 |
| B0DNZNWGX5 | ok | dec57bca360133d118907ea6f850573d | 130 |

（时间在 `2026-06-04` 约 19:26–19:35 UTC+8 时段；依赖 `known_listings` + Doris/MCP 可用。）

### 5.2 每 ASIN 中间文件与写入报告

| 模式 | 说明 |
|------|------|
| `{ASIN}_kb.jsonl` | `campaign_kb_experiment.py` 产出，供 `write_erp_all` |
| `{ASIN}_wizard.jsonl` | `wizard_kb_experiment.py` 产出 |
| `{ASIN}_write_report_batch.json` | `write_erp_all` 写入统计（decision/summary/card/pending/direction_detail 等） |

**当前存在的 ASIN**：`B0DDWX58G3`、`B0CGH9QRKK`、`B0GCZHVBWN`、`B0DNZNWGX5`（四者均有上述三类文件）。

**B0DDWX58G3 写入摘要示例**（`write_report_batch.json`）：

- listing：`shop_id=1622`，`parent_seller_sku=8pcs-fishnet`
- `modern_card=9`，`direction_detail=4`，`purpose_score=4` 等

### 5.3 B0CGH9QRKK 额外日志/报告

| 文件 | 用途 |
|------|------|
| `B0CGH9QRKK_api_analyze.json` | API 分析响应快照 |
| `B0CGH9QRKK_e2e_report.json` | `e2e_test_b0cgh9.py` 输出 |
| `B0CGH9QRKK_e2e_full_report.json` | 扩展 E2E 报告 |
| `B0CGH9QRKK_campaign_run.log` | Campaign 运行日志 |
| `B0CGH9QRKK_wizard_run.log` | Wizard 运行日志 |

### 5.4 失败批次残留（可选清理）

更早一次批量（Doris 超时、无 `known_listings`）曾生成 **空 Campaign** 的 `*_kb.jsonl`（`total_campaigns: 0`）。若与成功批次同名覆盖，以 **5.1 成功状态** 对应文件为准；无需提交。

---

## 6. 本地执行过的命令（非文件变更，供追溯）

| 操作 | 命令/入口 | 结果 |
|------|-----------|------|
| 首次四 ASIN 批量 | `python scripts/erp_db/run_erp_batch_sequential.py --asins B0DDWX58G3,B0CGH9QRKK,B0GCZHVBWN,B0DNZNWGX5` | 失败 4/4（Doris 上下文超时 → 0 活动） |
| 加 `known_listings` 后重跑 | 同上（或等价分步 Campaign/Wizard/ERP） | 成功 4/4（见 5.1） |
| Listing 解析探测 | `scripts/erp_db/resolve_listing_context.py`、`scripts/probe_db_hosts.py` | 验证 SKU/店铺 |
| ERP 推送（已提交功能） | `sale_ai master` ← `c1e69a1` | 远程已含 auto_push/Tab4/枚举，**不含**本节 2–4 本地文件 |

---

## 7. 与已提交代码的依赖关系

```text
run_erp_batch_sequential.py (已提交)
  ├── apply_strategy_preset.py          ← 本地未提交
  ├── campaign_kb_experiment.py         ← 已提交
  ├── wizard_kb_experiment.py           ← 本地未提交
  └── write_erp_all.py → push_full_to_erp (已提交)

CampaignFetcher / ERP 写入
  ├── mcp_db_context.py                 ← 本地修改（known 回退）
  ├── known_listings.py                 ← 本地新增
  └── listing_context.py                ← 本地修改（shop_account 修复）

WHP 联调文档
  ├── WHP-Tab4渲染与组合枚举说明.md      ← 已提交
  └── WHP-* 三份 + ERP 设计/现状两份      ← 本地未提交
```

---

## 8. 建议后续（可选）

1. **提交程序**：`known_listings.py`、`mcp_db_context.py`、`listing_context.py`、四个 `scripts/*.py`（批量管线完整可复现）。
2. **文档取舍**：WHP 对外只保留一份（建议 `WHP-Tab4渲染与组合枚举说明.md` 或 `WHP-前端对接完整说明.md`），其余本地副本可不提交或归档。
3. **硬编码**：`listing_context._lookup_from_erp_summary` 与 `known_listings` 中的 ERP 密码/连接宜改为 `erp_connection_kwargs()`（与 `auto_push` 一致），避免双份配置。

---

## 9. 未纳入本文的其它工作区

- **`AD_assistant_agent-v3.0.1`**：另有大量未提交改动（`erp_db/` 目录、根目录 ERP 文档、`strategy.py` 等），与 v3.0.2 非同一 git 工作树；若需单独清单可另建文档。
- **`.env`**：本地存在且 gitignore，含 `DATA_SOURCE=mcp`、Doris/MCP 网关/ERP 连接等，**未列入**上文文件列表。

---

*文档生成：2026-06-04，基于 `git status` 与 `cursor临时文件/` 目录实测。*
